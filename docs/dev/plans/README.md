# Implementation & decision records

One markdown per substantial Reikai feature or initiative: a developer-facing record of what was built and why. These describe current behavior and the decisions behind it, not the chronology of how a plan evolved.

These are distinct from the user-facing docs ([docs/README.md](../../README.md)) and the dev process / reference docs ([docs/dev/README.md](../README.md)); the plan docs cross-link those rather than duplicating them. For which doc owns what and what to update when, start at [docs/dev/README.md](../README.md).

The format these follow, and the rule for what earns a doc here, live in [.claude/rules/roadmap-plans.md](../../../.claude/rules/roadmap-plans.md). The forward-looking backlog is [ROADMAP.md](../../../ROADMAP.md).

## Foundation

- [Documentation website](website.md): the VitePress docs and download site in its own repo, reading the user docs out of this one at build time so there is never a second copy.
- [Reikai → Mihon rebase overview](rebase-overview.md): why Reikai moved off Yōkai onto Mihon, the phased structure (P0-P9), the `// RK` patch convention, and the decisions behind it.
- [ViewModel migration](viewmodel-migration.md): taking Mihon's move off Voyager `ScreenModel` onto AndroidX `ViewModel` across Reikai's own screens too, phased so each cluster ships compiling, with the R8 risk proven up front. Deferred (attempted once, reverted, parked pending an upstream release).
- [Metro DI migration](metro-di-migration.md): taking Mihon's move from Injekt to Metro (mihonapp/mihon#3608) across Reikai's own code too, as a hybrid that keeps Injekt alive for the extension contract. In progress: phases 0 to 4 have landed, the migrations, the Reikai-owned tail and cleanup remain. Carries the phase sequence, the traps and the riding-along upstream changes.
- [Legacy Yōkai database import](legacy-yokai-import.md): recovering a user's manga + novel library when they update in place from a pre-rebase Yōkai build, instead of crashing on the now-incompatible shared `tachiyomi.db`.
- [FlareSolverr Cloudflare bypass integration](flaresolverr-integration.md): the optional bypass proxy (Solverr / Byparr / FlareSolverr) as a WebView fallback for Cloudflare-gated sources, proxy-mode not cookie-replay, with all mechanics in a net-new `FlareSolverrClient` and a minimal `// RK` detection island.
- [In-app interactive Cloudflare (Turnstile) solver](turnstile-solver.md): the optional in-WebView solver that ticks the verification box an interactive challenge waits on, pressing it with real input whether or not there is a window, which is what lets a library update solve one; off by default, with the dead ends recorded so they are not re-tried.
- [Documentation system](docs-system.md): the docs revamp itself, the topic map and file-to-file workflow front doors ([docs/README.md](../../README.md), [docs/dev/README.md](../README.md)) that route "where does this go" and "what do I update for X", and the single-owner-per-fact rule that de-drifted the records.

## Library & shell

- [Library screen carry (P2)](library-screen-carry.md): how Reikai's single-list hopper library, multi-select actions, and opt-in update-errors screen ride Mihon's data pipeline via a parallel `reikai.*` renderer plus `// RK` islands.
- [Library tabbed shell (Manga + Novels)](library-tabbed-shell.md): one Library and Browse surface for both manga and novels via a content-type chip, with novels routed through Mihon's existing views.
- [Browse Feed tab (+ saved searches)](browse-feed-tab.md): rows of a source's latest / popular / saved-search results as a Browse surface, ported from Komikku (whose saved-search layer is TachiyomiSY's, absent from Mihon entirely). Opt-in on both surfaces, one saved-search table keyed by the neutral `SourceKey`, and the feed built as a second consumer of the global-search fan-out rather than a copy of it. Shipped: saved searches on the catalogue, the opt-in Feed tab over both content types, adding to the library from a cover, and backup. The per-source feed was built and dropped on sight, since a catalogue already answers what it asked.
- [Library sort: global + per-category overrides](library-sort-overrides.md): with "Per-category setting for sort" on, one global sort every category follows unless explicitly overridden, via a manga `CUSTOMIZED` flag bit mirroring novels, override-aware reads, a shared effective-sort header, and a reset-to-global action.

## Manga

- [Manga details parity (P3)](manga-details-parity.md): reaching parity on Mihon's manga details screen: cover-accent backdrop, swipe-to-refresh, two-finger range select, and the merge / Manage-sources additions.
- [Auto webtoon mode](auto-webtoon-mode.md): open a manhwa, manhua or webtoon in long-strip mode automatically, classified from genre tags and source name across every member of a merge group, computed per open and never stored.
- [Merge-aware manga reader](merge-aware-manga-reader.md): read a merged manga straight through all its sources in the reader (unified list with per-source labels, cross-source prev/next, per-source side effects), via a thin Reikai layer over Mihon's reader. The reader-layer record; reader scope-by-context is covered by the merge system rebuild.
- [Merge system rebuild](merge-system-rebuild.md): the multi-source merge (manga + novels) rebuilt onto a persisted group identity (the `merge_group` + per-type member tables) instead of a per-call derivation from preferences, fixing silent membership corruption, unmerges lost on restore, two disagreeing group definitions, browse pre-merging by title, and a library scan per open. Supersedes the pref-era merge-engine records. Complete on feat/0.4.0: all phases done and on-device verified (including slice 5, the chip-row collapse). The broader parallel-component consolidation onto the Entry* seam is a separate deferred initiative, planned in [merge-component-consolidation.md](merge-component-consolidation.md).
- [Recommendations & related carousel (P6)](recommendations.md): five suggestion streams merged, taste-reranked, tracker-gated cross-recs, and a See-all bulk-add grid.
- [Adult content from trackers](recommendations-adult-content.md): a tracking-wide setting that kept sexually explicit titles out of the taste profile, the carousel and Fill-from-tracker, with a user-editable tag correction list where a tracker cannot answer. Removed in full after an evaluation found its reach did not justify it. Kept for the measurements that decided each tracker's signal, the two services declined on evidence, and the Kitsu account-level gate that still shortens Fill from tracker.
- [MangaDex enhanced source](md-enhanced-source.md): wrap the installed MangaDex extension with metadata, OAuth login, an MDList tracker, follows sync, and a settings hub, reusing the EXH enhancement machinery. Feature-complete; the rest of Phase 6 was evaluated and deliberately dropped.

## Adult sources

- [Adult-source (EXH) subsystem](exh-subsystem.md): built-in E-Hentai / ExHentai, enhanced-source metadata indexing + library tag search, metadata viewer, account uconfig sync, favorited-gallery update checker, and a one-way favorites backup. Ported from Komikku, re-typed onto Mihon, wired in via `// RK` islands; the lighter-slice descopes and the path to Komikku parity.
- [Adult-source browse parity](adult-browse-parity.md): why the built-in EH browse only loads its first page and shows bare rows, traced to the dropped `MetadataMangasPage` carrier + `EHentaiPagingSource` cursor + metadata rendering, and the two-piece port that reached Komikku parity. Shipped.
- [Library tag-search engine](library-tag-search.md): the structured tag query language (`namespace:tag`, wildcards, exclusion, exact, aliases) for the library search bar, a trimmed in-memory port of Komikku's `exh/search` with wildcards made to actually work.

## Light novels

- [Novel browse & sources](novel-browse.md): how LN plugins install, fold into the unified Browse, fan out across global search, and reach manga-browse parity per source.
- [Novel details screen](novel-details.md): the light-novel detail hub at manga parity (tinted backdrop, chapter list, downloads, overflow actions), built on a Voyager `NovelScreen` + `NovelDetailsViewModel`.
- [Novel reader](novel-reader.md): WebView text canvas rendering plugin HTML inside native Compose chrome, with scroll resume, read-state sync, and per-novel display settings. Read-aloud (TTS) shipped with background playback and a lock-screen media notification.
- [Novel reader: tsundoku as the foundation](novel-reader-tsundoku.md): evaluation of tsundoku (a maintained, Apache-2.0 Mihon-fork novel reader) as the basis for replacing the novel reader. The migration plan and the feature harvest both moved to the reader surface doc, which takes the portable layers under both of tsundoku's novel viewers: their own viewers are welded to their forked `ReaderActivity`.
- [Headless LN plugin host (QuickJS)](novel-plugin-host.md): why and how novel sources run in a headless QuickJS runtime instead of a WebView, so they work in the background, with polyfill completeness as the make-or-break constraint.
- [Novel categories & hopper](novel-categories.md): the Novels tab gains its own categories, the shared category hopper + jump-to-category sheet, a tab-aware Display sheet, and LN plugin update detection.
- [Novel background update job](novel-update-job.md): the WorkManager worker that refreshes favorited novels on a schedule, with per-category gating and a smart-update skip filter, the novel twin of Mihon's library updater.
- [Novel tracking](novel-tracking.md): bind novels to any of the seven trackers that can tell novels apart and sync reading progress, reusing Mihon's trackers, group-aware across merged sources.
- [Novel backup & restore](novel-backup.md): light novels ride the same backup file as manga (chapters, history, tracks, categories, id-remapped merges) plus an installed-sources record that reinstalls extensions and re-downloads plugins on restore.
- [Novel parity backlog (shipped)](novel-parity-backlog.md): the manga↔novel parity features (history, migration, library modes, reader, stats, browse) that make novels feel native, with per-item commit SHAs and the deliberate trims.
- [Novel migration redesign](novel-migration-redesign.md): covers and chapter-count signal on results, a manga-style source-selection pre-step, and a source-to-target comparison row. Shipped.

## Unified surfaces (manga + novel)

- [Unified Updates tab](unified-updates.md): one Updates feed interleaving manga + novel behind an All / Manga / Novels chip, with shared filters, by-category, and merge-aware group-by-series.
- [Settings restructure](settings-restructure.md): an owner-approved settings rewrite. Reader splits into shared rows plus manga and novel drill-downs, About is rebuilt on the preference DSL so it reaches search, and source settings stop living in four places. Ruled 2026-09-03; all three passes shipped.
- [Content-layer reader surface](content-layer-reader-surface.md): the reader takeover. One host Activity keeps the window responsibilities, a shared engine owns navigation, position, chrome, the action verbs and the viewer contract over two providers, and novels move onto two tsundoku-ported rendering modes, native text and a first-party WebView. `ReaderViewModel` stays live as the manga provider; the image viewers are not diverged further, and the takeover owns the novel settings whose implementation lives in the vendored JS being deleted. Ruled 2026-09-03 and re-grounded the same day; steps 0 to 4b and 6 have shipped, step 5 (the seamless chapter window) is next.
- [Unified reader (shared chrome)](unified-reader.md): superseded by the reader surface above. Kept as the record of what shipped (the novel reader on the Compose shell) and of why the single-Compose-shell approach (Option A) was built and reverted, which is the most useful part of it for the takeover.
- [Content layer architecture](content-layer-architecture.md): the deeper unification that extends the Entry* UI-leaf seam down into the ScreenModel behavior, a Reikai-owned shared behavior + UI layer over a neutral `Entry` vocabulary with thin Manga/Novel adapters (the manga model stays live and synced), a delete-and-manifest dead-file policy replacing keep-inert, and the reader takeover folded in as the final phase. In progress: the details, library, browse, migrate and recents surfaces are closed out; downloads and the reader takeover remain.
- [Content layer: the details surface](content-layer-details-surface.md): the first surface cut over, one shared behavior + screen driving both manga and novel details over the neutral Entry* UI via a `MangaEntryAdapter` (reads the live `MangaViewModel`) and a `NovelEntryAdapter` (dissolves `NovelDetailsViewModel`), sequenced 0 to 5 with the manga body-UI migration isolated as the second checkpoint. All steps shipped; the surface has been exercised on-device throughout the library program since.
- [Content layer: the library surface](content-layer-library-surface.md): the second surface, collapsing the manga and novel libraries onto the shared content layer. The library UI is already shared (novels ride as a negative-id `LibraryItem`), so the remaining forks are the duplicated `NovelLibraryViewModel` pipeline and the id-disguise. Phase 1 extracts the shared `buildState` pipeline (and levels the novel lewd filter up), Phase 2 adds the neutral state + adapters + `EntryId`, Phase 3 reconciles the rest. Shipped in full, through the All-first takeover recorded in the All-chip doc below.
- [Content layer: the browse surface](content-layer-browse-surface.md): the third surface, collapsing the browse behavior twins (the shared bulk-favorite engine, the shared remove dialog and default-category kernel) and closing the ruled parity gaps (hide-in-library, per-language source switch; NSFW gated, the plugin format has no flag). Shipped in full: the verb collapse (steps 1-6, which gives the default-category rule one owner across every add path), then the takeover that overruled the no-takeover decision, assembling each of the four multi-source lists once over two providers and serving both per-source catalogues from one screen.
- [Content layer: the recents surface](content-layer-recents-surface.md): the fifth surface and the third orchestration takeover, collapsing history and updates into one engine that also serves the combined Recents feed, built All-first (All is the real feed, the chips are predicates) with a neutral item that separates a row's identity from the chapter it opens. Carries the owner's rulings (four modes, always collapse merged series, no paging, one category filter per rendered surface) and the defect list the takeover has to fix rather than carry across. In progress: steps 1 through 10 have landed, so both tabs render the shared screen over the engine, an Updates row swipes to read, bookmark or download, and a Settings toggle collapses the two tabs into one Recents tab holding Grouped, Feed, History and Updates. Only the closing behaviour inventory is left, and it sweeps for the one-lane assumption that produced four defects during the tab-set step; History's time grouping was cut to the roadmap.
- [Recents: the continue-reading row](recents-continue-reading-row.md): moving a combined-mode read row off the chapter you finished and onto the chapter it will open, whole rather than by halves, so its name, dimming, bookmark, progress, tap and download control all mean the same chapter. Carries the measurements that ruled out resolving in bulk (a fully-read library would load 195,140 chapters per pass, and the merge-aware SQL answer scans the same rows), the lazy per-rendered-row design that replaces it, and why History keeps naming the record. Not started; the target rule it builds on is shipped.
- [Content layer: the migrate surface](content-layer-migrate-surface.md): the fourth surface and the second orchestration takeover (the program's third amendment), one Reikai-owned migration flow (config, tuning sheet, count-fork into search or list, confirm with flags) over per-type adapters bottoming out at the two migrate use cases, replacing the structurally diverged manga and novel flows. Shipped, including the close-out of the nine behaviours the takeover inventory found missing.
- [Content layer: the add-to-library flow](content-layer-add-flow.md): one add sequence for both content types across every surface that can add an entry, replacing the per-surface copies of the duplicate check, the favorite write and the category decision. The order is ruled (resolve, then favorite, then file, with a picker deferring both writes to its confirm) and add-time grouping keeps its favorite-up-front carve-out. The decision, the write order and the recents add verb have shipped and are device-verified; the two history models left the add path entirely when their dead members went, so the shared duplicate dialog is what remains.
- [Library: the All chip](library-all-chip.md): the forward build plan for one library listing manga and novels together, with All as the real view and the Manga / Novels chips as filters over it rather than two screens with a switch. Providers yield rows, the shared layer assembles, sorts and buckets them, and the chip becomes a predicate. Carries the owner's rulings (counts follow the chip, a chip-emptied category is hidden) and the gotchas. Shipped: the complete record of the finished library surface, steps 0 through 6.
- [Category schema unification](category-schema-unification.md): retire the forked `novel_categories` table into the shared `categories` table with a `content_type` column (manga / novel / universal), so a mixed "Reading" bucket has an answer. The data half of the "All" library chip. Shipped and device-verified, plus the follow-on category-preference cleanup.
- [Merged read state](merged-read-state.md): one definition of read, bookmarked and downloaded across a merge group, so the library badge, the details "All" list, continue-reading, download-next, the reader and history all agree instead of each reading whichever source's copy won the dedup. Library and details halves shipped; the reader-adjacent remainder is open.
- [Merge component consolidation](merge-component-consolidation.md): collapse the parallel manga/novel merge components (the two near-twin managers, and the per-ScreenModel group read/observe wiring) onto the shared Entry* seam, so a merge behavior is written once. The last fork the merge-system rebuild left. Phase A and Phase B shipped.
- [Unified content UI (manga + novels + adult)](unified-content-ui.md): history of the UI-leaf seam era, the shipped foundation the content layer builds on (list surfaces, cover dialog, details screen P1-P6). Nothing here is forward-looking: the binding rules are `.claude/rules/content-layer.md` and the forward program is content-layer-architecture.md.
- [Manga/novel parity: drift fixes + twin collapse (round 2)](content-parity-drift-and-collapse.md): the next unification wave from a 2026-07-13 deep audit, fix the silent behavioural drifts in the still-twin surfaces (merged-novel read/bookmark propagation, browse toolbar, global-search sort, first-download prompt, migration spinner), then collapse browse / global search / library-settings / notes / migration onto the shared Entry* seam. Phases 1-7 done, minus the collapses assessed and declined.
- [Download-queue unification](download-queue-unification.md): manga (View) + novel (Compose) download queues merged onto one shared Compose component with a neutral row model, source grouping, the full move/cancel overflow menu, and a Reorder Mode for whole-source ordering (the reorder library can't do in-place header drag). Shipped.
- [Novel download storage re-key](novel-download-storage.md): novel downloads moved off unstable numeric-id paths + a wiped DB flag onto manga's stable-name folders + a disk-scan cache, so they survive reinstall / restore / storage-move, with rename-on-sync, temp-then-rename writes, and a one-time relocation migration. Sets up the download-subsystem unification (Road B).
- [Kitsu on one API](kitsu-single-api.md): moving Kitsu's last two calls (the taste-profile library pull and Fill-from-tracker's metadata read) off JSON:API onto GraphQL so the tracker speaks one API. Carries the schema grounding read from Kitsu's own server source, why a pre-production API was accepted knowingly, and the two defects only a device run caught: statuses that would all have read as unknown on a casing change, and a DTO reused across queries that coupled them to each other's field lists. Shipped.
- [Novel-specific trackers](novel-specific-trackers.md): RanobeDB, MyNovelList, NovelList and NovelUpdates, the four novel-first tracking services, grounded by reading tsundoku's and IReader's working implementations rather than their docs. Carries what not to copy (blocking writes inside coroutines, writes that cannot fail, session cookies in the log), the two identity problems to settle first, and why RanobeDB's newly token-authenticated API retires a quarter of tsundoku's file. RanobeDB (series-bound, pasted-token or WebView cookie login), NovelList and NovelUpdates are built; MyNovelList was assessed and declined, its API sound but its catalogue empty and its host abandoned.
- [One selection kernel](selection-kernel.md): range selection was written seven times in two incompatible algorithms across the app, and the copies had already drifted apart on the anchor, on select-all and on invert. Collapses all of them onto one pure `EntrySelection` over a `SelectionState` that carries its own anchor, fixes the stale-window bug that let a range skip a chapter you had dropped, and adds range selection to the two surfaces that never had it. Shipped and device-verified; carries the family-A ruling and the one gesture the app deliberately leaves non-uniform.
