# Content-layer recents surface (history, updates and the combined feed)

> **Standing rules for every session working this surface.** The program rules bind here and are not restated: they live in [.claude/rules/content-layer.md](../../../.claude/rules/content-layer.md), which loads every session (write-once, typed capability slots over nullable soup, derive-or-do-not-own, the pin-once ladder, decline expiry). For the program context read [content-layer-architecture.md](content-layer-architecture.md) and [unified-content-ui.md](unified-content-ui.md) first. Two more bind on this surface specifically. **All-first**, taken from the library ([library-all-chip.md](library-all-chip.md)): All is the real feed and the Manga and Novels chips are predicates over it, so anything describing the list or a row in it is one value, and only what describes a content type stays per type. And **every keyed structure keys on `EntryId`**, never a raw `Long`: the two id spaces overlap, so a `Long`-keyed map over a mixed feed cross-wires silently instead of crashing. The surfaces being replaced broke the first rule at the state level (two search queries, two loading flags, two dialog channels and two selections on one screen) and paid for it with a clear-all that prompts twice, a novel feed that gates the manga chip's spinner, and a long-press range that selects rows the user never swept.

## Goal

One Reikai-owned recent-activity surface serving manga and light novels across four modes: a combined title-centric feed, a combined grouped digest, Updates, and History. A Settings preference collapses the separate Updates and History tabs into one Recents tab holding those modes; with the preference off (the default), today's two tabs remain, rendering the same engine at two of its modes.

This is the fifth surface of the content-layer program ([content-layer-architecture.md](content-layer-architecture.md)) and its third orchestration takeover, after the library and the migrate flow. It supersedes the forward half of [unified-updates.md](unified-updates.md), which stays the record of the shipped unified Updates tab.

## Why

Two forces meet on these two screens, and doing either alone reworks the other.

**The behaviour is still forked.** History and Updates were built when the program's seam was drawn at the UI leaf, so they share row composables over per-type feeds and nothing below. The measured shape of the fork: the novel Updates feed filters nothing in SQL where the manga feed filters four axes; both feeds resolve category membership per row in Kotlin behind a screen-lifetime cache, on both types; the "started" predicate disagrees with its SQL twin in the negated branch; and list-level state (search, loading, dialogs, selection, clear-all) is stored twice, once per content type, on a single screen. These are duplicate-implementation defects, the failure mode the program's 2026-08-06 ruling exists to remove.

**The combined feed reshapes the same screens.** `unseensnick/Reikai#57` asks for a title-centric feed merging recently read, recently updated and newly added, opening the most relevant chapter, with Updates and History preserved. The owner's answer is a preference that collapses the two tabs into one Recents tab with modes inside, so nobody loses one-tap access. Building the behaviour seam first and the tab shape second would rework both, so they are one piece of work.

**The sync cost is the lowest of any surface taken over so far.** Twelve months of upstream history on both screens: History took five commits, every one cross-cutting mechanical work (the datetime conversion, the ViewModel migration, a compose-workaround removal, the immutable-collections drop, preference properties) and none behaviour-specific; Updates took thirteen, of which three are behaviour (`mihonapp/mihon#3589`, the earlier filters commit, and a selection-signature cleanup) and the rest mechanical or revert churn.

## Approach

### The amendment: scope and hard lines

This is the fourth orchestration amendment to the program, recorded alongside the others in [content-layer-architecture.md](content-layer-architecture.md). It supersedes the history-and-updates part of the 2026-08-06 "redo at the behaviour seam" ruling by naming what the redo actually is.

- **The takeover owns pure UI plus orchestration**: the two Reikai shells (`ReikaiUpdatesScreen`, `ReikaiHistoryScreen`) and their row builders, the two Reikai novel models (`NovelUpdatesViewModel`, `NovelHistoryViewModel`, which dissolve into providers), the bodies of the two Voyager tabs, and the category-filter control (`ReikaiUpdatesCategoryFilter`). The shared row leaves (`EntryUpdatesRow`, `EntryHistoryRow`) survive into the new engine rather than being replaced.
- **It also reaches, more lightly, into shell and plumbing files** the bullet above does not cover, and they are named so the scope is not a surprise mid-build: `ReikaiSourcePreferences` and `CategoryIdPreferences` (the retiring keys), `PreferenceRestorer` (their skips), `UpdatesFilterDialog` and `UpdatesSettingsViewModel` (the filter sheet), `historyView.sq` (a Reikai-only category predicate, since History carries a selection of its own), `CategoryFilterRow` (the shared picker's confirm, which also serves the library), `UiPreferences` and the Appearance settings screen (the new tab preference), and `HomeScreen` plus `MainActivity` (the tab set and the launcher shortcuts).
- **The engine floor stays Mihon and synced**, never reimplemented: `GetUpdates`, `UpdatesRepository` and its impl, `GetHistory`, `RemoveHistory`, `GetNextChapters`, `SetReadStatus`, `DownloadManager`, and every interactor below them. The novel sinks (`SetNovelReadStatus`, `GetNextNovelChapter`, the novel repositories, `NovelDownloadCache`) stay likewise. A step that starts reimplementing what `setReadStatus` does has gone too far.
- **`UpdatesViewModel` and `HistoryViewModel` stay live** as the manga providers, minimally patched, and are not manifested. That is this amendment's ruling, not an inherited default: the library takeover's equivalent clause is recorded as unresolved in [content-layer.md](../../../.claude/rules/content-layer.md), and the churn measured above gives no case for absorbing these two.
- **Replaced Mihon files are deleted and manifested in the same commit**, which the `pre-commit` hook enforces. See "What can and cannot be deleted", which also covers the two existing manifest rows this takeover invalidates.
- **The reader-adjacent verbs are out of scope.** Resume opens a chapter through the existing readers; nothing about reader behaviour changes here.

### All-first, taken from the library

The library's ruling ([library-all-chip.md](library-all-chip.md)) is adopted whole. That doc states it as "the chip becomes a predicate, not a branch", which is about the assembly algorithm; the mechanism underneath is worth stating separately, because it is what `LibraryEngine` actually does and it is easy to reimplement wrongly.

- **Every provider's flow always runs and is always collected. The chip then selects which providers' rows enter the assembly, and the assembly algorithm itself is chip-blind.** Nothing is gated at collection time, which is the property that matters.
- **This is about content types, not lanes.** Which lanes an engine collects is decided by the modes it renders: an engine rendering only Updates collects only the updated lane. That is not a chip branch and does not weaken the rule above, and it is what stops the two-tab shape running every query twice, since with the preference off each tab holds its own engine instance.
- **Anything describing the list is one value.** One search query, one loading flag, one dialog channel, one selection, one clear-all confirmation. Anything describing a content type stays per type: the feed queries, the download-state source, the scanlator predicate.
- **The assembly output is chip-tagged**, because the flow lags a chip flip by one emission and the screen must render an assembly only when the tag matches.
- **Counts follow the active chip**, computed over the filtered rows. **An empty section is always hidden**, no bare header.
- **Providers keep their own filtering.** They read different tables through different repositories, so filtering at the provider is honest; only assembly is shared.
- **Two deliberate departures from the template**, both because this surface has state the library does not. The library's engine forwards a search query to the providers and then reads it back out of provider state; here the engine **owns** the one query string and fans it out, because All-first says a value describing the list is one value and reading it back from two provider states is the shape that produced the two-search-fields defect. And the library derives only `searchActive` and the counts over the active providers, because it has nothing else list-shaped; here loading, emptiness and the last-updated line are derived the same way, which is what stops the novel feed gating the manga chip's spinner.

### Ruled design picks (owner, 2026-08-08)

1. **Four modes**, matching Yokai's set: a grouped digest, a flat combined feed, Updates, and History. Cut to three later if the digest does not earn its place; because modes are render policies over one item stream, that is deleting a policy rather than unpicking a design.
2. **The flat combined feed is what the request asked for.** `unseensnick/Reikai#57` describes one main entry per title, deduplicated by title, sorted by latest activity, opening the most relevant chapter, which is Yokai's `UngroupedAll` (whose own label is "All"). The digest is Yokai's `GroupedAll`, a different thing: at most four new-chapter rows, then continue-reading rows up to a combined nine across those two sections, then at most four newly-added, each under its own header.
3. **Mode names are placeholders** until they can be seen in the app. Nothing downstream depends on the strings.
4. **The combined modes always collapse merged series.** The existing `updates_group_by_series` preference survives, but only as the Updates mode's toggle; it does not gate the combined modes, which have no correct ungrouped reading.
5. **Newly-added respects the category filter but not the chapter-state filters.** A newly-added row has no chapter, so unread, started and bookmarked have nothing to test.
6. **No paging.** Each lane keeps its own bound (see below). Yokai pages every mode except the digest at fifty rows with a recursive top-up loop, and that is what forces cross-page deduplication bookkeeping.
7. **The whole Yokai affordance set is in scope** except paging: swipe actions, a per-row download button with its visibility preference, read-progress display, History's time grouping, and search within the surface.
8. **The category filter is one selection per rendered surface, covering both content types** (revised 2026-08-08, the same revision ruling 10 made to the chip and for the same reason; the first cut said one selection for the whole surface). Each rendered surface gets a master toggle, an include set and an exclude set: Updates, History, and the combined Recents tab, nine keys. The four per-type category keys and the old `updates_filter_categories` are dropped with a restore skip and no value carry-over, matching how the library retired its per-type filter keys. That retirement is why the live master switches carry an `_enabled` suffix. All six id sets register in `CategoryIdPreferences`' `sharedSets` bucket, so every cleanup path picks them up.
   - **What was right in the first cut, and survives:** with the combined tab on there is one selection over one list, which is what makes the combined modes coherent, since a feed that filters only one of its three lanes by category is not filtered by category. What was wrong is what happens with the preference off, where Updates and History are two tabs that look and behave independently and one selection would make each silently move the other.
   - **The alternative is worse than it looks.** One shared selection means History inherits whatever was set on Updates, and History has no control of its own until step 8, so a user with the preference off gets a silently shortened History and nowhere to see or change it. Per surface, History is simply unfiltered until it has a control.
   - **The shared picker's confirm becomes a delta write**, fixed once in `CategoryFilterRow` rather than per caller (step 1a). Today it replaces the whole stored set with the tri-state map it was handed, and that map only covers the categories that section displayed, so any surface showing a subset silently drops the ids it did not show. A delta write, `(stored minus the shown ids) plus the confirmed ids`, makes that impossible for every caller.
   - **This fixes a live bug in the library**, which already has one shared pair and a chip-scoped list: select categories under All, switch to the Manga chip, press OK in the picker without changing anything, and every novel-only category id is dropped from the filter. It fires in either direction and is silent. The library is a different surface, but this is the component this step is already changing, so the fix lands with it rather than being flagged and left.
   - **Each picker shows the union of all categories** regardless of the chip. With the delta write in place that is a display choice rather than a correctness requirement, and it is the right one here: a surface's selection spans both libraries, so hiding half of it per chip would misrepresent what is stored. The library keeps its per-chip list, which suits a surface whose chip scopes what you are looking at.
9. **The master toggle survives.** Its job is to park a selection without applying it, and the library carries the identical switch (`pref_filter_library_categories`). Retiring it here would make the same control behave differently on two surfaces and would force a no-master mode into the shared `CategoryFilterRow`, to buy alignment with an upstream preference this plan does not adopt anyway.
10. **The chip is per rendered surface, so there are three keys, not one** (revised 2026-08-08 after the first ruling proved wrong in the two-tab shape). With the preference on there is one Recents tab, one chip strip and one new key. With it off there are two tabs that look and behave independently, and linking their chips would move something for people who never opted in, against the whole point of defaulting the preference off. So `updates_content_type` and `history_content_type` stay, and the Recents tab gets its own third key. Flipping the preference back and forth then restores what each side had. `stats_content_type` is a different surface and is untouched.
11. **Tracker push stays on the details screens.** Marking read here will not push progress, matching both what these screens do today and what upstream's Updates tab does.

### Feed bounds

Each lane keeps its own bound and the combined modes are the deduplicated union of whatever the active lanes return. "Bounded" below means bounded in the query; the read lane is bounded by its own shape instead.

- **Updated lane**: three months and five hundred rows, as today.
- **Read lane**: no `LIMIT`, because the SQL already reduces it to one row per entry through the `max_last_read` subquery, so its size is bounded by how many entries have ever been read.
- **Added lane**: needs an explicit limit, since nothing bounds it naturally. Match the updated lane's five hundred unless measurement says otherwise.

### Deduplication, stated once

There is one rule: **one row per merge group, falling back to one row per entry for an entry that is in no group.** The survivor is the most recent, which the kernel gets by ordering before it collapses; the lanes each arrive newest-first but their union does not, so nothing falls out for free. The kernel implements the per-entry half; step 6 upgrades it to per-group by resolving series keys, changing the kernel's output rather than sitting beside it.

**The scope of the collapse belongs to the render policy, not the kernel** (ruled 2026-08-08 while building step 3). Updates does not collapse at all, since its grouping is a display toggle. The flat modes and the digest both collapse across every lane, so one title is one row.

**The digest collapsed within each lane until 2026-08-14, and the owner reversed it on device.** A series added, updated and read on one day filled a slot in all three sections to say one thing, against caps of four new-chapter rows, nine shared across the two chapter sections and four newly-added. An entry now claims the one section holding its newest activity, which is the same rule that orders the sections themselves. One of Yokai's two costs does not apply here, because the caps are taken after the collapse rather than before it, so a section that loses a row backfills instead of running short. The other stands and is accepted: which section wins is a timestamp comparison the user cannot see.

### The target chapter is decided per lane

The row a user sees and the chapter a tap opens are different things, and the rule is the lane's, not one global winner. This is why the item model separates them.

- **Read lane**: reopen the recorded chapter while it is unfinished, otherwise the oldest chapter still unread. This reverses the forward-only rule the surface shipped with, on 2026-08-13; the reasoning is in the second feedback round below.
- **Updated lane**: the first unread chapter of the update burst, Yokai's rule (the first unread chapter fetched within twelve hours of the row's own chapter), falling back to the row's own chapter.
- **Added lane**: the first unread chapter.

Per content type the mechanics stay behind the provider: manga resolves through `GetNextChapters` with the scanlator filter, novels through `GetNextNovelChapter` in source order. Both are merge-unaware today; step 6 closes that.

**Resolution is lazy, per rendered row.** Resolving eagerly at assembly means one chapter query per row per emission, which on a five-hundred-row feed is the cost Yokai paid for the same feature. Resolve when a row is rendered, cache by `EntryId` for the assembly's life, and invalidate with the assembly. If a row's subtitle needs the resolved chapter's name rather than the recorded one, that is the case to measure before committing to lazy.

### What can and cannot be deleted

Checked against [off-path-manifest.md](../off-path-manifest.md), whose `pre-commit` hook requires that no manifested path exists in the working tree **and that every `Replacement` exists**.

- **Two live rows name files this takeover deletes, and must be repointed in the same commit**: `UpdatesScreen.kt` currently points at `ReikaiUpdatesScreen.kt`, and `HistoryviewModelStateProvider.kt` points at `ReikaiHistoryScreen.kt`. Both replacements go away at the cutover, so both rows move to the new engine's file. Miss this and every subsequent commit fails the hook.
- **Two rows are unaffected**: `HistoryItem.kt` and `HistoryWithRelationsProvider.kt` point at `EntryHistoryRow`, which survives. (It stopped surviving later, when History adopted the combined row; both rows were repointed at `RecentsRows.kt`. See "History draws the combined row" below.)
- **Explicitly ruled live and unlisted, do not delete**: `eu/kanade/presentation/history/HistoryScreen.kt` still holds `HistoryUiModel`, which Mihon's own model emits and the shared screen consumes, and `eu/kanade/presentation/updates/UpdatesUiItem.kt` still holds the type-neutral last-updated line. The manifest records both as partially collapsed. If the takeover removes the last live symbol from either, that is when its row gets added.
- **Genuine candidates**: `UpdatesDeleteConfirmationDialog.kt` and `eu/kanade/presentation/history/components/HistoryDialogs.kt` at the cutover; `UpdatesFilterDialog.kt` only once the mode-scoped sheet actually replaces it, which is the affordance step, so it is deleted and manifested there rather than at the cutover.
- **Note that `EntryUpdatesRow` has no manifest row**, unlike its history twin. Nothing is wrong with that (its Mihon original is still partially live), but do not assume symmetry.

### What the requester already has

Two ingredients are shipped and must not be rebuilt: per-title collapsing (the Updates "Group by series" toggle, merge-aware) and manga-plus-novel interleaving on both screens. History additionally deduplicates to one row per entry in SQL already, through the `max_last_read` subquery in `historyView` and its novel twin. What is genuinely new is combining the three activity lanes, a newly-added query per content type, search on the Updates lane (which has none today), and the conditional tab set.

### Known defects in the surfaces being replaced

Recorded so the takeover fixes them deliberately rather than carrying them across. Verified in current code; each names its owning step, or says it is deferred or ruled.

- Clear-all under the All chip sets the delete-all dialog on both models, so the user confirms twice and gets two snackbars. **Step 7.**
- The novel feed's loading flag is unconditional in the shared loading expression, so the novel first emission gates the Manga chip's spinner. **Step 4**, by deriving loading over the active providers.
- Long-press range selection exists only for manga and fills its range using indices into the manga model's own list, which is not the rendered order once the All chip interleaves or grouping collapses. Novels have no range selection at all. **Step 7**, which is sequenced after the render policies precisely because "the rendered order" does not exist until then.
- The "started" filter's negated branch disagreed between SQL and the novel Kotlin path: SQL requires unstarted and unread, the Kotlin negation let read chapters through. **Closed in step 1**, by writing the novel SQL twin to the manga branch's shape (1b) and deleting the Kotlin branch (1c).
- The excluded-scanlators switch renders unconditionally and is a silent no-op on the Novels chip, which the capability-slot rule forbids. **Step 8**, as a typed slot hidden for novels.
- History resume is merge-unaware on both content types. **Step 6**, which changes each provider's resume alongside the collapse it enables.
- Tab-reselect resume on History compares manga-latest against novel-latest regardless of the active chip, so it can open a manga from the Novels chip. **Step 10**, with the rest of the reselect decision. Step 8 preserves today's reselect behaviour unchanged so the cutover moves one thing at a time.
- History search runs on the raw SQL title while the row displays a custom override, so a renamed entry cannot be found by its displayed name. **Closed in step 7**, by matching the row's own title in the engine instead of in SQL; the row already carries the override, so no overlay holder is needed here. This was History only: the Updates surface had no search at all.
- Novel update rows carry no live download progress; the provider hardcodes zero where manga rows merge the download manager's status and progress flows. **Deferred to Road B**, which owns the download-state reconciliation. The row keeps the honest zero rather than faking progress.
- Marking a chapter read from either feed pushes nothing to trackers, and **that is kept** (ruled above). Listed so a future session does not "fix" it into a divergence from upstream.
- The two engines' group chapter lists are ordered opposite ways and the shared target rules assumed one of them, so a merged manga resumed the chapter before the one just finished. Found by the 2026-08-09 write-once audit, **closed in step 8a**; it never reached a user, since nothing renders the engine yet.
- Dead code to remove with the surfaces: `NovelHistoryViewModel` injects a merge manager it never references, `UpdatesViewModel.showConfirmDeleteChapters` has no caller (making its dialog branch unreachable), and its `InternalError` event is never sent. Both history models' add members joined the list once the seam owned the add flow. **Closed in 8g**, which took the four models down to being feeds.
- The manga history add re-favorites an entry that is already in the library, resetting its `dateAdded`, where the novel path refuses that write for exactly this reason. Unreachable from a row, since the add button renders only for non-favorites, and reachable as soon as the engine dispatches over a selection. **Closed in 8b**, by the remove branch the neutral add decision forces the caller to answer.
- Tab-reselect resume on History reads no chip at all, so it can open a manga from the Novels chip. Live, and listed here rather than under the replaced surfaces because the takeover carried it across rather than inheriting it. **Closed in step 10**, by moving resume onto the seam so the chip selects which providers are asked.
- The engine computes `filterActive` over the union of its surface's lanes rather than the mode on screen. Invisible while each surface renders one mode, wrong the moment one renders four. **Closed first in step 10**, before the combined tab exists.
- `renderRows` handed every collected lane to every render policy, so on a surface rendering several modes the Updates mode drew read rows and History drew updated ones. Same shape as the one above and found the same way, by looking at the combined tab on device. **Closed in step 10**, by filtering the assembly to the mode's own lanes in one place; `renderRows` moved into the policies file so the rule is pure and pinned.
- A newly added row offered a delete that could not work: the per-row history dialog reaches `removeHistoryRecord`, which casts the payload to a history row and returns silently, and a newly added row carries no such record. Reported from the device before it was found in code. **Closed in step 10**, by choosing each row's trailing control from its lane.
- Both adapters' `ownItems` resolved a `ChapterRef` only against the updates model's row list, so a chapter verb dispatched from a read-lane row found nothing and silently did nothing. **This went live when the combined tab shipped and is the fifth of the one-lane defects**, missed at the time: it was parked as unreachable because a History surface has no `chapterActions`, but the combined surface is built by `forRecents` with an updates model, so it has them, and `FEED` and `DIGEST` both declare `SELECTION`. **Closed by the read-lane state commit** (see Status), which took the verbs off the rendered updates feed and gave the read lane its own chapter state.
- A sixth one-lane defect came with it, in the bottom bar's own predicates. Five read `any {}`, which is false over nothing and hides its button; the unbookmark one reads `all {}`, which is vacuously true. A selection that could answer for no chapter therefore offered exactly one action, the wrong one, wired to the no-op above. **Closed in the same commit**, by a non-empty guard.
- Deliberate and kept: the manga feed bounds on chapter upload date while the novel feed bounds on fetch date, because many light-novel sources leave upload date at zero. The reason is recorded in the novel view's own header comment and stays a typed divergence.

## Sequenced steps

Step 1 lands in the live screens and its fixes are visible immediately: filtering moves into SQL, the started negation closes, and the stale membership cache goes. Steps 2 through 7 are then compile-gated and render nothing, because the existing shells stay on screen until step 8 swaps them. That is deliberate (it is how the details and migrate surfaces were cut over), and it means every defect owned by a step after 1 stays live for the user until the cutover.

1. **The data floor**, in three ordered parts.
   - **1a, the preferences.** Introduce a master toggle plus an include and an exclude set per rendered surface, the sets typed `Preference<Set<String>>` and all six registered in the `sharedSets` bucket of `CategoryIdPreferences` (see the decision below on why not upstream's `List<Long>` shape), and the Recents tab's own chip key. Retire five keys with dead-key constants and a `PreferenceRestorer` skip, and no value carry-over: the four per-type category sets and `updates_filter_categories`. The two existing chip keys stay live, per ruling 10. Update `CategoryIdPreferences`' KDoc, which enumerates the buckets. **No cleanup migration and no `versionCode` bump**: a retired key's value lingers unread in the preferences XML, which is exactly how the library's sort, filter and group-by retirements left theirs. **Also in this part, convert `CategoryFilterRow`'s confirm from a replace to a delta write** (see ruling 8), which is a prerequisite for a shared pair behind a subset picker and simultaneously fixes the library's silent drop. Pin it with a test where the stored set holds an id the section never displays, and verify by mutation: restore the replace, watch the test go red.
   - **1b, the queries.** Port `mihonapp/mihon#3589` into `updatesView.sq` (the include and exclude predicates with their two empty-set booleans, uncategorized as sentinel id zero via `NOT EXISTS`). The `IN :collection` idiom is already in the tree, so it needs no proving. Add a **new** filtered novel query beside the existing `getRecentNovelUpdates` rather than changing it, because the unfiltered one has two other callers in the home-screen widget; select the same columns from the same view so the existing mapper and `NovelUpdateWithRelations` are unchanged. The new query carries the category predicate against `novels_categories` plus unread, started and bookmarked, with started written to the manga branch's shape so the negation divergence closes. Downloaded stays in Kotlin on both, because download state is not in the database. Add the same category predicate to both history queries, since History has a selection of its own. That half is a Reikai divergence with no upstream twin, and it does not stop at the `.sq`: parameters added to `history:` change `HistoryRepository` and `GetHistory` too, so the amendment's "engine floor stays Mihon" means those files keep their behaviour and their sync relationship while gaining a Reikai-only parameter, exactly as `GetUpdates` gained upstream's. Affordable because `historyView.sq` has taken no upstream behaviour change since it moved modules in 2023. Leave `getLatestHistory` and `getLatestNovelHistory` unfiltered: they back tab-reselect, and a filtered resume would jump to a different entry than the one the feed shows. Add a recently-added query per content type against the base tables rather than the library views, whose per-entry chapter-count aggregation a newly-added row has no use for, coalescing `date_added` on the novel side and carrying the category predicate too. Six queries in total gain it: two updates, two history, two recently-added. Index `novels_categories` on both columns: the `CREATE INDEX` statements go in **both** the `.sq` file (for fresh installs, which never run migrations) and a new `.sqm` (for existing ones), per the rule that index changes are not bundled. A SQLDelight migration needs no `versionCode` bump.
   - **1c, the consumption.** Point **all four** live models at the new queries, the two updates ones and the two history ones, since 1b gives every feed the predicate. Delete `applyReikaiCategoryFilter`, both screen-lifetime membership caches, and the novel Kotlin chapter-state predicates whose SQL twins now exist (the started negation closes by deleting the Kotlin branch, not merely by writing the SQL). The two chip keys are not touched here at all, since ruling 10 keeps them for the two-tab shape; only the Recents tab's own key is new, and nothing reads it until step 10. **This wiring is not throwaway**: the manga models stay live as the providers and the novel models reshape into providers, so the call sites move at most one seam. It also fixes a real wart, since re-categorizing a series while the screen is open currently does not reflect until reopen, on both content types. Two things to expect: `UpdatesViewModel`'s combine **arity changes**, because its category filter shares the fifth slot with the custom-info overlay and only the filter half leaves, so that island is rebuilt around the overlay rather than deleted; and `matchesCategoryFilter` loses both its Updates callers, surviving on its library one.
2. **The provider seam and both adapters.** A `RecentsProvider` extending a `RecentsBehavior`, modelled on `LibraryProvider` over `LibraryBehavior`, plus the manga and novel adapters that implement it over the live models. This step ends with two working providers emitting lane flows, not just an interface. The neutral item carries entry identity as an `EntryId`, a timestamp, and a lane; per-type payloads ride as opaque values the shared layer never inspects, the discipline the migrate flow's DTOs already use (plain `Any`, unwrapped only inside the owning adapter). Four things this description originally got wrong or left out, all settled while building it:
   - **The lane is a typed slot, not an enum beside a nullable chapter id.** Only two of the three lanes have a chapter, so `RecentsLane.Read` and `Updated` carry a `ChapterRef` and `Added` carries none. A nullable `Long` would have been the banned shape, making the absent case representable everywhere instead of only where it happens.
   - **Target-chapter resolution lives on the provider, not on the item.** Resolution is lazy per rendered row, and an item is built at assembly time, so a resolver stored per item would resolve eagerly or hold a closure over the provider. `RecentsProvider.targetChapter` is a suspend function taking the item.
   - **`RecentsBehavior` carries no state at all**, deliberately unlike `LibraryBehavior`, which exposes a state flow the library's engine reads a search query back out of. That is the shape this surface is being rebuilt to remove, so the seam gives a second copy nowhere to live.
   - **The newly-added lane has to be built end to end here**, since 1b wrote only its SQL: two row models, one Reikai-owned repository serving both content types, and DI. It is Reikai-owned rather than a patch on Mihon's manga repository because the lane has no upstream twin on either side.
3. **The assembly kernel, pinned by tests before it is wired.** Pure functions the way `assembleLibrary` is, with unit tests standing in for the models (which resolve Injekt at init and cannot run in a unit test). The kernel owns two algorithms, ordering and collapsing; the scope of the collapse, sectioning and caps belong to the render policies below. Nothing tags a lane: an item has carried its own since the adapters were written. Ordering is total, breaking a timestamp tie on the lane, then the content type, then the entry id, because a stable sort over a concatenation only preserves whichever order the lanes happened to arrive in and would let rows swap places between emissions. No inputs object: nothing about assembly is preference-driven. Verify each test by mutation: delete the clause it names, watch it go red, restore it.
4. **The engine flow.** Combine the providers' lane flows, apply the chip by selecting active providers, run the kernel, tag the output with the chip. Loading, emptiness and the last-updated line are derived here, over the active providers, and nowhere else. Every keyed structure keys on `EntryId`. Four things settled while building it:
   - **A lane flow carries its own loaded flag.** A lane mapped off a model's state emits the moment it is collected, with an empty list, because that state is seeded rather than opened by its first emission. So "has it emitted yet" reports loaded one tick after subscribe with nothing on screen, and `RecentsLaneRows` pairs the rows with the flag instead; a query-backed lane starts from a loading value.
   - **Whether a filter is narrowing the feed is the surface's question, not a provider's.** The chapter-state filters reach only the updated lane, so a provider-level flag would have History call itself filtered because of a filter set on Updates. The engine ORs the category half with the chapter-state half only where the updated lane renders, through a pure function so the rule is testable without a preference store.
   - **The engine owns the chip**, reading its own surface's key. The novel updates model's copy stays live until the cutover deletes it; both read one preference, so they cannot disagree.
   - **The engine orders and never collapses.** The collapse scope is a render policy's call (see Deduplication), so step 5 collapses at whatever scope its mode wants.
5. **The render policies**, the four modes over that one stream: flat (deduplicated, bounded), digest (lane-bucketed, capped, headed, with footers that jump to the single-lane modes), Updates, History. Each policy is a pure function pinned in the same test file as the kernel, and each chooses its own collapse scope (see Deduplication). Four things settled while building it:
   - **Yokai's caps are adopted whole**: four new-chapter rows, continue-reading up to nine minus that count (one budget across the two chapter sections, so a big burst cannot push reading off the screen), four newly-added. Its section ordering comes too: the three sections sort by whichever holds the newest row, rather than sitting in a fixed order. Its global collapse before sectioning was declined here and later adopted after all, see the dedup section above.
   - **Its twelve-hour upload tiebreak does not.** Inside new chapters, Yokai compares upload date for chapters fetched within twelve hours of each other and fetch date otherwise. `novelUpdatesView` selects no upload column at all, by design, because many light-novel sources leave it at zero, so porting the rule would order one content type's rows by a clock the other does not have, invisibly. The section keeps the kernel's order.
   - **A footer goes on the two chapter sections only.** It jumps to that lane's single-lane mode, and newly-added has none. Yokai does the same, for what is presumably the same reason.
   - **Updates grouping is per series and per day**, collapsing at two or more chapters, which is what the live screen already does; the flat feed carries no date headers, since a title-centric feed with day headers is History again.
6. **Merge collapse.** Series keys resolve once for whichever lanes are showing, upgrading the kernel's per-entry deduplication to per-group and giving History collapsing it has never had. The same step makes each provider's resume merge-aware, since a collapsed row must open the group's next chapter rather than one member's. Assembly never passes the chip down to the merge repository, which rejects the mixed content type on every method that takes one. It lands in two commits, collapse then resume, because the two fail differently: a wrong collapse shows the wrong number of rows, a wrong resume opens the wrong chapter. What the collapse half settled:
   - **Membership is a map from `EntryId` to group id**, carried on the assembly rather than read beside it, so a policy can never pair one emission's rows with another's groups. It is empty while series merging is off, gated at the provider because `membershipChanges()` is the raw repository flow and does not gate itself.
   - **Group ids are unique across both content types** (one `merge_group` table with a `content_type` column), so the two providers' maps merge without collision and a group id needs no type prefix. Raw entry ids still do, which is what the standalone half of a grouping key carries.
   - **The dedupe is `dedupeByMergeGroup`**, the project's one definition of counting a merged series once, generalised in its key type so a mixed feed can key by `EntryId` where the per-type surfaces keep keying by raw id.
   - **Three modes collapse, Updates does not.** Flat and History collapse across everything, the digest collapses inside each section, and Updates leaves it alone because its grouping is a display toggle with its own control.

   What the resume half settled:
   - **Merge-awareness means something different in each lane.** Newly-added is the group's first unread, which the library already resolves for both types. Read keeps resume-where-you-were, over the group's unified list rather than one source's, because the plan rules that intent distinct from next-unread and switching it only for merged rows would make one row type behave differently for an invisible reason. Updated keeps its burst inside the row's own source, since fetch times do not line up across sources; only the read-elsewhere carry-over crosses the group there.
   - **One resume rule, not two.** `resumeInGroup` is neutral and both adapters call it over their own unified list, with a chapter another source already read passed in as read. Each type keeps only its list-building, which is genuinely per engine.
   - **The novel cross-source read carry-over was built rather than recorded as a gap** (owner, 2026-08-09). Manga had `MergedChapterProvider.readInOtherSources` and novels had nothing, so a novel chapter read on one source could be reopened from another. `NovelChapterAggregation.readInOtherSources` is its twin, keyed the same way the merged unread count is, and pinned by the same four cases as the manga side.
   - **The novel next-unread moved out of the library ViewModel** into `GetNextNovelChapter`, so recents and the library resolve one definition. The library's continue button gains the carry-over with it, which is a live behaviour change on the novel side and the parity fix that motivated it: manga's continue button has skipped cross-source-read chapters since the merged badge landed.
7. **One value each.** The engine owns the search query and fans it to the providers, matched through a neutral overlay holder so a custom title is searchable; dialogs and selection move onto the engine, selection computed over the rendered order that steps 5 and 6 now define. **Selection is a `Set<ChapterRef>`, not a `Set<EntryId>`** (corrected 2026-08-08 while building step 2): this surface selects chapters, and two chapters of one series are independently selectable, which an entry-keyed set cannot express. `ChapterRef` pairs the chapter's raw id with its `EntryId` because `chapters._id` and `novel_chapters._id` overlap exactly the way entry ids do, so a bare `Long` set over a mixed feed marks the wrong row instead of crashing. Step 2 built it; the entry-keyed verbs that genuinely act on an entry rather than a chapter (removing an entry's history) keep taking `Set<EntryId>`. Three things settled while building it:
   - **The search moves out of SQL rather than gaining an overlay.** This plan said to reuse the library's `LibraryQueryOverlay` so a renamed entry is findable by its displayed name. That holds for the library, whose rows deliberately keep source values so filter, sort and grouping read them; here every model already writes the custom title into the row it emits, so matching the row's own title in the engine is the whole fix. It also gives the updated and added lanes a search neither has ever had, and costs one pass over a per-lane-bounded feed. The models keep their unused SQL query parameter until the cutover deletes the call sites.
   - **Plain title matching, not the library's query grammar.** `libraryQueryMatches` is generic in its row type but sweeps every `MangaField`, and a recents row cannot answer most of them, so the binding would be mostly nulls answering terms nobody typed.
   - **Two selections exist between here and the cutover.** The engine owns a `Set<ChapterRef>` while all four models keep their own id sets and keep baking `selected` into the rows they emit. That is the duplicate storage this surface exists to remove, tolerable only because nothing renders the engine yet, so the contract is explicit: the new renderer reads the engine and never the payload's flag, and the models' selection is deleted at step 8.
8. **The cutover**, restructured 2026-08-09 after a write-once audit of the whole surface (see Status). It lands in seven commits, because three behaviours reached from these rows are implemented once per content type with nothing binding them, and shipping the cutover without them would leave the surface's own rule broken:
   - **8a, the reading-order defect.** Manga's merged chapter list arrives newest-first and the novel one oldest-first, while the shared target rules walk forward from the recorded chapter, so a merged manga resumed the chapter *before* the one just finished. The contract is now stated once on `RecentsChapter` (ascending reading order, always) and each adapter sorts into it: a merged list by chapter number, the only key comparable across sources, an unmerged one by Mihon's own comparator. The added lane's "first unread" became one shared rule too, which drops the manga side's application of that entry's chapter filters; a filter about what to list should not decide where a newly added series starts.
   - **8b, three seam verbs**, in two commits. The first takes `refresh` and `openDetails` and shares the cross-source read carry-over; the second takes `addToLibrary`. Refresh has three copies, one per model plus an inline one for the All chip, and all three stay live until the cutover deletes their call sites. The All chip's copy reports "updating both libraries" whenever either job started, which the engine does not fix: `refresh()` answers one Boolean for the same reason. Both jobs do start, since the inline copy combines them with Kotlin's `or`, which does not short-circuit; the defect is the message, so it belongs to the shell at 8e. `openDetails` is a resolved `Screen` rather than a neutral payload the caller re-branches on, because a novel screen is keyed by source and url while a manga one is keyed by id, and only the provider can turn a row's id into either. **The `addToLibrary` half moved out of this plan** into [content-layer-add-flow.md](content-layer-add-flow.md), and blocks the rest of step 8. Building it here turned up a divergence deeper than the known one: the two types favorite at different points in the sequence, manga resolving the category decision first and novels favoriting first, so there was no single order for the engine to delegate to and delegating to either would have baked the fork into the engine. That plan rules the order and hands this seam one add sequence to call. It has since shipped that sequence, verified it on device, and added the seam's own verb: `RecentsBehavior.addToLibrary` is implemented in both adapters, each handing its history model the ids it owns.

     **The seam now owns the whole add flow** (owner, 2026-08-10), reversing the ruling that kept it on the two history models. That ruling rested on the seam having no dialog channel and on each type's duplicate and category prompts being its own; both premises are gone, so the decline expired with its evidence. Fanning `addToLibrary` out to the models as first planned would have left the cutover shell rendering three dialog channels, the engine's plus one per model, which is the shape this surface's own indictment names as a defect. So the two models leave the add path entirely rather than being routed through `decideAdd`, and their add members are deleted at 8g.

     What the grounding pass established, and what the build has to respect:

     - **Nothing outside History reaches those add members.** Both models are held only by the History tab, by the shell 8e deletes, and by the two adapters nothing constructs. The same method names elsewhere belong to other models.
     - **The neutral pieces already exist**, so this adds no new UI. `EntryDuplicateCardUi` carries the raw entry id the dialog's group map is already keyed by, `EntryDuplicateDialog` is generic with a `toUi` mapper so a pre-mapped card list renders through the identity case, migrate is already neutral at both call sites, both category prompts feed one `ChangeCategoryDialog`, and a neutral open resolves through the engine's existing `detailsScreen`.
     - **The adapters delegate to the two adders, not to the models**, and no adder signature changes: every one of them also backs browse, global search, both details screens, both bulk-favorite models and two conformance suites.
     - **`AddFavoriteResult` is already the neutral answer** (added, failed, or a category prompt carrying its initial selection). Manga answers it directly; the novel adapter wraps its adder, whose browse-facing add is built for a pending row rather than a stored one, which is the only case recents ever has.
     - **The favorited-entry guard is not optional.** `MangaLibraryAdder.resolveAddFavorite` routes through `changeFavorite`, which toggles, so calling it for an entry already in the library would remove it; browse never hits that because `decideAdd`'s remove branch catches it first. The same guard closes a live defect on the manga history add, which re-favorites an already-favorited row and resets its `dateAdded` where the novel path explicitly refuses to. Unreachable from a row today, since the button renders only for non-favorites, and reachable the moment the engine dispatches over a selection.

     So 8b's remainder is: a suspend add contract on the provider seam answering `AddFavoriteResult`, with the duplicate lookup, the category prompt, the picker confirm and add-time grouping beside it; both adapters implementing it over their adders; an engine `addToLibrary` that runs `decideAdd` once and raises neutral duplicate, category and migrate cases on the channel it already owns; and the shared decision case added to the add flow's conformance suite rather than pinned twice.
   - **8c, the shared row mapping**, whose scope this description had wrong in two ways, both corrected against the code before it was built. It is not a collapse of two existing mappers: a `RecentsRow` carries only an item, and an item carries only identity, a timestamp, a lane and an opaque payload, so nothing assembled could be drawn at all. The only display accessor the seam had was `title`. 8c is therefore the display projection the engine never got, and the two live shells are its specification rather than its subject. And the read-progress predicate's four copies do not split history against updates, since History computes no progress at all; they split the flat row against the grouped child row, once per content type.

     What it landed: a neutral `RecentsRowUi` (cover, title, favorite, chapter) answered per rendered row by `RecentsProvider.rowUi`, matching the other two accessors on the seam; a lane-shaped chapter half, because the read feed carries a number and no name while the updated feed carries the reverse, so one flat type would need a sentinel for whichever half a feed lacks; a typed per-type progress, pages against hundredths of a percent, since only the renderer knows how either is written out; and `namedChapter`, the one rule both feeds share, that progress shows only where reading stopped short. `title` became a default over `rowUi`, so the title exists once.

     **The eight dispatchers were not moved, they were made unnecessary**, and each was checked to have a replacement before this was called done: the two key builders are the policies' grouping and the item's own identity, selection is the engine's, the cover-tap is `detailsScreen`, and read, cover and title are fields on `rowUi(member)`. They are deleted with their shell at 8e rather than here, since they live in it.

     The pin: `RecentsMappingTest` was a hand-maintained twin pair, which the rule amended 2026-08-10 puts on the last rung, so it became a probe-based conformance suite over both content types. Ten cases run twice plus the two cross-type cases that compare both in one body, up from ten single-type tests.
   - **8d, the shell**, over the engine and the four policies. Scoped against the code 2026-08-10, which corrected the one-line description in three ways. **The engine could not be rendered as it stood** (all three closed by the seam commit recorded in Status): `providers` was private, so a screen could not reach `rowUi`; there was no mode concept anywhere; and `updatesRows` needed a `groupBySeries` and an `expandedKeys` that nobody owned, the latter a `remember` in the dying shell that died with the process. **Building the engine constructs four models**, and the two updates models query on construction, so a naive shell doubles the default two-tab shape's database work. The two history models no longer do: porting mihonapp/mihon#3728 write-once moved both onto a feed that runs only while its state is collected, which is what an uncollected lane now buys. The updates pair keeps its eager init, upstream included, because both sides patch a row's download state in place rather than deriving it. And **two of the six row cases had no reachable composable** (closed by the row commit in Status): the group and child rows were private in the file 8e deletes, and nothing anywhere drew a section footer.

     **What the 2026-08-11 sweep of both live screens found the shell owed**, and where each landed: the per-row download action (the download verb now carries a `ChapterDownloadAction`), the six bottom-bar predicates (read off the selected rows' own projections, filtered out of the rendered items by `chapterRef`), the filter-sheet opener, and the Upcoming and category routes. **Still owed by the cutover, because they belong to the tabs rather than the screen**: the badge reset on enter and dispose, the splash gate, hiding the bottom nav during a selection, and tab-reselect resume across both types, which has no engine path. Snackbars are the shell's: the verbs answer, they do not announce.

     Ruled while scoping (owner, 2026-08-10): the adapters get **per-surface factories** so each takes only the models its lanes need; **selection is declared per mode**, so History keeps having none at the cutover and gains it deliberately at step 9; the **mode type is created here** with the name step 10 will use, while persisting it stays with step 10, which is the step whose shortcuts need it; and the real refreshing signal was **dragged forward** rather than carrying the replaced screens' fixed one-second fake.

     The shell owes the union of what the two live screens draw, which is asymmetric: Updates has filter, calendar and refresh actions, an action mode with select-all and invert, a six-action bottom bar, pull-to-refresh, its own delete-confirm dialog and a filtered-empty state with a Filter button; History has one action, a search toolbar, no selection, no bottom bar and a filtered-empty state with no way in. Two live defects come with it, and the engine already fixes the second: the pull-refresh spinner was faked, and History's clear-all raises both models' dialogs under All.
   - **8e, the cutover commit**: both tabs render the new shell, the two shells are deleted and the two manifest rows whose replacements they were get repointed at it. No new rows: everything deleted here is Reikai-owned. Hook-bound, so it is self-contained.
   - **8f, History's filter entry point and the mode-scoped sheet**, which took the sheet over rather than parameterising Mihon's (owner, 2026-08-12): the takeover was a matter of time either way, and step 9 was going to add sections to it. **8g, the dead code.**

   The engine replaces both shells; the two Reikai novel models stay live behind their adapters, stripped of every list-describing member and, since 8b's reversal, of their add members too; the two Mihon models stay live behind adapters. What keeps all four live is that they are the feeds, not that they hold any state the engine now owns. The manga-only Upcoming calendar and the scanlator predicate become typed capability slots, hidden for novels rather than inert. **History gains a filter entry point**, which it has never had, and until this step its own selection is unreachable and therefore always empty: inside the Recents tab there is one sheet for that tab's selection, but with the preference off History is a separate tab with a filter of its own, and a filter with no way to see or change it is not a filter. The sheet's mode scoping lands here rather than at step 9 for the same reason: Mihon's sheet draws unread, downloaded, started, bookmarked and the scanlator switch unconditionally, and none of those apply to a history feed, so opening it unchanged from History would ship five no-op controls, the exact defect this plan indicts the scanlator switch for. Reselect behaviour is preserved exactly as it is today. The cutover deletes the two replaced shells and **repoints the two manifest rows whose replacements they were**, in one commit; the dead members the deletion strands are 8g's. Verify on a debug build and on a minified `preview` build before moving on, per the program's per-surface bar.
9. **The affordance harvest, which is swipe and nothing else.** Swiping a row runs the existing `ChapterSwipeAction` preferences, which already offer toggle-read, toggle-bookmark, download and disabled per direction; the delete-after-read side effect rides `SetReadStatus` and `SetNovelReadStatus`, and tracker push deliberately does not follow. The step's original description had one thing wrong, corrected against the code before it was built: those preferences do **not** already route through the shared details behaviour on this surface. `EntryDetailsBehavior.chapterSwipe` serves the details screens; the recents seam had no swipe verb at all, and the gesture existed in exactly one composable, Mihon's `MangaChapterListItem`, whose helpers were private.

   **Swipe is Updates-only, and that is a surface decision rather than a content-type one** (owner, 2026-08-12), so the write-once rule is satisfied: manga and novels share one row and one seam, and both got it in the same commit. History is where it stops, because three things are missing there rather than one. `chapterActions` is null by construction on a History surface (`forHistory` builds no updates model), each adapter's `ownItems` resolves a `ChapterRef` only against the updates model's row list, and a history row projects `RecentsChapterUi.Number`, which carries no read, bookmark or download state for a swipe icon to read. Giving History swipe means answering all three, and it is what would finally make the `ownItems` gap reachable.

   **The per-row download button and the read-progress display were struck from this step (owner, 2026-08-10)**, because both already ship: the button is Mihon's, whose item carries the two download providers and whose row draws the indicator with no gate on it, and Reikai inherited that verbatim. The "visibility preference" this step used to name was a Yokai affordance with no twin in the base and none in Reikai, so it was a backlog item for something nobody had lost. Read progress landed with the row projection.

   **History's time grouping was cut** (owner, 2026-08-12) and moved to `ROADMAP.md`. Checked against Yokai rather than left unchecked: `group_chapters_history_type` buckets by series within a week or a day (its key is the pair of manga id and formatted date, defaulting to `ByWeek`), and `collapse_group_history` is the default expansion state for those buckets, inert unless a time mode is active. Reikai's History already deduplicates to one row per entry in SQL through the `max_last_read` subquery and already draws day headers, so what Yokai adds here is week buckets and an expander over the other read records, and that expander needs the history query to stop collapsing. That is a feature with its own shape, not a harvest.

   Yokai's three-tab options sheet is likewise not owed: step 8f already took the sheet over and made it mode-scoped, and `UpdatesFilterDialog` is already deleted and manifested.
10. **The tab set.** A preference next to the tablet-UI mode in `UiPreferences`, rendered in the Appearance settings group, defaulting off so nothing moves for an existing user until they opt in. It lands in five commits, because two of them fix live defects that the combined tab would otherwise inherit and one builds a surface nothing renders yet.

    **The prerequisite is a defect the surface already carries and cannot see.** `recentsFilterActive` states the rule correctly, that the chapter-state filters count only where the updated lane renders, but the engine hands it the union of every lane its modes draw rather than the lanes of the mode on screen. With one mode per surface those are the same set, so the rule holds by luck; a four-mode tab makes a filter set on Updates report History as filtered, which is the exact complaint the function exists to answer. The screen is already mode-scoped and derives its own chrome from `mode.lanes`, so the engine is the half that lags. It is fixed first, alone, and pinned by a multi-mode engine case.

    **The chip-blind resume is fixed here, at the seam** (owner, 2026-08-12), rather than parked. Tab-reselect resumes the newest read by asking each history model for its latest row and comparing timestamps with no chip read at all, so from the Novels chip it can open a manga. The fix is a `latestRead` verb on the provider seam: each adapter answers from its own unfiltered query, the engine takes the newest across its **active** providers, and the result goes through the existing `open`, which already resolves the read lane's target and already answers the sealed open type that splits a manga Activity from a novel Screen. Chip-blindness then cannot come back, because the chip decides which providers are asked rather than being checked after the fact. The deliberate half survives: each query stays unfiltered, so search text and a category filter still cannot move what resume opens.

    **The mode becomes persisted** before the tab exists, since the shortcuts resolve by writing a mode and then switching tab. The engine seeds from the stored value filtered to the modes it renders, falling back to the first, and `setMode` writes it; `getEnum` stores the constant's name and falls back to the default on a name it does not know, which is why those names are frozen and also why a removed case degrades rather than crashes.

    **Then the surface, then the switch.** The combined tab and its engine builder land unreachable: all four models through the `forRecents` factories, `RecentsSurface.RECENTS`, and all four modes declared, which is not optional because the digest footer's `setMode` requires the mode it jumps to. The tab carries the union of what the two tabs still own, and two of those are rulings rather than transcriptions. **Reselect is mode-dependent**, which the build had to sharpen: the rule as written ("updated pushes the queue, read resumes") was silent about the two combined modes, which draw both lanes. It resolves to resume wherever there is reading to resume, leaving Updates, the one mode with no read lane, holding the download-queue shortcut it has always had. **The badge reset fires only where the visible mode draws the updated lane**, so opening the combined tab in History mode does not clear a count the user has not looked at. Bottom-nav hiding comes along for free and closes a gap, since History never hid it.

    The switch commit makes four fenced edits to Mihon files. The tab list moves out of its object property into `Content()` and is computed from the preference; on flip the navigator's current tab is forced back into the allowed set, because an orphaned tab renders unselected rather than crashing (selection is a per-item class comparison and content comes from the navigator, not from the list). The badge is re-keyed off a marker interface both tabs implement, since a type test cannot answer for a third tab and the count would otherwise vanish and never reset. The two shortcut intents stay exactly as they are and are resolved through the preference at the point they select a tab, which keeps `MainActivity` and `shortcuts.xml` untouched; that matters because the action strings are hardcoded in the XML rather than referencing the constants.

    **The mode switcher and the mixed-lane row came out of the first device pass** (owner, 2026-08-12), and both are part of this step rather than a follow-up. The tab shipped with four modes reachable only through two launcher shortcuts, so three of the four could not be selected at all; the switcher is a tab strip under the toolbar, tabs rather than a second chip row because it is navigation between views while the chips filter whichever view is showing. Its strip order is its own list, since the declaration order picks the fallback mode and is not free to rearrange. **The strip carries Grouped, Feed, History and Updates**, keeping the digest on trial: the owner's read is that Yokai's "Grouped" tab is really the group-by-series setting, which this surface already has under the Updates mode's `GROUPING` capability, so the digest is expected to be cut once it has been seen. Cutting it is deleting a render policy, which is what ruling 1 anticipated.

    The row shape was the other half. A combined feed drew each row in the shape of its own lane, so one list alternated between a 56dp update row with a download control and a 96dp history row with a delete, in two cover shapes. **The two combined modes now draw one row for every lane**, with the subtitle and the single trailing control chosen by the lane: an update names its chapter and downloads, a read row gives its chapter and read time and deletes its record, and a newly added row says when it was added and offers neither. That last one is a defect closed rather than a preference: `removeHistoryRecord` casts the payload to a history row and returns silently otherwise, so the delete a newly added row was offering could never do anything. Swipe follows the same rule, with the two lanes that carry no read, bookmark or download state passing `Disabled` so no gesture is drawn rather than a dead one.

    **The combined filter seeds from Updates, once** (owner, 2026-08-12). Flipping the preference on otherwise reads an empty `recents_*` triple and a default chip, silently dropping whatever the two tabs were filtering by. On the first flip only, gated on the recents chip key never having been written, the Updates chip and category triple are copied across; a user who has since set their own combined filter is never overwritten. Updates rather than History because it is the tab whose filters actually narrow a feed. This is a one-shot at flip time, not a version-gated data change, so it needs no migration and no `versionCode` bump.
11. **The behaviour inventory, last.** Walk both replaced surfaces end to end, marking each behaviour present, deliberately dropped with the reason, or missing. It runs after step 10 so the affordances and the tab shape are in place; run it earlier and it reports its own unfinished work as missing. This is the completion bar, not device verification; the migrate surface passed a device pass and still had two upstream behaviours silently dropped.

    **It also sweeps for one particular assumption**, because step 10 turned up four instances of it in a single sitting: code that is correct while a surface renders one lane and wrong once one renders three. The four were `filterActive` asking about the surface rather than the mode, `renderRows` handing every lane to every policy, the row shape being chosen per lane, and a newly added row offering a delete with no record behind it. All four were invisible for as long as every surface collected exactly one lane. The sweep asks of each piece of the surface: what does this assume about how many lanes are on screen, and what does it do when the answer is three.

**Every step is done**, step 11 included (see Status).

Dependencies: 1a before 1b before 1c. Step 2 needs step 1, because a provider emits an added lane that does not exist until 1b writes its query and sits over models 1c repoints. Step 3 needs 2; step 4 needs 3; step 5 needs 4. Step 6 needs 5 (it changes what the policies render) and step 7 needs 6 (selection ranges over the final rendered order). Step 8 needs 7. Steps 9 and 10 need 8 and are independent of each other. Step 11 is last.

## Tests

The pin-once ladder binds here, and the surface starts with almost nothing pinned: no test covers `applyReikaiCategoryFilter`, the novel started negation, the restore-skip block, or the shared-set restore path, so the exact defects step 1 fixes are untested on both sides of the change.

- **Step 1a** pins the picker's delta write with a stored id the section never displays.
- **Step 1c** pins all six predicates in `RecentsFilterQueriesTest`, through the repositories rather than the generated queries, because the repository is what turns an empty id list into the query's "no constraint" flag. Every case runs for both types: include, exclude, exclude-beats-include, the uncategorized sentinel, the empty selection, and the started negation. An earlier draft of this section claimed a `.sq` predicate could not be unit-tested here; it can, on the in-memory JDBC driver the merge tests already use.
- **Step 2** pins what the adapters can pin without a device: the updated lane's burst rule in `RecentsTargetTest`, and row mapping in `RecentsMappingTest`, twinned per type. The mapping tests exist for two things nothing else catches: the two engines hand over timestamps in different types (`java.util.Date` against `Long`) and must leave as one, and a manga and a novel sharing a raw row id must not collapse into one identity. The adapters themselves resolve Injekt at construction, so their verb forwarding is not unit-testable and is verified at the cutover.
- **Step 3** pins the kernel in `RecentsAssemblyTest`: ordering, the three tie-break levels, the collapse survivor in both directions, and that a shared raw id does not collapse a manga into a novel. One of its fixtures was vacuous on the first pass (the entry ids happened to encode the expected order, so removing the lane tie-break changed nothing) and only the mutation pass caught it, which is the case for running mutations rather than trusting a green suite. **Step 5** pins each render policy in the same file.
- **Step 4** pins the engine in `RecentsEngineTest`, over hand-built providers rather than the adapters: chip selection, the chip tag, ordering across both types, the lane set actually collected, loading and last-updated over the active providers only, and the filter-active rule as a pure function. What it cannot reach is each adapter's mapping of its model's loading signal, since an adapter resolves Injekt at construction; that is verified at the cutover.
- **Step 6** pins collapse on both content types, or states in one line why a type cannot do the thing.
- Every new test is verified by mutation before it counts.

## Key files

Paths are from the repo root, which **is** `.../yokai-y2k/app`, so app-module sources live under `app/src/main/java/`.

To be created, under `app/src/main/java/reikai/presentation/recents/`: the engine, the `RecentsProvider` and `RecentsBehavior` seam, the two adapters, the neutral item and screen state, the assembly kernel and its tests, and the render policies.

Surviving into the new engine:

- `reikai/presentation/updates/EntryUpdatesRow.kt` and `reikai/presentation/history/EntryHistoryRow.kt`: the neutral row leaves. (The history one was later replaced by the combined row and deleted; see "History draws the combined row" below.)
- `reikai/domain/entry/EntryId.kt`: the identity every keyed structure uses.
- `reikai/presentation/library/LibraryProvider.kt`, `LibraryBehavior.kt`, `LibraryEngine.kt` and `LibraryAssembly.kt`: not consumed, but the structural template, down to the chip-tagged assembly and the lazily-resolved preference flows.

Dissolving into providers: `reikai/presentation/updates/NovelUpdatesViewModel.kt`, `reikai/presentation/history/NovelHistoryViewModel.kt`.

Staying live and synced behind adapters: `eu/kanade/tachiyomi/ui/updates/UpdatesViewModel.kt`, `eu/kanade/tachiyomi/ui/history/HistoryViewModel.kt`, `eu/kanade/tachiyomi/ui/updates/UpdatesSettingsViewModel.kt`.

Replaced at 8e: `reikai/presentation/updates/ReikaiUpdatesScreen.kt` and `reikai/presentation/history/ReikaiHistoryScreen.kt`, both deleted, and the bodies of `eu/kanade/tachiyomi/ui/updates/UpdatesTab.kt` and `eu/kanade/tachiyomi/ui/history/HistoryTab.kt`. `reikai/presentation/updates/ReikaiUpdatesCategoryFilter.kt` is filter-sheet content rather than shell, so it stays live until 8f scopes the sheet.

Elsewhere:

- `data/src/main/sqldelight/tachiyomi/view/`: `updatesView.sq`, `novelUpdatesView.sq`, `historyView.sq`, `novelHistoryView.sq`, plus the new filtered novel query and the recently-added queries.
- `data/src/main/sqldelight/tachiyomi/data/novels_categories.sq` and a new `.sqm`: the two indexes its manga twin has.
- `app/src/main/java/reikai/domain/category/CategoryIdPreferences.kt` and `reikai/domain/source/ReikaiSourcePreferences.kt`: the retiring keys and the three new per-surface triples.
- `app/src/main/java/reikai/domain/category/RecentsCategoryFilter.kt`: `RecentsSurface` and the one flow every recents feed reads its selection through.
- `app/src/main/java/reikai/presentation/category/CategoryFilterRow.kt`: the shared picker whose confirm becomes a delta write. Its other caller is `reikai/presentation/library/LibrarySettingsSheet.kt`, which is where the library's silent drop lives.
- `app/src/main/java/eu/kanade/tachiyomi/data/backup/restore/restorers/PreferenceRestorer.kt`: the dead-key skips.
- `app/src/main/java/reikai/presentation/widget/UnifiedUpdatesWidgetManager.kt` and `UnifiedUpdatesGlanceWidget.kt`: the other two callers of the unfiltered novel updates query. They must keep it.
- `app/src/main/java/eu/kanade/tachiyomi/ui/home/HomeScreen.kt` and `ui/main/MainActivity.kt`: the tab list, the sealed tab intents, the badge, and the shortcut mapping.
- `app/src/main/baselineProfiles/baseline-prof.txt`: pins the current `getRecentUpdatesWithFilters` descriptor, which goes stale when the signature changes. Cosmetic, but do not be surprised by it.
- `refs/yokai` (a sibling of this repo, not inside it): the Recents feature reference. `RecentsPresenter`, `RecentsController`, `RecentsViewType` and `history.sq`'s union query carry the algorithms; none of the code ports, since Yokai is Conductor plus RecyclerView over mutable models.

## Status

**Every step is built and the closing inventory has run, so the takeover is complete.** The cutover has shipped, so the shared screen is what both tabs draw. Planned 2026-08-08, grounded by a research pass across both current surfaces, the Yokai reference, the tab-set consumers and the deferred upstream commit, then cold-read twice at three altitudes (sequencing, claim-versus-code, and a dry run that tried to execute step 1), with the corrections folded back in each round.

Step 1 landed as six commits. `07dc096da` made the shared picker's confirm a delta write and fixed the library's silent drop with it. `6c44a5041` collapsed the four per-type category keys and their master switch onto one selection over both content types, retired five keys with restore skips, and reduced the picker to one section over every category. 1c later split that selection per rendered surface, per the revision to ruling 8. `0e193a338` took the query half of `mihonapp/mihon#3589` unwired, with `798c04367` recording it in the sync ledger and rewriting the pending row to say which half was refused. `51dc74fad` added the filtered-empty state and the picker's per-category restriction labels. `85d38d2b0` finished 1b: the filtered novel updates query beside the unfiltered one the widget needs, the category predicate on both history feeds, a recently-added query per content type, and the `novels_categories` indexes with migration 34.

`b22e290b3` finished 1c: it wired all four models to those queries and deleted what they replaced: `applyReikaiCategoryFilter`, both screen-lifetime membership caches, and the novel Kotlin unread / started / bookmarked predicates. The four hand-rolled preference-to-filter derivations collapsed into `RecentsCategoryFilter`, which resolves one surface's selection and clears both id lists when that surface's master toggle is off, because emptiness is the only "no constraint" signal a query gets. `UpdatesViewModel`'s fifth combine slot now holds the custom-info overlay alone; the filter joined the preference flow the subscription re-runs on, which is what makes a re-categorization reflect without reopening the screen.

Five things step 1 shipped that this plan did not originally call for. Two came from owner rulings mid-build and are recorded in Decisions: the filtered-empty state, and the picker labels, both out of the same question, whether a single-type chip should ignore a filter it cannot satisfy, which was answered no. A third is the per-surface revision to ruling 8, ruled after the device pass showed one selection reaching straight into History. Two came from 1c's scouting: History reuses the filtered-empty string with no Filter button, since its entry point does not exist until step 8 and a screen that cannot explain an empty feed is worse than one that cannot change it; and both sides of History's tab-reselect now ask their own unfiltered latest-entry query, where the manga side used to read the rendered feed and so could be moved by a search or a filter.

Device pass on the Fold covered, through 1b: the picker as one list with second-line restriction labels and none on universal rows; a manga-only category included from All emptying the Novels chip while the Manga chip shows that category's entries; the reverse with a novels-only category; the filtered-empty message and its Filter button; migration 34 applying to a real database without incident; and both feeds still loading afterwards. Not exercised on device: the delta write, because with one section over every category this picker never hides an id, so only the library can reach that path. It is unit-pinned and mutation-verified instead.

1c's own device pass, on the build before the per-surface split, confirmed the machinery on Updates and on the then-shared History: the master toggle off left both feeds complete while a selection sat stored, turning it on collapsed Updates to the included category's entries live and tinted the filter icon, and both tabs showed the filtered-empty message on the chip with nothing left. It has to run again after the split, where the point is now the opposite: a selection made on Updates must leave History untouched, and History must stay unfiltered until step 8 gives it a control. Still to cover: re-categorizing a series while a screen is open, the novel "not started" filter no longer showing read chapters, and tab-reselect on History with a search box filled in.

`405489315` is step 2: the seam (`RecentsBehavior`, `RecentsProvider`), the neutral item with its typed lane and `ChapterRef`, the shared burst rule, both adapters over the four live models, and the newly-added lane's whole floor. It renders nothing: nothing constructs a provider until the cutover, so the only live change is the DI registration of a repository with no other caller. The step-2 plan text above records the four things its original description had wrong; the one that reached beyond this step is the selection type, which corrected step 7.

`5d0cf9951` is step 3: two pure functions and their tests, nothing wired. It corrected three things this plan said: there is no lane to tag, the survivor does not fall out of an already-ordered stream, and the collapse scope is a policy's decision rather than the kernel's.

`afb25d324` is step 4, the engine: one `RecentsEngine` per rendered surface, taking its providers, its surface and the lanes it renders, and exposing the chip, the ordered chip-tagged assembly, loading, emptiness, the filtered-or-not reason and the last-updated line. It renders nothing either; nothing constructs one until the cutover. It reshaped the seam twice, both recorded in the step text: lane flows now carry a loaded flag, and providers gained the per-type last-updated value the line is derived from. Twelve tests over hand-built providers, each verified by mutation, and they pin the two defects the surface is being rebuilt for: an unloaded lane on the hidden content type no longer holds the visible one's spinner, and the last-updated line ignores the type the chip hides.

`87de24bd4` is step 5, the four policies plus the neutral row they emit, all pure and none wired. Three of them are transcriptions of code the two live shells already run, so the work was mostly deciding what not to carry across; the digest is the only new one. Twenty tests, each verified by mutation across seven rounds. The Yokai claims this plan rested on were checked against the clone rather than trusted: the caps, the shared nine, the section ordering, the twelve-hour tiebreak and the footer placement are all where the plan said, and the two the plan had slightly wrong (its flat feed dedupes by entry id, not title, and has no headers at all) are recorded above.

Step 6 landed as two commits, collapse then resume, both recorded in the step text above. The collapse commit is `4f9354418`, the resume one `7a660bf13`. The resume commit carries the one live behaviour change the whole surface has made since step 1: the novel library's continue button now skips a chapter another grouped source has read, which its manga twin has always done. Everything else still renders nothing. Eighteen tests across the two, each verified by mutation; two of them were rewritten mid-pass after a mutation showed the fixture could not fail (an unmerged novel whose only read row was its own, and a resume whose expected answer was also the answer to searching from the start).

`be8901a6b` is step 7, which put the last list-describing values on the engine: one query, one `Set<ChapterRef>` selection with its range computed over an order the caller supplies, one dialog slot, and the bulk verbs dispatching over the active providers. The seam gained a `title` accessor and a `clearHistory` verb; a nullable set left over from step 6's manga resume was cleaned up with it, since a null standing for "no group loaded" is the shape this surface's rules forbid. Ten tests, each verified by mutation, including two that only fail under a mutation of the exact clause they name (the blank-query guard, and the range's missing-anchor fallback).

`8e6a4ad1d` finished 8b, after `c1699dc4f` amended the program rule the old ruling leaned on. The seam gained four suspend add verbs answering `AddFavoriteResult`, both adapters implement them over the two adders rather than over the history models, and the engine runs `decideAdd` once and raises neutral duplicate, category and migrate cases on the channel it already owned. Two things it fixed on the way: an entry already in the library is no longer re-added, which closed a live `dateAdded` reset on the manga side, and adding from a row now applies default chapter settings and binds an enhanced tracker the way every other manga add path does. The outcome-to-prompt mapping was collapsing into a twin as it was written, so it became one `addEntryOrPrompt` kernel both adders call. Ten tests, each verified by mutation across three rounds; nothing renders it yet, so the gates were compile, spotless, the suite and a minified preview build.

`497c0f2b3` is 8c, whose scope the step text above now records correctly: it built the display projection rather than collapsing two mappers, because nothing assembled could be drawn before it. The corrections it forced are in that text. Twenty-four conformance cases against the ten twinned tests they replaced, each new assertion verified by mutation across four rounds; gates were compile, spotless, the suite and a minified preview build, since nothing renders it.

`80fb26485` gave the surface a real refreshing signal, the first piece of 8d and dragged forward from 8f (owner, 2026-08-10) rather than copying the replaced screens' fixed one-second fake. Each update job answers whether it is running as a flow, over one shared WorkManager helper since each supplies only its own tag; the seam carries it per content type beside `lastUpdated`, and the engine derives one value over the active providers. A scheduled update counts as well as a pulled one, which is already what the jobs treat as "already running".

Two commits then made 8d's shell cheap to render before any of it was written. `ac0fe5d17` ported mihonapp/mihon#3728 onto both history models, so a history feed runs only while something collects its state, seeded null rather than upstream's empty list because null is what this surface's screen and read lane read as "not loaded yet". `25a07a320` made the engine's own derived flows match: the assembly, the last-updated line and the refreshing signal are shared while subscribed, the chip stays eager because the verbs read it synchronously, and selection pruning moved out of the assembly transform. Without the second, the first buys nothing here: an eagerly shared assembly holds every provider's feed open for as long as its tab lives. The pin is that a derived flow does not open a provider's flow until something collects it. Note for anyone adding to that suite: a pin watching `assembled` is vacuous, because its upstream runs on `Dispatchers.Default` and escapes the test scheduler, so an eager-sharing mutation survives it.

`08d717d4d` is 8d's seam commit, which closes the three gaps the scoping note above named and one it did not. `RecentsOpen` is sealed over an `Intent` and a `Screen`, because manga reads in an Activity and novels in a Voyager screen, and the scope a launch asks for is the lane's. **That pairing is a correctness constraint, not a preference**: the read and added lanes resolve their target over the merge group, so it can belong to a sibling source, and a source-scoped launch of one throws on manga and silently loses prev/next on novels. Only the updated lane resolves in-source, and only it asks for source scope. The four chapter verbs moved behind a `RecentsChapterActions` slot answered only where the updates model is, both models on an adapter became optional so a History surface can skip building them, and `refresh` went straight to each library's job, which takes it off the capability entirely. `RecentsMode` carries each mode's lanes and whether it supports selection; its constant names are the on-disk format for step 10, which persists through `getEnum`, so they are frozen. The engine now takes modes rather than lanes, owns the group-by-series toggle and the expanded groups, and forwards the row projection.

`908592896` is 8d's row commit: the group and child rows left the Updates screen for the shared layer, the digest's section header and footer were written for the first time, and the read-progress rule became one function. The live screen calls the shared rows rather than keeping a second copy until the cutover, which is what made this verifiable on device before the shell exists. `labelValue` is split out of the composable label so the arithmetic can be pinned: a page count is stored zero-based and reads one-based, hundredths round down, and neither claims progress at zero. Nothing else about the screen changed, which the device pass confirms.

`f88f0c129` and `7d1ef2208` are the shell, in two halves: chrome and rows, then selection, the bulk actions and the dialogs. `d228ca78c` sits between them and closes what a pre-build sweep of the surrounding code turned up, including a chip flip that left the toolbar counting rows the chip had just hidden. Three shapes settled while writing it. A row's composable is chosen by what its chapter half turned out to be rather than by the mode on screen, which is what lets a combined feed draw a mixed list without asking any row its content type. A list key carries the lane, because the digest collapses within each section and one entry read and updated today is two rows, so an identity-only key repeats and throws. And a mode's affordances are one typed set rather than a flag each, after the second flag was about to be added.

Two things the shell cannot do and does not pretend to. History draws no filter action at all, because the sheet's model writes the Updates surface's preference keys whatever surface asked for it, which 8f fixes by parameterising it. And the tab-level behaviours stay on the tabs: the badge reset, the splash gate, hiding the bottom nav, and reselect resume.

One behaviour the takeover would have dropped silently was caught here rather than at the inventory: the per-row history dialog offers both "this record" and "every record for this entry", and the seam carried only the second. `removeHistoryRecord` is the first.

`33ae8317f` **is 8e, the cutover**: both tabs render the shared screen, the two shells are deleted, and the two manifest rows that named them are repointed at it. Three things it settled. The screen's snackbar host became a parameter, because History's tab-reselect resume answers "no next chapter" from outside the screen and the host the screen made for itself would have rendered that nowhere. The flat updates row now goes quiet during a sweep, matching upstream and the screen it replaces: the shell had left its cover tap and its download button live, a slip its own group-child row had already got right, and a cover tap mid-selection navigates away and takes the selection with it. And `ReikaiUpdatesCategoryFilter.kt` stayed live, against the plan text: it is not shell, it is the Updates filter sheet's only category row and the only switch for group-by-series, so deleting it here would have taken both controls off the sheet with nothing to replace them. It goes at 8f, with the sheet.

What stayed on the tabs is what needs the host rather than the feed: the badge reset, the splash gate (now reading the engine's assembly rather than a model's loading flag), hiding the bottom nav during a selection, and both reselect behaviours. History keeps both models resolved for its reselect resume, and keeps collecting their events for the reader-open cases and the feed's internal error; the cleared-history announcement is the screen's now, so the tab ignores that event. The Updates filter sheet moved onto a tab-local flag, since the engine owns dialogs and the model's dialog slot is 8g's to remove.

Before the cutover the surface was compared against all three references. Upstream Mihon turned up exactly one real regression, the selection-mode gating above; the other three candidates died on reading the code (upstream never sends the Updates internal-error event either, the History empty string had diverged deliberately long before, and select-all still expands a collapsed group's members). Tsundoku's history progress suffix and its paging, and Yokai's swipe actions, week bucketing and options sheet, are features neither Mihon nor Reikai ever had here, so they are backlog rather than losses. Five tests died with `buildHistoryRows`; the two that pin the shared row mapping moved to `EntryHistoryRowUiTest`.

`17bbf4ea6` is 8f. History gains the filter it has always been filtered by: both history models already read `recentsCategoryFilterFlow(HISTORY)`, so the surface had a selection nothing could show or change, and the sheet that could have changed it wrote the Updates keys whatever opened it. The sheet moved into the shared layer and Mihon's `UpdatesFilterDialog` was deleted and manifested, taken over now rather than parameterised in place (owner) because step 9 adds sections to it anyway. What it draws is the mode's capability set: the four chapter-state filters and the scanlator switch under `CHAPTER_FILTER`, the grouping switch under `GROUPING`, and the category row always, since every mode has one. That gave `GROUPING` its first production reader; it had been declared, pinned by a test and read nowhere.

Two shapes settled with it. The filter action is no longer capability-gated, because a filter every surface has needs no permission to appear; only the sheet's contents vary. And the surface-to-keys mapping became one internal function serving reads and writes, so `UpdatesSettingsViewModel` takes a `RecentsSurface` through a factory and the picker can no longer reach another surface's keys. The pin is a parameterized test over all three surfaces plus an isolation case, both verified by mutation. The screen owns the sheet rather than each tab opening its own, which is what stops a second copy appearing when the combined tab lands.

**8g is the dead code**, in three commits. `ae9c459cf` took the four feed models down to being feeds: selection, the event channels, the library-update triggers, search, the dialog unions and the whole add-from-a-row flow the seam owns since 8b, plus every dependency those paths were the last user of, three of which the models were already injecting and never touching. The two history row mappers went with it, since the shell fills the neutral row from the engine's projection, which also collapsed the rule they pinned (both types formatting a read time alike) from a twinned pair into one expression.

`753336613` retired the last two half-collapsed Mihon files. Neither was waiting for anything: `HistoryUiModel` had only its `Item` case left in use, its date headers being built and then filtered straight back out by the adapter, so it was deleted outright and the manga history model now emits stored rows; the last-updated line moved into the shared row emitter. Both originals were deleted with their manifest rows, the way `MangaInfoHeader` reached the manifest.

`63280a87d` deleted what the retired marker rule had preserved on three other surfaces (owner, 2026-08-12): the library model's settings-sheet, change-category and delete openers with their dialog union and state slot, the details model's selection flag, the library settings model's three superseded upstream actions, and the never-called donation composable. **Marking dead code is the keep-inert rule in member's clothing**, and the notes were teaching that retired rule to whoever read them next. The one inert holdout left is the download queue model, which the rules name as sanctioned until Road B.

The device pass read the stored preferences rather than the rows: filtering History wrote `history_filter_categories_*` and left every `updates_*` key untouched, its sheet drew the category row alone, and the feed followed the selection. Two false alarms came from `uiautomator` seeing only rendered rows, so a scrolled list read as a filtered one twice; scroll to the top before believing a feed changed.

The emulator pass covered both tabs on all three chips: grouping and expansion, the grouping toggle, search on each surface, a selection started by long press and by a group row, select-all and invert, mark read and back to unread through the bottom bar, the cover and download button staying quiet during a selection, the bottom nav hiding and returning, back clearing the selection, the filter sheet with both Reikai rows still in it, the filtered-empty state and its Filter button, a refresh reporting through the screen's own snackbar, reselect pushing the download queue on one tab and resuming the globally latest read on the other, the per-row history dialog with both of its choices, one clear-all dialog rather than two, and an expanded group surviving a rotation. Not exercised: the badge reset, which needs a pending unread count, and the add-to-library prompts, which need a history row whose entry is not in the library.

`3d66cd5db` is step 9. Swipe reuses Mihon's gesture rather than copying it: `getSwipeAction` and `swipeActionThreshold` became `internal`, which is the whole of the mechanism and the same move Komikku makes for its own Updates swipe. Three things were established before it was built and are worth keeping. Upstream has no feed swipe at all, on Updates or History, and neither does tsundoku, so there was no upstream shape to match and the behaviour matches the details row instead: same component, same two preferences, same threshold and undo. Yokai's recents swipe is not the reference either, being hardcoded to mark-read in one direction with no preference behind it. And the preference names are crossed (`swipeToEndAction` holds the start-side action), so the engine resolves them once into a `RecentsSwipeActions` value and nothing downstream repeats the inversion.

Three shapes settled while building it. A swipe uses per-row engine verbs rather than the selection ones, because the selection verbs clear the selection and a swipe must leave a running sweep standing. No capability case was added: swipe attaches inside the `Named` branch of the row dispatch, which is the only branch carrying the state its icons read, so the modes that cannot swipe are excluded by the row shape rather than by a flag. And `Disabled` is a no-op instead of upstream's throw, since `getSwipeAction` draws no gesture for it and a crash behind an absent gesture buys nothing.

One display gap was found by driving it rather than by reading it, and closed in the same commit: a bookmark set on a group child row wrote correctly and showed nothing, because `RecentsGroupChildRow` never drew a bookmark icon. Pre-existing, since a bulk bookmark over a selected child was equally invisible, but a swipe makes it a one-gesture action, so the child row now draws the icon its flat twin always had. The write was proved rather than assumed: with the Bookmarked filter on, the feed collapsed to exactly that chapter.

Six tests, each verified by mutation: five on the download state-to-action rule, one on the crossed preference binding. Device pass on a minified preview build on both the emulator and the Fold, which is what a gesture needs: both directions on a novel row, mark-read on a manga row, a group child, and a swipe during an active selection leaving the selection at one.

**Step 10 landed as seven commits.** `4ad4fadc0` scoped `filterActive` to the mode on screen, and its pin needed a test-only preference store whose `changes()` emits: `InMemoryPreferenceStore`'s yields nothing at all, so anything combining two preference flows never produces a value and a test reads the seed instead of the derivation. That trap is why the gotcha below says to pin such rules as pure functions; the emitting store is the way out where the rule is a wiring question rather than a pure one.

`ed848cedc` moved resume onto the seam, closing the chip-blind resume. The device pass made the case concrete: the newest manga read was two minutes newer than the newest novel, so the old comparison opened the manga from every chip. Its one deliberate behaviour change is the tie-break, which now follows the feed's own total order and so goes to manga where the hand-written comparison gave it to novels. The change stranded a good deal of the two history models' event machinery, deleted with it: `getNextChapterForManga`, `sendNextChapterEvent`, both `OpenChapter` events, the novel `resume`, both tab collector branches, the tab's own `openChapter`, and the two interactors those were the last user of.

`13736fb27` persists the mode through one key, filtered to the modes a surface renders, which is what lets a single key serve all three surfaces and what absorbs a mode that stops existing. `d9ba79b5d` built the combined surface unreachable, and pulled the splash gate and bottom-nav hiding into one composable the three tabs share rather than writing a third copy. `7dbad74d6` is the switch: the preference, the derived tab list, the badge re-keyed off a marker interface, the shortcut resolution and the one-shot seed.

`26d65fc9a` and `75b842a73` are the mode switcher and the mixed-lane row, both recorded in the step text above. They came out of looking at the shipped tab rather than from the plan, which is the argument for the device pass happening before the record is written rather than after.

**The device pass ran on the emulator against the stored preferences.** It covered: the tab set unchanged with the setting off; the seed carrying an Updates chip of MANGA into `recents_content_type` on the first flip; the tab set becoming Library, Recents, Browse, More; the current tab moving to its replacement rather than being orphaned; both shortcuts resolving to the combined tab on the matching mode and persisting it; all four modes rendering distinctly; the strip absent on the two single-mode tabs; and the feed's rows carrying one control each. Not covered: the newly-added row, because nothing in the emulator's library was added inside the lane's three-month bound, so its section is correctly hidden and no such row exists to look at. That case is verified by reading only.

**The read lane became actionable (2026-08-12)**, closing the fifth and sixth one-lane defects above. The four chapter verbs were keyed to a rendered updates row purely by accident of history: every one of them re-read the chapter from that row's id, so they now take chapter ids and the models' four entry points were re-keyed rather than duplicated. The read lane then needed chapter state it had never carried, so both history views gained the chapter's own read, bookmark and progress plus the values its download state is looked up by, behind migration 35. The projection split that hid the gap was the real cause and is gone: `RecentsChapterUi` now says only how a row labels its chapter, and `RecentsChapterState` carries what its actions read, so a lane that labels by number can still answer for its chapter. Download state for a read row resolves on call, from the queue and the on-disk index, rather than riding the feed: combining the whole history feed with the download queue would re-map every row of it on each tick to feed a control that lane does not draw.

Two things the build turned up. Both history models overwrite `title` with the user's custom title while a download folder is named from the stored one, so the row carries `storedTitle` and the lookup uses that; a renamed entry would otherwise have reported every chapter as not downloaded. And the `NovelDownload.State` to `Download.State` mapping already existed as three identical private copies, so it moved next to the model rather than becoming a fourth. Swipe stays on the updated lane by ruling rather than by capability now, and the comment saying otherwise was corrected. Verified on device for both content types: a read-lane row's chapter takes a bookmark, on a manga and on a novel.

**A latent database defect surfaced with it, and is fixed here because it blocked the migration** (2026-08-12). The driver verifies foreign keys after applying migrations, and the emulator's database failed that check with seventeen `novel_chapter_match_key` rows whose chapter no longer existed. They predated this work and were not reachable through it: the last migration passed the same check on 2026-08-08, so all seventeen were produced after that by current code. Every one was the old row id of a chapter a sync had deleted and re-added under a new id, and keys exist only for merged entries, which is why the count was small and novel-only.

The producer was the merge-group reconciliation (`ReconcileChapterMatchKeys` then, `ReconcileMergedChapters` now), which reads a group's chapters and writes their rows in a later transaction with nothing re-checking existence in between. A chapter deleted in that window takes a key on a dead id. Both upserts now carry an `EXISTS` guard inside the statement, which closes the window without holding a transaction across the read and keeps the rest of the batch. Migration 36 clears what the unguarded version left behind, because an orphan is not cosmetic: it makes the next migration to ship crash on startup, whatever that migration is. What stays unexplained is why the foreign key did not reject those inserts itself; on an enforcing connection it does, which is what the new test demonstrates. The guard is correct either way, since a chapter vanishing mid-reconciliation should be skipped rather than raise.

**The rule audit (2026-08-10)** re-swept this surface and the add flow against the program rules, after the constant-zero download progress was caught by eye. It found four places where the two types answered the same question differently, closed in `4fc789de5`. One was live: the category picker's confirm on a novel filed categories without ever favoriting, because the recents adapter reused the add-time-grouping confirm, whose `Stored` case deliberately skips the favorite. Both types now have a stored-row confirm that favorites first and re-reads the row rather than trusting a snapshot. The others: a newly added novel whose group dropped its own chapters resolved no chapter where manga fell back; a failed group add reported success on manga; and download progress became a typed slot rather than a zero indistinguishable from a stalled download. The system-category predicate was unified on the shape upstream and both reference forks use, checked against all three first. Every `twin of` marker now names the rung that pins it, and the two that could name none turned out to be the pair that had drifted.

**The write-once audit (2026-08-09)** swept the whole surface for behaviours implemented once per content type with nothing binding the two, since a takeover that leaves those in place ships the very rule it is built to enforce. It found one live defect in step 6's resume (the reading-order disagreement above, closed by 8a), three flows duplicated in full (add-to-library from a row, refresh, open-details), the read-progress predicate written four times, and a long tail of same-body pairs. What it ruled NOT worth sharing now, with reasons: the four identical SQL category blocks (pinned by twin tests, and sharing them needs a view or codegen), the custom-info overlay and `TriState` conversion (both would mean patching Mihon model bodies), the download-state merge (Road B owns it), and the three-line adapter twins, where a shared helper costs more than it saves. It also killed one claim: novel chapter deletion is confirmed, by the shell's own dialog, before either model deletes.

**Step 11, the behaviour inventory, ran on 2026-08-12 and closed the surface.** Both replaced generations were walked end to end: Mihon's updates and history screens, their two row composables, the updates filter dialog and both host tabs at the synced base, and Reikai's own two screens at the commit that deleted them. Almost everything is present, much of it improved: the toolbar actions and their gating, the six bulk verbs with their predicates, every dialog, date headers, row anatomy down to the unread dot and the progress label, grouping with its expand state, the deep links, the badge, the first-feed-ready signal, pull-to-refresh, and the whole add flow. What went missing clustered in one place worth naming, because it is where the next takeover of this shape will lose things too: the mixed-lane row has a single trailing control slot and one subtitle line, so the losses were affordances that shape had no room for, plus per-lane wording flattened onto one string. None of them were logic.

Six were fixed in the same pass. An empty feed said "no recent updates" whatever the view was, so History reported itself as having no updates and the string that says otherwise was referenced by nothing; the wording is now the lanes' answer. A read row in the combined feed could not be added to the library, though the read lane is not favorite-gated and the engine's whole add flow sits behind it. A collapsed group's cover still navigated away mid-selection, where the screen it replaced went quiet. The read row's own controls stayed live mid-selection, where the download control beside them did not. Clearing history announced success whether or not the wipe happened, because the shell fired its message off the dialog's confirm while both models' cleared-event went to nobody; the verb now suspends and answers, and that dead event is gone. And the chapter-state filters narrowed one lane of three: the combined views draw all four controls, but only the updated lane's query reads them.

Deliberately dropped, each with its reason: upstream's `InternalError` event on the updates model, which upstream declares and never sends; upstream's long-press-to-deselect, since a tap does it and a long press here extends a range; the fake one-second refresh spinner, replaced by the real job state; and upstream's explanatory line above its category tab, which belonged to a tab this sheet does not have. One divergence is kept by ruling (owner, 2026-08-12): resuming a finished chapter from a read row opens the next unread, where upstream reopens the chapter you finished.

**The inventory's own leftover closed on 2026-08-12.** The selection was pruned against the assembled stream while the screen drew something narrower, so a row the digest's section cap or a chapter-state filter had taken off the screen kept counting in the toolbar and kept being acted on. The cap case needed no filter at all: an update burst arriving takes the read section's budget while you are selecting. The engine now owns what is drawn (`rendered`, which is also where the chip-lag guard and the loading flag moved) and prunes to it, and the bulk bar answers for the same rows. The rule behind it is cross-surface and is recorded in [content-layer-architecture.md](content-layer-architecture.md): a surface's exclusions prune, what navigation hides does not.

Two costs are recorded rather than fixed. A read row's download state is resolved on demand, so the bottom bar's two download predicates and the downloaded filter each pay a queue and disk lookup per selected or judged row; the filter pays it only while it is on, and the row itself deliberately never pays it. And that filter does not re-evaluate when the download index changes, only when the feed does, so a chapter downloaded while a filtered history feed is open holds its place until the next emission.

Both gates the forward work waited on are cleared: the ViewModel migration shipped for every Mihon file on these surfaces, and the upstream sync base reached upstream head. `mihonapp/mihon#3589` (mihon `1d8a2b05d`) is the one upstream commit sitting below the synced base and this surface owns it; `GetUpdates.kt` was deliberately taken at mihon `6d69903a5` rather than head, and is byte-identical to that blob, so the four lines that commit adds are still there to port.

## Post-cutover feedback round (2026-08-13)

The requester tried the shipped combined tab from preview r1350 and raised four things in
`unseensnick/Reikai#57`. All four landed across `425c32ce0`, `e15a3a926`, `da3cc8796`, `fc0670848`
and `d3f49ff54`, and the reply went back on r1369.

**Rows in the combined modes now draw at History's height** with a poster cover, and the room that
buys carries the chapter, the time with its lane's verb (added, updated, read) and the reading
progress as separate lines. `RecentsGroupRow` moved with the flat row: Grouped draws both kinds in
one list, so leaving groups at the old height would have recreated the spliced-lists effect one row
shape exists to avoid.

**A caught-up series drops out of Grouped and Feed**, behind a `recentsShowRead` preference that
defaults to hiding, matching Yokai's `showReadInAllRecents`. Which entries still have an unread
chapter is one query per emission (`recentsUnread.sq`), read from the chapter side so the partial
indexes both chapter tables already carry answer it alone. Deliberately **not** resolved through
`RecentsProvider.targetChapter`, whose own KDoc rules that out: it is per rendered row by design, and
filtering the feed there would put a chapter query per row on every re-emission. The rule itself is
one pure function, `RecentsRowGate.keeps`, pinned by a test parameterized over both content types.

**The filter sheet is tabbed** (General, Chapters, Updates) and draws every control from every mode,
where it used to hide whatever the current mode could not answer. The scope that hiding used to state
is now said out loud: the Chapters and Updates pages each carry a line naming the sections they
reach. Yokai's `TabbedRecentsOptionsSheet` is the same shape, three always-present tabs.

**The toolbar's secondary actions moved into the overflow at every width**, after a first pass that
kept them inline on a tablet. It also stopped taking the Scaffold's pinned scroll behaviour, whose
only effect here was tinting the bar once content slid under it; with the mode strip between the bar
and the list, the outer nested scroll never sees the list return to the top, so the tint stuck.

**History gained `SELECTION`**, reversing the `emptySet()` ruling recorded in `RecentsMode`. The old
reason was that a History selection "would arrive with the affordances that act on [a burst]", but
the combined modes already give a read row that selection and every verb behind it acts on the
chapter the row names. Withholding it made one row answer differently depending on the tab drawing
it. **Not gated on the combined-tab
preference**: what a row can do follows its chapter, never a display setting, and gating it would
have left the two configurations permanently apart given the tab is off by default. History still
takes no grouping (no burst) and no chapter-state filters (those preferences are the Updates view's).

The per-row download control that came with it was **taken back off History** (owner, 2026-08-14).
The rule it was added under still holds, and it is the rule that removes it: a History row names the
chapter you finished, so a button offering to fetch that chapter is busywork, where the same button
on a combined-mode row names the chapter you have not read yet. It also restores parity with
upstream, which draws no download control on a history row. The selection keeps its download verb,
gated as every other verb there is on what the selected chapters can answer for.

**Three Yokai settings were assessed and none were added**, all for the same reason: Reikai already
behaves the way the setting toggles *into*. `groupChaptersHistory` is moot because both history views
collapse per entry in SQL (`GROUP BY` on the entry id, see also the ROADMAP's History-time-grouping
line); `sortFetchedTime` is moot because both adapters already stamp an update row with `dateFetch`,
and the inverse is blocked because novel updates carry no usable upload date; `collapseGroupedUpdates`
is moot because `expandedGroups` seeds empty, so groups already start folded. Worth knowing before
mining that sheet again.

## Second feedback round (2026-08-13)

**The read lane's target rule is reversed.** The requester's case: a hundred-chapter series read from
95 to 100 with 1 to 94 untouched. The caught-up filter keeps that row, correctly, because the entry
has unread chapters; the rule picking the chapter walked forward from the recorded one and so could
reach none of them, leaving a live Continue reading row whose tap answered "No next chapter". It now
reopens the recorded chapter while it is unfinished and otherwise takes the oldest chapter still
unread, which `resumeInGroup` composes out of `firstUnreadOf` beside it. Sequential readers see no
change at all: the two answers differ only where unread chapters sit behind the bookmark. It also puts
this surface back in step with the library's continue button and the details resume, which have always
opened the oldest unread for the same series.

`resumeTarget` now carries the fallback chain the two adapters used to spell out separately, and its
last clause is the added lane's rule. That clause is what makes the gate's promise true: the filter
keeps a row because the entry has something unread, so the rule has to be able to name it. The
own-source pass in the middle stays lazy, since it is only for a recorded chapter the cross-source
stitch dropped, and it costs a query.

**The row then moved onto its target, whole** (`8c982480c`). Display and target were already free to
disagree, and the reversal widened the gap: a row could say chapter 100 and open chapter 1. Renaming
the row alone was tried and rejected on device, because its dimming, unread dot, bookmark and download
control all still described the recorded chapter, so a renamed row drew itself as read while naming
something unread. What landed instead moves the lot: `RecentsProvider.targetRow` projects the resolved
chapter into a label, a state and a download control, the engine remembers one per record, and the four
bulk verbs are mapped onto it before they dispatch. The record is untouched, so History still names,
dims and acts on it: that tab is a log of what was read rather than a list of what to read next. Full
record: [recents-continue-reading-row.md](recents-continue-reading-row.md).

Two seam facts worth carrying forward. The memo hangs off the lane data rather than the assembly,
which is what lets a search keystroke leave resolutions alone while a chapter write clears them, and
it is emptied on a mode switch so a resolution made in Grouped cannot answer for a History row. And
the four selection verbs now take the chapters to act on rather than reading the selection back out of
the engine, because the mapping suspends and a verb resolving internally would have to clear the
selection out from under its own dispatch.

## History draws the combined row (2026-08-14, `48f3fe85a`)

A History row and a Grouped read row are about the same thing, and they used to look nothing alike.
History drew its own leaf, `EntryHistoryRow`, which carried only cover, title, chapter number and read
time, so it said "Ch. 31 - 6:33 AM" on one line and nothing about how far in you got. So History now
draws `RecentsCombinedRow` like every mode but Updates: title, then the chapter with its unread dot,
then when you read it, then the progress line. `EntryHistoryRow.kt` is deleted, and the two manifest
rows that named it are repointed at `RecentsRows.kt`.

It is the shape that moved, not the semantics. The row still names the record rather than resolving a
target, and it still takes no swipe. Both content types moved together, because the row is the shared
one.

The download control comes back with it, on every row, superseding `fde3a86fd` (owner, 2026-08-14).
That commit removed it because downloading a chapter you have finished is busywork, which holds for a
default action but not for an affordance: the selection menu has always offered download on a read
row, so withholding the row control was an asymmetry rather than a capability limit. It was first
tried gated on the record's read flag, and the gate was worse than either end of it: a control that
appears and disappears down the column encodes a state the unread dot and the dimming already say,
and it reintroduces exactly the raggedness the flat row shape exists to remove.

This also closes the "tsundoku history progress suffix" line above, which the pre-cutover comparison
recorded as backlog rather than a loss.

## Grouped and Feed date their older rows (2026-08-14, `719eb5b1b`)

The two combined modes draw no day header, by design: History and Updates group under one, and a day
header in Feed "would make it History again". That left their rows carrying a bare clock time with
nothing to anchor it, so a row read three weeks ago read exactly like one read this morning.

A row from today still shows the clock time. Older than that, the slot takes the date from
`relativeDateText`, the same function the day headers use, so it honours both Appearance settings:
Today / "3 days ago" within a week, the user's date format beyond it or whenever relative timestamps
are off. History and Updates rows are untouched, since their header already says the day and the clock
time is the part their row adds, which is upstream's shape.

Deliberately not `relativeTimeSpanString`, which is what the "Library last updated" banner uses: it
reads neither preference, so a user who set a date format would see it ignored on this one surface.
Upstream applies `relativeTime` to dates only, and this stays inside that convention.

## Decisions & tradeoffs

- **All-first rather than a mode-scoped chip.** Chosen by the owner after the library proved it. The alternative, letting the chip decide what gets collected, is what produces the class of defect this surface already has: state that describes one list stored once per content type, with the two copies free to disagree.
- **The Grouped view stays** (owner, 2026-08-12), settling the trial it shipped under. It was kept provisionally because its Yokai namesake turns out to be the group-by-series setting, which this surface already has under Updates, so the two could have been the same thing under one name. They are not: Grouped is the lane-sectioned digest, and it answers a question no other view does.
- **Four modes, cut down later rather than added later.** The digest and the flat feed answer different questions and neither substitutes for the other, and the flat one is what was actually requested. Building both as render policies over one stream makes the reduction cheap and the addition-later expensive, so the risk is taken in the direction that is reversible.
- **The combined feed is bounded, not paged.** Yokai's endless paging is the source of its cross-page deduplication bookkeeping. A bounded feed keeps the deduplication a single pass over a known set.
- **One shared category-filter set rather than two per-type sets.** Category ids are one space: the schema unification shifted every novel category id by a fixed offset precisely so it cannot collide with a manga id, so per-type sets encode a distinction the data does not have. This follows the library's identical ruling.
- **The shared pair keeps Reikai's `Set<String>` shape rather than adopting upstream's `List<Long>`.** Upstream's `mihonapp/mihon#3589` declares its two keys through `getLongArray`, which exists here too, so both compile. The deciding factor is `CategoryIdPreferences`: its whole purpose is that one declaration makes every cleanup path (category delete, the scrub migration, the restore remap) pick a key up, and those paths are typed for `Set<String>`. Adopting upstream's type would need a parallel bucket and hand-maintained coverage, which is the drift that class exists to prevent. The cost is that upstream's preference declaration and its filter-sheet UI are not taken verbatim; the SQL, repository and interactor halves of that commit still are.
- **The category filter reaches every lane of whichever surface is rendered, and each rendered surface owns its own selection** (ruled 2026-08-08, revised the same day). The first half settles what the old ruling was really about: leaving the filter on the updated lane would mean the combined modes filter one of three lanes by category, which is not a category filter. The second half is ruling 10's argument, which the first cut of this ruling had not absorbed: with the combined tab off, Updates and History are two independent-looking tabs, so one shared selection would have each silently move the other, and would leave History constrained by something set elsewhere with no control of its own until step 8. The price is worth stating whole: nine preference keys instead of three; `historyView.sq` gets a predicate upstream does not have, which propagates into `HistoryRepository` and `GetHistory`; History gets a sheet it has never had and that sheet must become mode-scoped a step earlier than planned; each picker collapses to one section over the union; and the consequence below has to be accepted.
- **A category include filter hides non-library entries from History, and that is accepted.** `historyView` has no `favorite = 1` gate, unlike `updatesView`, so History shows entries that were read but never added to the library. Those have no category rows, so they read as uncategorized and survive an include filter only when Default is among the included ids. This is what a category filter means once the feed is not library-gated, and special-casing non-library rows would be a rule nobody could predict from the UI. It is recorded because it is invisible until someone hits it.
- **The picker's clobber is fixed in the shared component, not worked around per surface** (ruled 2026-08-08). A replace-write behind a subset picker is a defect wherever it appears, and the component now backs three surfaces, so fixing the mechanism once is smaller than teaching each caller to be careful and it retires the class of bug rather than an instance. It also means each surface picks its display scope on the merits instead of being forced into the union by a correctness constraint. This touches the library, which is a different surface, and it is included on the "refactor alongside the change that motivates it" rule rather than as a cleanup pass.
- **The master toggle survives** (ruled 2026-08-08, reversing an earlier recommendation to retire it). Upstream derives "filtering is on" from set emptiness and has no toggle, which was the argument for dropping ours. Two things outweigh it: the library carries the identical master switch, so dropping this one makes the same control behave differently on two surfaces, and the checkbox is drawn by the shared `CategoryFilterRow`, which would have to grow a no-master mode to lose it. The convenience it buys, parking a selection without applying it, is real on its own terms.
- **A shared filter set inherits the library's lossy restore.** `PreferenceRestorer` remaps shared sets on the manga pass only, because novel categories do not exist yet at preference-restore time, so a backup's novel ids in the pair are dropped. The library accepted that on the grounds that a filter is cheaply re-picked; the same reasoning applies here, and it is a small regression against today's novel keys, which restore intact.
- **The chip is one value per rendered surface, not one per mode and not one for the app.** Inside the Recents tab there is one chip strip over one list, so one key; switching mode must not move it. With the preference off there are two independent-looking tabs, so their two existing keys stay. The first cut of this ruling collapsed all three into one key and was wrong for exactly the users the preference default is protecting.
- **Row identity and target chapter are separate fields, the target rule is the lane's, and resolution is lazy.** Conflating identity and target is what makes a title-centric feed impossible to add later without reworking both screens. Picking one global rule would silently change what tapping a history row does. Resolving eagerly would put a chapter query per row on every emission.
- **The digest drops Yokai's twelve-hour upload tiebreak** (ruled 2026-08-08). It orders new chapters by upload date when two were fetched within twelve hours of each other. Novels have no upload date to order by, and unifying the two feeds on one column was already rejected for hiding novel updates, so the rule would silently apply to half a mixed section. The cost is that a batch of chapters fetched together sits in fetch order rather than reading order, which the target-chapter rule already handles on tap.
- **The recents seam owns the whole add flow** (owner, 2026-08-10, reversing the earlier decline). The decline said the add's prompts stay on the model that owns the surface, because the seam carried no dialog channel and each content type's duplicate and category prompts were its own. Both premises expired: the duplicate dialog became neutral, the category picker collapsed onto one shared dialog, and the engine has carried a dialog channel since step 7. Honouring the decline past its evidence would have put three dialog channels on one shell, which is the defect this surface is being rebuilt to remove, and would have made the routing work thrown away at the cutover that deletes its call sites. The price is a wider seam, five verbs where there was one, and adapters that talk to the two adders rather than to the models.
- **The two Mihon models stay live.** The takeover stops at orchestration, and this surface's upstream churn is the lowest of any taken over so far, so there is no case for absorbing engine files.
- **The assembly kernel is pinned before it is wired.** The library did this and it is the only part of a takeover that can be tested without a device, since the models resolve Injekt at construction.
- **The kernel orders totally, and the collapse scope is not its decision** (ruled 2026-08-08). A partial order over a concatenation of lanes leaves equal-timestamp rows in whatever order the lanes emitted, which shuffles between emissions for no visible reason; the tie-break runs lane, then content type, then entry id, and a read leads at the same instant because it is the one thing the user did rather than something that happened to their library. Keeping the collapse scope out of the kernel is what stops the digest inheriting Yokai's dropped row, and it costs one extra call at each policy.
- **A single-type chip does not ignore a filter it cannot satisfy** (ruled 2026-08-08). Asked whether including a manga-only category should leave the Novels chip unfiltered rather than empty, the answer is no: that is a silent no-op of a filter for one content type, which is the thing the capability-slot rule forbids and which the scanlator switch is being fixed for. It also inverts the result instead of explaining it, and it has a cliff, since adding one universal category to the same selection would make the filter apply again with nothing on screen to say why. The real complaint, an unexplained empty screen, is answered by the two items below.
- **An empty feed says whether a filter caused it, and offers a way in** (built in step 1). The surface previously showed the same message whether nothing was new or a filter had hidden everything, which sends the user looking for chapters that are behind their own filter. This also covers the chapter-state filters, which the rejected proposal above would not have.
- **The picker marks a category restricted to one library**, on a second line in the secondary colour rather than in the label, since one selection now spans both libraries and picking a manga-only row while thinking about novels is otherwise invisible. Universal rows carry no line: a label on every row marks nothing. This needed an optional subtitle on Mihon's `TriStateItem`, declared before `onClick` so existing trailing-lambda call sites keep binding correctly.
- **The chapter-state filters reach every lane that has a chapter, judged in memory where no query can** (owner, 2026-08-12). A view drawing four filter controls that narrow one of its three lanes is the partial rule this program exists to remove, and the read lane can now answer all four: it carries the chapter's read, bookmark and progress since the read lane became actionable, and its download state is resolvable on demand. The updated lane is left to the query that already filtered it rather than judged twice against a rule written twice. The newly added lane is not judged at all, because it names no chapter, which is a capability it genuinely lacks rather than a filter quietly skipped. The filters apply only where the view offers the controls, since the four preferences are shared with the Updates mode and obeying them elsewhere would narrow a feed with nothing on screen saying so. Two costs, both recorded in Status: the downloaded filter pays a per-row lookup while it is on, and it re-evaluates with the feed rather than with the download index.
- **Resuming a finished chapter opens the oldest unread, diverging from upstream** (owner, 2026-08-12, widened 2026-08-13). Upstream's history resume hands back the recorded chapter itself whatever its state, so tapping a row you have finished reopens what you just read. Both lanes here resolve their target through the shared rule instead, which reopens the recorded chapter while it is unfinished and otherwise moves to the oldest chapter left. Kept because it is what the row is for, and because the two content types would otherwise need the upstream rule re-implemented on the novel side to match a behaviour neither wants.
- **The target rule reads no chapter filters, and that is a correctness requirement rather than a preference** (ruled 2026-08-13). The library's continue button narrows by the entry's own unread, bookmarked and downloaded filters before it picks. Doing the same here would answer a narrower question than the caught-up filter that admitted the row, so a bookmarked-only entry with no bookmarked chapter, or one set to show read chapters only, would be kept on screen and resolve nothing, which is the exact defect the rule was reversed to remove. Teaching the gate the same filters is not available either: it is one sweep over every entry, and download state is not in the database. The price is that an entry carrying its own chapter filters can resume differently here than from its details page.
- **Tracker push stays where it is, on the details screens** (ruled 2026-08-08). Marking read from this surface will not push progress, which matches both these screens today and upstream's Updates tab. The swipe affordance makes the surface a plausible place to finish a chapter, so the alternative was arguable, but it would put a remote write behind a gesture on a feed built for skimming, and diverge from Mihon for no parity gain.

## Gotchas worth knowing before starting

- **The home-screen widget reads the unfiltered novel updates query.** Two of its three callers are the widget, so change that query's signature and the widget silently starts filtering. Add a filtered query beside it instead. The manga widgets are safe: they call the unfiltered `subscribe` path, not `getRecentUpdatesWithFilters`.
- **All four models hardcode their `RecentsSurface`**; only the added lane takes it from the adapter it was constructed with. An engine built for the combined Recents tab therefore filters its read and updated lanes by the Updates and History selections until the cutover parameterizes or dissolves those models. Deliberate (owner, 2026-08-08): patching two Mihon files early buys nothing while the tab does not exist.
- **The in-memory preference store's `changes()` emits nothing at all**, where the Android one emits the current value on collect. So a preference-driven flow can be given a starting value in a unit test (`stateIn` seeds from `get()`), but never a second one, and a test that expects a preference write to propagate silently passes on the initial value instead. Pin such a rule as a pure function, not through the store.
- **Merging the two flows couples their emission rates.** Each content type has its own download-cache tick, and one completed download would re-run assembly over both types' rows. The library's guards are unequal (the manga combine has two `distinctUntilChanged`, the novel one has none, and each debounces search on its own timer), so hoist the query above both and do not let the weaker guard become the shared one.
- **The merge group repository rejects the mixed content type on every method that takes one**, sixteen of eighteen; the two that take no content type (`getGroup`, `dissolveGroup`) are keyed by group id instead. Merge groups are per type and only the providers reach the repository, each with its own hardcoded type; assembly must never pass the chip down.
- **Each tab gets its own `ViewModelStore`, and it survives tab switches.** `TabNavigator` builds a real `Navigator`, the per-screen `AndroidScreenLifecycleOwner` is keyed by `Screen.key` (the class name by default), and `TabNavigator` passes `disposeSteps = false` so switching tabs does not dispose the outgoing one. Two tabs resolving the same ViewModel type therefore get two separate instances, which is what makes a per-tab chip possible without any trick, and equally what makes an ungated engine run its queries twice. Verified against the voyager 2.2.21 sources; the rule file used to claim the opposite and has been corrected.
- **Removing the current tab from the tab list fails quietly, not loudly.** Tab selection compares classes, so the orphan tab keeps rendering with nothing highlighted. Whether the tab also survives process death is a separate question about how Voyager saves the navigator; verify that path with an actual process kill rather than a preference flip.
- **`TabOptions.index` is read nowhere in this repo**, so index collisions when the tab set changes shape are cosmetic.
- **The unread badge's counter is already shared**: the novel update job increments the same preference the manga job does, and the Updates screen resets it. Only the badge's tab-type key is manga-specific.
- **History is already one row per entry in SQL.** What it does not collapse is a series merged across sources, which are distinct rows. Do not "fix" the per-entry deduplication that is already there.
- **The two feed bounds use different columns on purpose.** Manga bounds on chapter upload date, novels on fetch date, because many light-novel sources leave upload date at zero. Unifying them would hide novel updates.
- **Moving a preference between `CategoryIdPreferences` buckets changes which delete paths scrub it.** The category-delete routing picks buckets by content type, so a key that moves from the per-type buckets into the shared one is scrubbed by every content type's delete instead of one. That is the intent here, but it is a behaviour change, not a reshuffle.
- **`UpdatesViewModel`'s `// RK` islands are not all category-filter islands.** Twelve markers and seven islands, of which three are delimited; the third opens with the category filter and closes after the custom-info overlay, so moving filtering into SQL rebuilds that island around the overlay rather than clearing it. Count them case-sensitively, or `work` and `Network` match too.
- **No Compose UI tests exist**, so a bulk deletion of composables cannot be caught by the suite. Verify a cutover by function inventory and by walking the consumers of every state field that moves.
- **Adding a parameter to a Compose component can rebind an existing trailing lambda.** Putting `subtitle` after `onClick` on `TriStateItem` silently pointed a caller's trailing lambda at the new parameter. The compiler caught it, but the same shape on a same-typed parameter would not fail. Add to the middle, not the end.
- **The `date_added` column is not null on manga and nullable on novels**, so the recently-added queries coalesce on the novel side, in the predicate and the ordering both. Without it a novel added before the column existed drops out of the lane rather than sorting last.
- **Names this plan deliberately does not fix, and the first step has to choose.** The four new preference key strings and their dead-key constants; where the master toggle and the chip key live, given that `CategoryIdPreferences` holds only the two id sets; the new engine's filename, which step 8 writes into two manifest `Replacement` cells; the `.sqm` number; and the persisted mode enum step 10's shortcuts switch on, which no earlier step creates. Pick them once, early, and use them consistently.
