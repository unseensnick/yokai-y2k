# Merge system rebuild

## Goal

One merge system serving both manga and light novels, built around a real, persisted group identity instead of a per-call derivation from preferences plus same-title favorites. Membership becomes a stored fact keyed by entity id, so the library, the details screen, and the reader all read the same group, grouping is created deliberately (never silently), and a whole class of bugs disappears at the root: silent membership corruption, lost unmerges on restore, disagreeing group definitions, browse pre-merging things you never added, and a full-library scan on every details and reader open.

## Why

The current design (`reikai/domain/MergeGroupAlgebra`, `reikai/domain/manga/MangaMergeManager`, `reikai/domain/novel/NovelMergeManager`) never persists a group. It recomputes membership every time from four SharedPreferences sets (`manga_manual_merges` / `manga_manual_unmerges` and the novel twins in `ReikaiLibraryPreferences`) plus a live same-title scan of the whole favorites list. It works, and it bought real things: no schema, instant reversibility, auto-merge-by-title with zero setup. A scout in this cycle verified the costs against current code, and they are no longer acceptable. Every problem below was confirmed by reading current code, not from memory.

- **A live id-reuse corruption path.** `mangas.sq` and `novels.sq` both declare `_id INTEGER NOT NULL PRIMARY KEY` with no `AUTOINCREMENT`, so SQLite reuses `max(rowid)+1` after the top row is physically deleted, and nothing prunes merge prefs on removal (`MangaMergeManager` states sibling removal is intentionally not there; the library `deleteManga` path only sets `favorite = 0`). The trigger is narrower than "any unfavorite": unfavorite keeps the row, so the id is freed only by a physical delete, which happens on the user-run Clear-database (`deleteNonLibraryManga`, deletes `favorite = 0` rows) or on migration. But the hazard is real: a stale pref pair can capture an unrelated new manga that inherits the freed id. `MangaMergeManager.applyMergePrefHealing` only mitigates when both entries are tracked on the same service with disagreeing remote ids; an untracked phantom survives.
- **Restore silently destroys deliberate unmerges.** Merge groups round-trip correctly (serialized as stable `{url, source}` refs and id-remapped, `BackupMangaMerge` / `BackupNovelMerge`), but unmerges are gated by `takeIf { size >= 2 }` and, on the manga side, resolve only against favorites. A dropped unmerge is invisible, and since `autoMergeSameTitle` defaults on, a pair the user deliberately split re-merges itself on restore. Restore also unions with local prefs and never prunes. (One asymmetry the earlier notes missed: `NovelBackupCreator` resolves non-favorite members via a `getById` fallback, so the novel side is not favorites-gated the way manga is.)
- **Two non-equivalent definitions of the group.** `MergeGroupAlgebra.computeGroupIds` unions manual merges with same-title siblings; `MangaMergeCollapse` buckets by merge key `?:` title, so an item that has a manual-merge key is bucketed by that key and its title is never consulted. Worked example: manual-merged A + B and same-title C are one group on the details screen, but a two-cover group plus a standalone C in the library. The library and the details screen disagree about who is in a group.
- **Browse pre-merges copies you never added.** Because `computeGroupIds` always includes the opened id and unions it with every same-title favorite, opening a not-yet-added copy from Browse renders it merged into your library group purely by title (reproduced on a MangaFire copy of a series whose other sources were in the library). The intended behavior is that Browse shows only the source's own details; grouping should be an explicit act, not a title coincidence.
- **A hot-path cost.** Resolution calls `getFavorites.await()` (the whole library) plus, on manga, N parallel tracker fetches, uncached, on every details open and every reader open. (The pref write is conditional on healing actually dropping something, not on every open, so that part of the earlier framing was overstated; the scan and tracker fetches are the real cost.)
- **No group identity.** No id, no row, no owner. Scope cannot be expressed (opening "the group" versus "this source" is indistinguishable to the reader, which forced an Intent-flag patch that was scouted and abandoned in favour of this rebuild). A group cannot own state: no per-group preferred source, title, cover, or cross-source resume. Preferences and the DB cannot commit atomically, so membership changes hand-roll optimistic rollback.
- **`source_order` is overloaded** as a global reading-order index, a per-source column repurposed. The reader sorts on `source_order` alone, so the merge stitcher overwrites it onto one 0..N scale; any list built outside the one blessed function collides the two scales and breaks prev/next (shipped bug, fixed in `3498c37eb`, closes unseensnick/Reikai#49). Manga restamps descending, novel details ascending, so the two sides even disagree on direction.
- **It is roughly 1.2 systems, not 1.** Only `MergeGroupAlgebra` (pure id math) is truly shared. Managers, aggregation, collapse, chips, manage-sources dialogs, backup models, and about eight preference keys are duplicated per content type, against the standing unified-content goal. Novels are the weaker half: the novel reader trusts a caller-passed `orderedChapterIds`, and History and Updates open it with the two-argument reader constructor (in `HistoryTab` / `UpdatesTab`), passing none, so a merged novel opened from either silently degrades to one source.

A real regression net already exists and must be preserved: `MangaMergeManagerTest`, `NovelMergeManagerTest`, `MergedChapterProviderTest`, `MangaMergeCollapseTest`, `NovelMergeCollapseTest`, plus `MangaMergeBackupRoundTripTest` and `NovelBackupRoundTripTest`. (`MergeGroupAlgebraTest` was retired in the cleanup pass with the pref-era algebra it covered; the surviving reconstruction logic is covered by `MergeGroupReconstructionTest`.)

## Approach

Give the group a persisted identity shared by both content types, and make every screen resolve membership by looking up "which group is this id in?" instead of re-deriving it. Once membership is a stored row, the library scan and the two-definitions split both disappear, FK cascade removes the stale-pref capture, browse stops pre-merging (a non-member resolves to just itself), and scope falls out of identity (opening the group and opening a member become different things, no flag). Auto-merge-by-title stops being a silent live force and becomes an explicit add-time prompt; grouping is always a real row you chose. The group owns a per-group source-ranking override layered over the retained global default, and its display overrides. (A dedicated group-owned ordering table was designed but parked before Phase 3, see Decisions; reading order stays computed in-memory per open, as both readers already do.)

In plain English first, then the mechanism, phase by phase. The spine is 0 -> 1 -> 2; the rest can flex. The phases are the build sequence, not separate releases: the whole rebuild ships together (big-bang), so no reduced version is ever released.

### Design decisions that shape the schema

- **Two per-type member tables, real cascade.** Manga and novels are separate tables with separate id spaces, and a SQLite column can foreign-key to only one parent, so a single member table cannot cascade to both. The group entity is one `merge_group` table with a content-type discriminator; membership lives in two tables (manga members, novel members), each with `ON DELETE CASCADE` to its own parent. A deleted entity's membership vanishes automatically, which is the structural fix for the id-reuse capture. The order table (below, added in Phase 3) cascades the same way, so a delete can never strand an orphan order row.
- **Group-owned ordering table (designed, parked before Phase 3, see Decisions).** Parked because a scout found its driver already fixed (the merge stitcher no longer overwrites `source_order`) and cross-source resume derivable from read-state, so Phase 3 keeps ordering in-memory. Retained here as the revive design. A per-group order table (`group_id`, `chapter_id`, `position`) holds the reading order; the upstream `chapters` / `novel_chapters` tables are left byte-for-byte Mihon, which keeps future reader syncs clean. `source_order` regains its single per-source meaning. Staleness is handled three ways, strongest first: (1) one transactional writer recomputes a group's order in the same DB transaction as any membership change, so membership and order can never disagree (only possible now that this lives in the DB, not prefs); (2) the order is treated as a rebuildable cache, never a source of truth, so a mismatch is a recompute, not corruption; (3) a cheap validity fingerprint (a hash of member ids, their chapter counts, and the effective source ranking) is checked on open and recomputed on mismatch, which is the path-agnostic catch for the one input that changes outside a membership transaction (new chapters arriving from a library update). A dedicated `reading_order` column on the chapter tables is kept as a fallback design if the extra join ever proves too costly; the same staleness recipe applies to it.
- **Full group ownership, delivered by the rebuild.** The `merge_group` row owns a per-group source-ranking override (the global `preferred_manga_sources` / `preferred_novel_sources` list stays as the default; a group uses its own member ordering only when it explicitly overrides, so the common case is unchanged, and this absorbs the standing "Per-group Preferred-sources override" backlog item), an optional title and cover override, and is the natural home for cross-source resume. Storage lands in the phase that first consumes it: identity, membership, display overrides, and the override flag in Phase 0. A persisted ordering table and any resume pointer are parked (see Decisions): the Phase 3 scout found ordering is fine computed in-memory per open and resume is derivable from read-state. This is build sequencing inside one big-bang release, not a scoped-down v1.
- **Explicit membership retires the unmerge concept.** Unmerge pairs only ever existed to override auto-merge-by-title. With auto-merge gone as a live force, "not merged" simply means "not a member." Unmerges are honored once, at migration, so a pair the user deliberately split is not re-materialized, and are vestigial afterward. This deletes the restore-side unmerge-drop bug rather than porting it forward.

### Phases

- **Phase 0: group persistence (foundation, no behavior change).** New migration (`30.sqm`, mirrored into the `.sq` files) for `merge_group` and the two per-type member tables, with the cascades above and the group-owned columns whose storage is known now (title / cover override, the per-group source-ranking override flag, and `source_priority` on each member). The order table and any resume pointer are deferred to Phase 3, where their shape is validated against real queries (migrations are additive, so this costs nothing). Repository plus model in `reikai/domain/merge` + `reikai/data/merge`, registered in DI; no consumers wired yet. Tests: a round-trip that proves membership stores and reads back ordered and that deleting an entity cascades its membership away (the id-reuse fix), plus establishing the missing SQLDelight migration-test pattern.
- **Phase 1: one-time migration (gated on the next `versionCode`, like `SetupCategorySortOverrideMigration`).** Read the four pref sets, reconstruct today's groups (manual entries plus the current same-title auto-groups), honor existing unmerges as exclusions, and materialize everything as rows, so nothing a user has today un-groups. This is the only place the old auto-merge derivation runs.
- **Phase 2: resolution cutover (kills several bugs at once).** Replace `computeRelatedMangaIds` / `computeRelatedNovelIds` derivation with a group-row lookup by id. One definition now serves library collapse, details, and reader, so the two-definitions split disappears; the whole-library favorites scan on every open is gone; FK cascade removes the stale-pref capture; and browse-details of a non-member resolves to just itself (the source's own details), fixing the pre-merge.
- **Phase 3: reader scope.** Give the reader an explicit **scope**, group (the unified cross-source list) or source (one source's own chapters), chosen by the entry point (Option 2, scope-by-context): the details All chip, Library resume, and History open group scope; Updates and a specific source chip open source scope. Both content types obey the one rule, replacing today's accidental split (manga is always group-scoped; novels are source-scoped from Updates / History because those tabs pass no chapter list). The scope signal travels from the entry point (a boolean Intent extra on the manga reader, an explicit parameter replacing the fragile `orderedChapterIds` on the novel reader); the group is still resolved from the entry id, and read-state propagation stays group-wide so marking a chapter read still de-dupes across sources in either scope. Reading order stays computed in-memory per open (both readers already restamp a throwaway copy, never the DB); the persisted order table plus its staleness machinery is **parked** (see Decisions), its original driver (the merge stitcher overwriting `source_order`) already fixed and cross-source resume derivable from read-state. The per-group source-ranking override is deferred to Phase 5 (wiring a consumer before its setter UI exists would ship dead code).
- **Phase 4: backup / restore on the new model (done).** Round-trips the group entity (members still as `{url, source}` refs) off the `merge_group` tables instead of the retired prefs: create reads `getAllMemberships`, restore resolves refs to fresh ids and calls `MergeGroupRepository.merge`. Restore is additive (Option A, see Decisions), not a blind pref-union, and the unmerge round-trip is gone (concept retired; the proto fields stay declared so old backups still restore). The existing round-trip tests were rewired to assert the repository path.
- **Phase 5: unify and make grouping visible (planned, sliced).** In order: (1) **toggle UIs**, the master switch (`series_merging_enabled`) in the library display menu and Settings, and the same-title suggestion toggle (`auto_merge_same_title`, repurposed from silent auto-merge to gating the add-time suggestion) in Settings (placement rationale in Decisions); (2) **consolidate** the two now-identical Settings "clear merges" actions (both call `repository.clearAll`) into one; (3) the **add-time prompt**, an "Add to existing group" action on the shared duplicate dialog (`DuplicateMangaDialog` / `DuplicateNovelDialog`, wired through the shared library adder and the details models) that favorites the new copy and merges it into the **selected** match's group, gated by the same-title suggestion pref; the duplicates are group-collapsed and picked through an explicit selection mode rather than merged wholesale (see Decisions); (4) the **per-group source-ranking override**, net-new `merge_group.sq` update queries + `MergeGroupRepository` setters (none exist today) wired into `ChapterAggregation.aggregate` / `NovelChapterAggregation.aggregate` at all four call sites (manga `MergedChapterProvider`; novel details, library `getResume`, and reader), plus the **manage-sources modal redesign** (design C1, see Decisions) and the **abandoned-add fix** (defer the add-time merge until the favorite actually lands, see Decisions); (5) **collapse the parallel manga/novel merge components onto the shared `Entry*` seam**, deferred to a fresh deep-researched session as the final step, its own commit (see Decisions). Retire the old merge / unmerge / auto-merge pref keys at the end (kept read-only so old backups still restore); the global preferred-source keys stay.

Rollout is big-bang: the cutover flips everything to rows, verified on-device, then shipped as a preview release to shake out real-world problems before a stable cut.

## Key files (the current surface a rebuild replaces or absorbs)

- `reikai/domain/MergeGroupAlgebra`: the pure set math, the one already-shared piece; the group-lookup layer superseded it. Removed in the cleanup pass, its one surviving helper (`parseUnmergedPairs`, used by the Phase 1 migration) inlined into `reikai/domain/merge/MergeGroupReconstruction`.
- `reikai/domain/manga/MangaMergeManager`, `reikai/domain/novel/NovelMergeManager`: the parallel managers, including the healing pass; replaced by the shared group repository plus interactors.
- `reikai/domain/manga/MergedChapterProvider`: reader group resolve, the shared aggregate plus reading-order policy; keeps the aggregate, changes the resolve and the ordering transport.
- `reikai/presentation/library/MangaMergeCollapse` (plus `NovelMergeCollapse`): the library's separate, non-equivalent bucketing; replaced by a single group-row read.
- `reikai/presentation/manga/MergeSourceChips`, `ManageSourcesDialog` (plus novel twins), `reikai/presentation/browse/components/EntryDuplicateDialog` (shared by both content types): the chip row, manage dialog, and the add-time prompt host.
- `reikai/domain/library/ReikaiLibraryPreferences`: the merge / unmerge / auto-merge keys (per type) are retired (kept read-only for old-backup restore); the global `preferred_manga_sources` / `preferred_novel_sources` keys are retained as the default source ranking a group falls back to.
- `data/backup/models/BackupMangaMerge` (plus novel twin), `MangaRestorer` / `NovelRestorer`, `PreferenceRestorer`, `BackupCreator` / `NovelBackupCreator`: the ref-based round-trip; re-pointed at the group entity.
- `data/src/main/sqldelight/tachiyomi/data/mangas.sq` / `novels.sq` / `chapters.sq` / `novel_chapters.sq`: the id-reuse and `source_order` overload hazards; addressed by the new tables plus cascade, not by editing these.
- Wider blast radius (resolution callers that must move to the group-lookup API): both migration use-cases, `PropagateTrackerLinks` / `PropagateNovelTrackerLinks`, `GetNovelTracks`, `DeleteNovelTrack`, the Updates group-by-series header, both migration source-pick screens, the Settings clear-merges actions, and `LegacyYokaiDbImporter`. DI is four singletons on the Metro graph (`ReikaiBindings` provides the two merge managers).

## Status

**Complete on `feat/0.4.0`.** Decided 2026-07-15 after the audit; scouted and designed 2026-07-16, scope refined 2026-07-17 (per-group source override, the Phase 0 / Phase 3 storage split, and the Phase 3 order-table park below). Phases 0-4 are done and on-device verified (2026-07-17): persistence (`6876eb03b`), the one-time migration (`d738013fc`), the full resolution cutover across repo ops, both managers, and the library collapse (`0a708899e`, `19e3ea816`, `5f117ebbb`, `950660d21`), reader scope-by-context for both content types (`f1055e632`, `f476028d9`), and backup/restore on the group tables (`1fecfc98f`). Grouping now lives in the tables for both content types, the two-definitions bug is closed, browse no longer pre-merges, the full-library scan is gone, the reader opens group scope or source scope by entry point (the novel History/Updates single-source degradation is fixed), backup/restore round-trips the groups off the tables (additive restore), and the master merging switch's backend is in (UI pending). Backup (the big-bang gate for a preview) has landed.

**Phase 5 slices 1-3 are done and on-device verified (2026-07-18).** The master switch and the same-title suggestion toggle UIs (`403129a05`), the two "clear merges" Settings actions collapsed into one per content type (`166286e26`), and the add-time grouping prompt for manga (`ddce96635`) and novels (`bb4304509`). Grouping is now visible and explicit end to end: adding a series that matches one in your library offers to group it, over group-collapsed duplicate cards the user picks from, on every surface that can add one (manga details / browse / global search / history / MD Follows, and the four novel equivalents; bulk-select adds still skip the prompt by design).

**Phase 5 slice 4 is done and on-device verified (2026-07-18).** The per-group source-ranking override backend (`b35902c2c`): `merge_group.sq` gained its first source-order writers, the repository sets each member's priority and flips the group's override flag atomically, chapter aggregation gained a per-member ranking channel that wins over the source-id list (so two members sharing one source still order distinctly, which a source-id ranking cannot express), and all four aggregation reads resolve the override from the group. The consolidated manage-sources modal (`46ce0d5bd`): one shared `ManageMergeSourcesDialog` replaces the two per-type twins, with live drag-reorder (persists + re-aggregates), a primary badge on the always-trunk-first top row, long-press / Select multi-select for batch split-remove, per-source chapter counts on manga too, and cover-theming for all manga details dialogs. The manga-only abandoned-add fix: the three manga `addToExistingGroup` paths now favorite up front (matching the novel side) before the optional category choice, so cancelling that choice can no longer strand a merged-but-unfavorited member.

The cleanup pass is done: the dead pref-era `MergeGroupAlgebra` was removed (its surviving `parseUnmergedPairs` inlined into `MergeGroupReconstruction`), the managers' unused `clearManualMerges` and `MangaMergeManager.RelatedIdsResult.cleanupCount` are gone, the two dead `novelAutoMergeRequireAuthor` `.changes()` triggers are dropped, and the old pref-era merge-engine docs are retired.

**Phase 5 slice 5 is done: the merge chip-row twins are collapsed onto the shared seam.** A scout confirmed the two chip composables (`MergeSourceChips` for manga, `NovelMergeSourceChips` for novels) were byte-identical bar their per-type source-info type and `mangaId` / `novelId` field names, and their two info types (`MangaViewModel.MergeSourceInfo`, `NovelMergeSourceInfo`) are structurally identical. They are now one shared `reikai/presentation/components/MergeSourceChips.kt` taking the neutral `ManageMergeSourceRow` (the same row type slice 4's shared `ManageMergeSourcesDialog` uses); both details screens map their `mergeSources` state into it, exactly as they already do for the manage-sources dialog. The two twin files are deleted. The collapse-helper twins (`MangaMergeCollapse` / `NovelMergeCollapse`) were left per-type on purpose: the scout found they share only a ~12-line bucket-and-pick algorithm while their outputs (`List<LibraryItem>` with in-place badge stamping vs a `CollapsedNovel` wrapper) and downstream consumers genuinely differ, so extracting a generic used twice would trip KISS for no anti-divergence gain. This closes the merge rebuild.

## Decisions & tradeoffs

- **A group has two member reads, and they must never be collapsed into one (decided 2026-08-06).** Removing an entry from the library preserves its group, so a re-add rejoins it; the entry must nonetheless stop feeding what the group shows. `getFavoriteMembers` is what every display and aggregation read asks for, resolved once at `EntryMergeManager.computeRelatedIds` so the chapter list, the source chips, the reader, resume, the tracker interactors and the migration source picker are all scoped by one clause rather than a filter per call site; the three group-aggregate queries carry the same filter. `getMembers` stays the whole truth and is what the split undo, backup create and restore, and the repository's own rewrites read: scoping it would silently drop a removed member from a restored group. A test pins each half. The one deliberate exception is `overrideRankingMemberIds`, which still returns the whole group, because a removed member only occupies a position the ranking skips over.
- **Every path out of the library hands out the group's tracker (decided 2026-08-06).** A merged group shares one tracker binding, and the hand-out skips non-favorites, so it has to run before the unfavorite. Only the manage-sources removal did that; the library bulk delete, the details remove, the browse remove and the gallery-chain reconciler did not, and their sources came out untracked. `EntryMergeManager.handOutTrackersBeforeRemoval` does it once per group and no-ops with grouping off, where nothing was shared to hand out.
- **Ground-up and both content types from the start**, rather than manga first. Novels are already the weaker half, and doing manga alone would entrench the duplication the unified-content goal exists to remove.
- **Persisted identity, not Komikku's model.** Komikku gives the group identity via a real manga row with a synthetic source plus a `merged` join table, which buys `source == MERGED_SOURCE_ID` as a single dispatch predicate but costs fake rows every query must know about. A group table gives the same identity without them.
- **Two per-type member tables (chosen over a single discriminated table).** A single member table cannot foreign-key-cascade to two parents in SQLite; the per-type split buys a true `ON DELETE CASCADE`, which is the structural fix for the stale-pref capture. Cost: some SQL-layer duplication, inherent to two id spaces.
- **Order computed and cached in a group-owned table: designed, then parked before Phase 3 (2026-07-17).** A Phase 3 scout confirmed the two things that justified the table are already handled. First, the merge stitcher no longer overwrites `source_order`: both readers restamp the merged reading order onto an in-memory `.copy` only (`MergedChapterProvider.restampReadingOrder`, `NovelDetailsViewModel.restampReadingOrder`), never writing it back, so the DB column already carries its single per-source meaning (the pre-fix overwrite the Why section describes was closed in `3498c37eb`). Second, cross-source resume is derived live from read-state (first unread chapter in aggregated order), not a stored pointer, so no persisted order is needed for it. Phase 3 therefore keeps ordering in-memory per open and ships only the source-ranking override, the novel History/Updates degradation fix, and reader scope. The table (transactional recompute writer plus validity fingerprint, kept above as the revive design) stays parked; revive it only if on-device use over a few days surfaces a concrete need: recompute cost on a hot path, or a persisted order/scope the reader must page through consistently. Tracked in ROADMAP Parked.
- **Revived and landed for a different reason than either park condition anticipated (2026-09-08).** Neither recompute cost nor reader paging brought it back. The library badge had been given its own SQL restatement of the stitch's rules over per-chapter match keys, and a restatement cannot express a chapter placed by position rather than by key, so the badge and the chapter list reached different totals for one series. The table exists now as `merged_chapter_unit`, holding the stitch's own output (which merged chapter each source's chapter belongs to, and its rank among the copies) rather than only an order. Staleness is the derived-inputs comparison the match keys already used, lifted from chapter scope to group scope, so a stale group is rebuilt whole; the validity fingerprint above was not needed. The list, the reader and the read carry-over still stitch live, which is the same walk rather than a second definition; pointing them at the table is the remaining work. See [merged-read-state.md](merged-read-state.md).
- **Per-group source ranking overrides, not replaces, the global list.** The global `preferred_manga_sources` / `preferred_novel_sources` ranking stays and covers the common case; a group falls back to it unless it sets an explicit override (a boolean on `merge_group` plus `source_priority` on its members). This keeps existing behavior on upgrade (every migrated group defaults to the global list, so nothing per-group is computed at migration) and folds the standing ROADMAP "Per-group Preferred-sources override" item into the rebuild.
- **Auto-merge-by-title becomes an explicit prompt.** It stays useful (it still catches same-title dupes at add time) but stops being silent: a reporter on unseensnick/Reikai#49 insisted they were not merged while the code had grouped them by title. After the rebuild, everything grouped is a visible row the user chose, surfaced through the existing duplicate dialog.
- **The unmerge concept is retired**, not ported. Explicit membership makes it unnecessary except as a one-time migration exclusion, which deletes the restore-side unmerge-drop bug at the source.
- **Restore is authoritative for the entries a backup names.** Decided 2026-07-17 as additive (each backup group materialized via `MergeGroupRepository.merge`, local-only groups untouched) and replaced once that proved order-dependent: every write landed on what the previous one had left behind, so backing up `{A,B}` and `{C,D}` and restoring onto a library where A had been merged with C produced a single four-member group whose card interleaved four sources, with nothing in the error log. Every group is now resolved against ONE snapshot of local membership and written with `MergeGroupRepository.materializeGroup`, which states the whole group rather than folding into an existing one. An entry the backup groups leaves whatever local group it was in; local members the backup says nothing about keep their own group when at least two of them remain, with their hand-set order, and are left standalone otherwise, so a restore rearranges only the entries it actually describes. This still fixes the original blind-union-plus-dropped-unmerge bug (a deliberately split pair no longer re-merges itself), and on a fresh-install restore all three shapes are equivalent.
- **Membership is the only thing the backup format carries.** It has neither member order nor the per-group ranking flag (a separate, parked gap), so where the named entries already had a local group, that group's answer to both is the only one anybody has and it is carried across. Resetting instead would flatten an order the user dragged into place, silently moving the library cover and the details trunk onto whatever the global preferred-source list ranks first. Survivor and append rule match `MergeGroupRepository.merge`: the group of the first named id that has one decides the order and the flag, its members keep their relative order, and entries it did not contain go last; members of that group the backup does not name fall to the remainder rule above. The ranking is the one thing that does not always survive: a group keeps it only while it still leads with the source the user put in front, which holds for a restored group and a remainder alike.
- **Reader scope by entry point (Option 2, decided 2026-07-17).** Opening a merged-group chapter shows either the unified group list or one source's own list, picked by where you tapped: Details / Library / History open group (continue the series), Updates / explicit source-open open source (stay in that source's feed). Rationale: Updates is inherently a per-source event and is where the group's chapter-number dedup bites hardest (a non-trunk source's new chapter is dropped from the unified list, so group scope would open a chapter that is not in its own list, the case `MergedChapterProvider.withOpenedChapter` had to patch around); History is a reading-timeline resume, so it continues the series. Both content types obey the one rule, ending today's accidental split (manga always group; novels source-scoped from Updates / History). This supersedes the earlier `merge-aware-manga-reader.md` Intent patch rather than reviving it: that extra had to double as group identity, whereas now identity is resolved from the entry id and the signal carries only scope. The mechanism that design got right (scope the reader's `chapterList`, keep `mergedGroup` full so the group-wide read-state pass still de-dupes) carries over.
- **Phase 5 toggle placement (decided 2026-07-18).** The master switch goes in the **library display menu** (and Settings): it changes how the library looks right now (off expands a group to its per-source entries, on collapses them), a display concern. The same-title suggestion toggle lives in **Settings only**: it affects add-time behavior, not the current view. `auto_merge_same_title` (left functionally inert after the Phase 2 cutover) is repurposed from silent auto-merge to **gating whether the add-time duplicate dialog offers "Add to existing group"**, so grouping stays an explicit choice.
- **Add-time grouping is a selection over group-collapsed duplicates, not a merge of every match (decided 2026-07-18).** Duplicate detection is deliberately fuzzy: `mangas.sq` matches a favorited entry by title **substring** (`lower(title) LIKE '%…%'`) or a shared tracker id. So the "duplicates" list can contain a genuinely different series ("Solo Leveling: Ragnarok" matches "Solo Leveling"), and merging the new copy with every listed duplicate over-groups distinct series. Only the user can tell them apart, so grouping is an explicit pick:
  - **Group-collapse.** Each model resolves the duplicates' existing groups (`MergeGroupRepository.getGroupId`) and passes a `groupIdByMangaId` map; the dialog collapses same-group duplicates into one card (a merged group of 4 renders as a single card, a stray renders as its own). The common "join the group I already have" case is then one pick, and non-members are structurally excluded.
  - **Selection mode.** A `Checklist` toggle button (copied from the browse toolbar's `onToggleSelectionMode` action) turns it on. Off, the existing gestures are untouched (tap = migrate, long-press = open). On, tap toggles and long-press range-selects. The "two-finger range-select" is not a multi-pointer gesture: it is long-click plus a `toggleRangeSelection` indexOf-over-the-list + anchor algorithm, ported from the standalone `RelatedMangasBrowseViewModel` precedent (the library's own copy lives in `LibraryViewModel`). `Modifier.selectedBackground` (presentation-core) draws the highlight; the library grid's `selectedOutline` / `GridItemSelectable` are `internal` and not reusable here.
  - **The merge** then takes only the selected members; `merge()` absorbs the whole group of any selected member, so one member of a group is enough to join it. Selection state lives in the dialog (an ephemeral sub-flow), so no ScreenModel gains selection plumbing.
- **An abandoned add strands a group member: fix folded into the override slice (decided 2026-07-18).** The add-time flow merges the new copy, seeds its categories from the group's, then favorites it. With no default category set the favorite defers to the "choose a category" dialog, and dismissing that dialog leaves the copy merged and filed but never favorited. Membership is not favorite-filtered (`MergedChapterProvider.load` stitches every member's chapters), so the stranded copy quietly feeds chapters into the group's merged list and shows in manage-sources while staying invisible in the library. **Manga-only:** the novel add-to-group path already favorites up front (`NovelLibraryAdder.favoriteReturningId` / `NovelDetailsViewModel.addToLibrary`) before its category prompt, so a dismissed novel add leaves the copy merged **and** favorited (safe); it never strands. The fix brings the manga path to that same shape rather than adding new plumbing: the three manga `addToExistingGroup` paths (details `MangaViewModel`, `HistoryViewModel`, the shared `MangaLibraryAdder`) now favorite up front, then merge + seed, then run a **non-gating** category step (apply the default, or show the picker), so the favorite has landed before the picker can be dismissed. The browse/search picker gained an `alreadyFavorited` flag on `Dialog.ChangeMangaCategory` so its confirm files categories without re-toggling the favorite (matching the novel side's `applyCategories`); the details/history picker re-favorites idempotently, so it needed no flag. This closes the manga/novel divergence in favorite timing instead of patching around it. It rides with the override slice, not the add-time slice, because that slice already reworks the manage-sources modal, which is where a stranded member is visible and removable. Shipping the prompt first is acceptable: cancelling an add is rare and deliberate, and re-adding the entry repairs it. The merge-on-cancel half predates the selection rework (`83ff20a8d` fired the merge in a parallel coroutine regardless of the dialog's outcome); the category seeding added the stray category rows, which are inert (nothing reads a non-library entry's categories) and overwritten by the next add.
- **Manage-sources modal redesign (design C1, decided 2026-07-18).** The modal keeps its select / remove / split actions and gains per-group source reordering that drives the override. C1: each source row has a drag handle (live reorder, top = trunk, badged `● primary`) plus per-row Split and remove (✕); long-press enters the app-standard contextual multi-select for batch Remove / Split; a Reset-order action clears the override back to the global default; Remove-all stays. Reordering applies **live** (like the download queue) and sets `override_source_ranking` + `source_priority`; Reset clears it. C2 (always-visible checkboxes, no long-press, busier rows) is the backup if long-press multi-select tests poorly. The modal redesign is pulled into Phase 5's override slice; the broader component consolidation stays deferred (below).
- **Component consolidation deferred to a fresh deep-researched session (decided 2026-07-18).** Collapsing the parallel manga/novel merge components (managers, collapse helpers, chips, manage-sources dialogs) onto the shared `Entry*` UI-model seam (per [unified-content-ui.md](unified-content-ui.md)) is the **last Phase 5 step, its own commit, done in a fresh session after a deep code-research pass**, not surface-level. The two managers are domain-layer and already share `MergeGroupRepository` (a separate, simpler merge); the UI twins fold onto Entry mappers + slots. Only the manage-sources modal is pulled forward, its redesign lands with the override slice. (Since shipped: that consolidation happened in Phases A and B; see [merge-component-consolidation.md](merge-component-consolidation.md).)
- **Pooled reader/chapter view has no page bar; a single source keeps its pagination (carried forward from the retired novel-merge record, still in effect).** The unified list pools whatever chapters are in the DB across every grouped source and shows no page bar, because pages don't align across sources so a unified page bar is meaningless. Selecting one source shows that source's own paginated list. Tradeoff: a partially-fetched paginated source contributes only its fetched chapters to the pooled view until a refresh-all; single-page sources (the majority) are always complete. Aggregation itself (`ChapterAggregation` / `NovelChapterAggregation`: distinct-recognized-number trunk and `Float`-narrowed dedup for manga, most-chapters trunk and title-first `matchKey` for novels) is unchanged by the rebuild and documented in those files' KDoc.
- **Merged "All" view can hide a chapter present in a single source (novel cause fixed, manga variant parked, 2026-07-18).** Reported on-device: a chapter shows in a source's own chapter list but reads as "missing" in the unified "All" view. Two distinct mechanisms, one per content type.
  - **Novel (fixed).** `NovelChapterAggregation.matchKey` matches chapters across sources by a normalized title, and the normalizer stripped every standalone number token, so a sequel-titled chapter collided with its base: "Pleasureful Repeats 2" and "Pleasureful Repeats" both reduced to the same key, and when the sequel was only on a sibling source it was deduped out of the unified list (then flagged by the missing-chapter detector). The number-stripping exists to drop leading chapter-number noise ("Chapter 1 - 0 Surviving Just to Die" and "0 Surviving Just to Die" must still collapse to one). Fix: strip only the LEADING number tokens; a number that follows a title word is kept, so the sequel stays distinct while the leading-noise case still collapses. Covered by a new `NovelChapterAggregationTest` case.
  - **Manga (parked, watch item).** `ChapterAggregation` dedups by recognized chapter number (Float-narrowed), not title, so the novel bug does not apply. A per-source chapter is absent from "All" by two intended paths: a sibling source's chapter whose number is not recognized is dropped (it can't be matched, so only the trunk's unrecognized chapters are kept), and a false number-collision drops one of two genuinely-distinct chapters that recognize to the same float. Both are the deliberate number-dedup tradeoff, so telling a real bug from working-as-designed needs a concrete manga repro (which series, which chapter's title and number). Tracked in ROADMAP Parked; revive with an example.
- **Out of scope (adjacent, not this rebuild):** duplicate DB rows from URL mismatch (a source handing back a different URL for a series already stored makes a second row that no grouping system pulls in automatically). That is a duplicate-detection / URL-canonicalization concern; the add-time title prompt catches it on add, but the rebuild does not solve row-level duplication itself.
