# ViewModel migration (Voyager ScreenModel to AndroidX ViewModel)

> **Status: resuming.** Both halves of the deferral trigger are met: upstream shipped the migration in v0.20.2 and fixed its own `private` models in the same release. The inventory below was re-derived against the released code on 2026-08-06 and replaces the old `c3b99aea0` snapshot. The [durable approach](#approach-durable) and [durable lessons](#durable-decisions-and-lessons) survived the re-derivation intact; the counts and the phase split did not, and are corrected here.

## Goal

Take Mihon's migration off Voyager's `ScreenModel` onto AndroidX `ViewModel` (mihonapp/mihon#3594), across Reikai's own screens as well as the Mihon ones upstream's diff covers, and retire the `voyager-screenModel` dependency. No user-facing change; the payoff is staying on upstream's screen architecture so every later sync applies cleanly.

## Why

It is the one unported upstream change, and it is not a routine sync. Upstream removes the `voyager-screenModel` dependency, adds a `:core:viewmodel` module, and renames every `*ScreenModel` / `rememberScreenModel {}` to `*ViewModel` / `viewModel<T>()`. Reikai has its own screen models that upstream's diff does not cover at all, plus Mihon model files carrying `// RK` patches. Deferring it means every future sync of any screen file fights a base that no longer matches. That standing cost is the reason to do it eventually; the reason not to do it yet is in Status.

## Status

**Done, bar one deliberate holdout.** Phases 0 through 6 have landed. **Every Mihon file is on `ViewModel`**, so an upstream screen-file sync now applies against a matching base, which was the point. Two files still import `cafe.adriel.voyager.core.model`, both Reikai's own: `NovelReaderScreen.kt` and `NovelReaderScreenModel.kt`.

**The novel reader is not being migrated, decided 2026-08-07.** The reader takeover ([content-layer-reader-surface.md](content-layer-reader-surface.md), which absorbed Option 3 and rejected its fold-in shape) puts a Reikai-owned engine over two providers in Mihon's `ReaderActivity` and deletes `NovelReaderScreenModel` outright, so migrating its 878 lines and re-driving TTS, progress and history on device is work with a scheduled expiry. The usual payoff does not apply either: the file is `reikai.*`-owned with no Mihon counterpart, so migrating it buys zero sync diffability. **The cost of stopping here is one dependency**: `voyager-screenModel` stays in the catalog and the bundle, diverging from upstream on two lines. No Mihon file depends on it, so nothing else is held back. It leaves when Option 3 deletes the model.

- **Phase 0** (`fa06232cd`): `core/viewmodel` taken verbatim, catalog and settings wired, both systems live side by side, two pilots migrated (`NovelSourcesFilterViewModel` bare, `MetadataViewViewModel` factory-backed). R8 settled, see below.
- **Phase 1** (`ca1fd1096`): fifteen leaf-screen files, thirteen taken verbatim and two hand-merged (`ClearDatabaseScreen`, `RestoreBackupScreen`). Includes mihon `98705910e`.
- **Phase 2a** (`52a0bc077`): the Sources, Extensions and Migrate tabs and their models, both content types, twenty files.
- **Phase 2b**: the browse cluster's heavy half, 26 files. `GlobalSearchViewModel` taken wholesale (its predecessor was byte-identical to pre-migration upstream); five hand-merges, each drift-checked against the post-migration blob with only RK hunks left. Four factories, three bare models.
- **Phase 4**: details and reader settings, 31 files, sixteen models, the largest phase. Only the reader settings sheet stays hand-constructed, as upstream leaves it. `MangaViewModel` is the big hand-merge (2044 lines against upstream's 1201) and takes upstream's companion byte-identically; the reader settings model and its six consumer files were byte-identical to pre-migration upstream, so all seven were taken verbatim.
- **Phase 5**: history, updates, downloads, more, stats plus the two leaf pairs, 23 files, twelve models. Every Mihon file was drifted, so no verbatim copies; upstream's diff for these paths is 189 insertions against 189 deletions with zero non-mechanical lines, a pure rename spec. Eleven bare models and one factory (`PagePreviewViewModel`, keyed on `mangaId`). Carries the `MoreViewModel` un-privating and the `DownloadQueueViewModel` `onCleared()` conversion.
- **Phase 3**: the library cluster, 20 files, eight models. Every file was drifted, so there were no verbatim copies at all; upstream's own diff for these paths is 123 insertions against 123 deletions with zero non-mechanical lines, so it served purely as a rename spec. Seven bare models plus two factories (`UpdateErrorsViewModel`, `LibraryEngine`). All 59 scope sites swapped 1:1.

An earlier attempt on `feat/0.4.0` was reverted and preserved in a **local-only backup branch** (`backup/feat-0.4.0-viewmodel-migration`). It predates the released upstream shape, so treat it as a source of lessons, not of code.

**Both halves of the trigger are met.** The migration `c3b99aea0` shipped in **v0.20.2** (2026-08-01), with v0.20.3 and v0.20.4 on top; the `private`-model crash fix `98705910e` (mihonapp/mihon#3609) shipped in the same release. Take the fix wholesale rather than hand-widening an enumerated list to `internal`.

**The trip-wire also fired.** Waiting was not free. The old snapshot counted about 68 model files and 79 call sites; a cycle of Reikai screen work later it is 74 model classes and 72 call sites, with the Reikai-owned share up from about 33 models to 41. Every further cycle of screen work adds models the migration has to carry.

**Sync consequence, corrected.** The old wording said the synced base stays before the migration commit. It does not: `c3b99aea0` is an ancestor of the current synced base `45b1e781e`, because later commits were ported around it and the base advanced past it. Reikai is therefore already carrying an unported commit below its own base, which is the state the deferral was meant to avoid. [upstream-sync.md](../upstream-sync.md) carries the same correction.

**Sequencing, amended 2026-08-07.** The reader step is dropped from this initiative (see Status) and its work is absorbed by the tsundoku migration, which the owner has committed to landing **inside 0.4.0**: the release is now cut after it rather than before. So the order is the sync backlog, then the history and updates redo, then the tsundoku reader migration, then the cut.

**Sequencing, decided 2026-08-06.** The migration runs first, then the 33-commit upstream sync backlog lands on a matching base, then the content layer's history and updates redo. Three reasons: the redo's own files (`HistoryScreenModel`, `UpdatesScreenModel`) are renamed by the migration, so doing the redo first means doing that work twice and destroying rename detection on the files it just diverged; two of the redo's steps are already inside migration phases 5 and 7; and the sync's largest commit (`6d69903a5`, java.time to kotlinx.datetime) lands directly on the date handling both surfaces use.

The old sequencing note (library surface first) is spent: that surface shipped. Its claim that the port is "R097, about three percent, purely mechanical" was the best case quoted as typical and is corrected in [the inventory](#inventory-re-derived-against-v0204).

## Two layers, and which one to trust

Upstream's side of this is now fixed: v0.20.4 is released and the diff will not change under us. Reikai's side still moves, so the inventory below drifts every time a screen is added or a model is split. Re-run its counts before starting a phase rather than trusting a number written weeks earlier.

What does not drift is the reasoning: why a bare `viewModel<T>()` resolves without a factory, why a `private` one crashes, why per-screen scoping is free under Voyager, why the phases have to be vertical, and which of Reikai's own structures (the `MangaDexFollows` subclass, the drift-heavy library files, the merge code) are traps regardless of upstream's exact diff. Those are properties of AndroidX `ViewModel`, kotlinc, R8, and Reikai's architecture, not of any particular upstream commit.

## Approach (durable)

**Superseded by the Metro DI port (2026-08-21 note): models are graph-constructed now, `CreationExtras` keys are gone and `StateViewModel` was removed upstream. Kept as the record of what this migration planned.** **The default factory does the work, and Reikai's Injekt pattern survives untouched.** A model whose constructor arguments are all Injekt defaults is resolved with a bare `viewModel<LibraryViewModel>()`, no factory. A model needing real arguments (a `mangaId`, a `sourceId`) gets a `companion object` holding typed `CreationExtras` keys plus a `viewModelFactory`, and the call site passes values through `extras`. A `Context` parameter comes from `Injekt.get<Application>()` instead of `LocalContext`. The replacement base is one file: `StateViewModel<S>` (`mihon.core.viewmodel`), a shape-for-shape stand-in for `StateScreenModel`. No shim and no typealias: a hard rename.

**Per-screen scoping is free, and needs no wiring.** `viewModel()` inside a Voyager `Screen` resolves to that Screen's own `ViewModelStore`, not the Activity's, because `voyager-navigator`'s Android variant hands every Screen an `AndroidScreenLifecycleOwner` (which implements `ViewModelStoreOwner`, provides `LocalViewModelStoreOwner`, and clears the store on pop). This holds only under a Voyager `Navigator`: content hosted outside one falls through to the Activity store silently. `ReaderActivity` and `WebViewActivity` are outside one and are unaffected, because `ReaderViewModel` is deliberately Activity-scoped via `by viewModels()` and `ReaderSettingsViewModel` stays a manual `remember { }` construction (it takes lambdas, so no factory is possible).

**Phases are vertical, one screen cluster at a time.** Reikai's models are constructed inside Mihon files (a Mihon tab builds a Reikai novel model, a Mihon queue screen builds both queue models), so "all Mihon files, then all Reikai files" cannot compile in between. Each phase takes the Mihon model, the Reikai models it constructs, and every call site together. The two systems coexist throughout (both dependencies present, some models migrated and some not), which is what allows phasing; `voyager-screenModel` drops only at the end.

**Each cluster is one commit, compiled, spotless-clean, and opened on-device.** A green build does not prove a screen opens (the `private`-model crash below compiles fine and dies on tap), so every migrated screen is exercised on a minified `preview` build before its phase is called done. Commits cite the upstream PR and the migration commit SHA.

## Durable decisions and lessons

These are the findings worth carrying forward verbatim; none depend on upstream's exact diff.

- **R8 needs no new keep (proven).** AndroidX's shipped consumer rules keep only `<init>(SavedStateHandle)` and `<init>(Application, SavedStateHandle)`; a `-keep class mihon.**` preserves the class, not its members. A bare `viewModel<X>()` on an all-Injekt-defaults model resolves through the reflective `SavedStateViewModelFactory` to a no-arg constructor that nothing keeps, so the risk was real. It was settled directly on the reverted attempt: the minified `preview` pilots opened cleanly and `dexdump` on the preview APK confirmed the no-arg constructors survive. Reikai's existing `ReaderViewModel` survives only by accident of shape (`@JvmOverloads constructor(savedState: SavedStateHandle, ...)` generates exactly the kept constructor). Re-confirm on the first minified build when resumed, but no keep is expected; if one is ever needed it is one line, `-keepclassmembers class * extends androidx.lifecycle.ViewModel { <init>(); }`.
- **A bare-`viewModel<T>()` model must never be `private`.** The default factory instantiates reflectively from `androidx.lifecycle`, and a private top-level Kotlin class is package-private in bytecode, so `newInstance()` throws `IllegalAccessException` the moment the screen opens. This is **not** an R8 trap: the modifier comes from kotlinc, so it fails on every build type, debug included, and a green build hides it. The fix is one word: widen to `internal` (public in bytecode, still module-private in Kotlin), marked `// RK`. An explicit factory is the other valid fix, and is what upstream already does wherever a model takes arguments, which is why only the no-arg ones break. Upstream currently ships this bug on several of its own no-arg models (`CreateBackup`, `ClearDatabase`, `MigrateDialog`, `More`, and at least `MigrationConfigScreen.ViewModel`, with more likely), so taking such a file verbatim imports the crash. Upstream has since fixed it in mihon `98705910e` (mihonapp/mihon#3609), which only drops the `private` modifier from the affected models: take that wholesale rather than hand-widening an enumerated list to `internal`.
- **`flowWithLifecycle` cannot survive inside a ViewModel.** A ViewModel outlives the composition, so a `Lifecycle` captured from it is the dead Activity's after a rotation. Upstream removed all of its `flowWithLifecycle` calls for this reason. Reikai's own instances are the custom-info overlay and the gallery-metadata flow, both cheap and self-triggered, so dropping the lifecycle parameter is safe; the flows then collect until the Screen is popped rather than pausing on navigate-away. The one flow worth watching is any chapter-list combine that takes `downloadCache.changes` / `downloadManager.queueState` unfiltered, since it re-fires on app-wide download activity; if that measures as real, scope those two inputs to the open entry (as the download flows already are) rather than reintroducing a lifecycle. Rejected alternatives: `ProcessLifecycleOwner` (a permanent divergence in the hardest-to-merge file) and `stateIn(WhileSubscribed)` (rewrites the worst file's state model and diverges from upstream forever).
- **`ioCoroutineScope` is deleted, per upstream's own rule.** It was `Dispatchers.IO`; `viewModelScope` is `Dispatchers.Main.immediate`. Rewrite the sites that launch real work to `viewModelScope.launchIO` and the `stateIn` / `cachedIn` sites to plain `viewModelScope` (their scope only hosts collection; the flows do their own IO). This is compiler-enforced: `ioCoroutineScope` is an extension on `ScreenModel`, so it stops resolving the instant a class extends `StateViewModel`, and no site can be missed silently.
- **A subclass that overrides a parent model needs its own factory.** Kotlin companion objects are not inherited, so a model that extends another and overrides methods (Reikai's `MangaDexFollowsScreenModel` extends `BrowseSourceScreenModel` and overrides its paging + metadata hooks) will, if it leans on the parent's factory, construct the parent type and silently lose every override, with no compile error. Give each such subclass its own key(s) + factory. This is a property of Reikai's class hierarchy, so it applies whatever upstream renames the parent to.
- **Drift, not markers, is the real hazard in the big files.** Reikai's library and details models and tabs are well over upstream's length, so upstream's diff maps to them as a spec to re-lay by hand, not a patch to apply by line. Budget these as re-writes guided by the upstream diff, and drift-check the result against the upstream post-commit blob (only RK-attributable hunks may remain).
- **Merge code is out of scope for the surfacing pass.** The merge-system rebuild ([merge-system-rebuild.md](merge-system-rebuild.md)) replaces or absorbs the merge surfaces and is ground-up across both content types; roughly a third of the details model's Reikai lines are merge handling. Extracting them during this migration is work the rebuild discards plus a conflict handed to it. Likewise leave blocks entangled with Mihon's private state (merged-chapter init, group refresh, related-mangas loader, merge actions) and the deliberate `EntryDetailsUiState` coupling ([unified-content-ui.md](unified-content-ui.md)).
- **Surfacing buried Reikai logic rides this migration, it is not a separate sprint.** The blocks worth lifting out of Mihon files are exactly the ones the migration renames, so doing them in the same pass avoids a rename-plus-extraction commit that destroys rename detection. It is the `unified-content-ui` initiative applied to files that skipped it (shared leaf, per-type mapper, optional slots, net-new in `reikai.*`), which is explicitly exempt from the no-standalone-refactor rule when planned and sequenced.

## Inventory, re-derived against v0.20.4

Measured 2026-08-06 against `refs/mihon` at `68aa4b003` and Reikai at `d53563baa`. Reikai's numbers drift with every screen added, so re-run the counts before each phase.

### Upstream's shape

`c3b99aea0` is 91 files, 1503 insertions, 1091 deletions. It renames roughly 29 `*ScreenModel.kt` files to `*ViewModel.kt`, adds the `core/viewmodel` module (three files, of which `StateViewModel.kt` is an 11-line shape-for-shape stand-in for `StateScreenModel`), and **drops `voyager-screenModel` from the catalog and the bundle in the same commit**. Upstream could migrate atomically; Reikai phases, so both systems coexist here until the teardown and no upstream commit models that intermediate state.

**The port is not a uniform find-and-replace.** Rename similarity runs from 68 to 97. Seven files are substantial rewrites rather than renames: `MigrateSearchScreenModel` (68), `ReaderSettingsScreenModel` (73), `WebViewScreenModel` (75), `DeepLinkScreenModel` (80), `MigrateMangaScreenModel` (81), `UpdatesSettingsScreenModel` (84), `HistoryScreenModelStateProvider` (85). `GlobalSearchScreenModel` is a delete-and-add, not a rename, because its constructor lost its default argument values. `LibraryScreenModel` is the 97 the old plan quoted as typical.

**Several upstream renames are no-ops here**, because Reikai already deleted and manifested the file: the migration flow's models (`MigrationListScreenModel`, `MigrateSearchScreenModel`, `MigrateMangaScreenModel`, the config screen's embedded model), `MangaCoverScreenModel`, and `HistoryScreenModelStateProvider`. Check [off-path-manifest.md](../off-path-manifest.md) before porting any rename; a manifested path takes its change in the Reikai twin instead.

**The worked examples to copy.** Factory-needing: upstream's `MangaViewModel` companion, holding `CreationExtras.Key` values plus a `viewModelFactory { initializer { ... } }`, with the call site in `MangaScreen` passing `extras`. Bare: upstream's `CategoryViewModel`, no companion, resolved `viewModel<CategoryViewModel>()`. `ReaderSettingsViewModel` is the third shape: a plain `ViewModel()` taking lambdas, still constructed by hand because a factory is impossible, and still living outside any Navigator.

`98705910e` un-privates twelve nested model classes across nine files by deleting the modifier, no `internal`, no added factory. One file also renames its nested `ViewModel` class to `Model` to stop it colliding with the `androidx.lifecycle.ViewModel` import. At v0.20.4 upstream ships zero remaining private-and-bare models, so files taken verbatim are safe.

### Reikai's shape

**74 model classes across 66 files.** By owner: 41 `reikai`, 15 Mihon-patched (carrying `// RK`), 13 Mihon-clean, 4 `exh`, 1 `mihon`. By constructor: **38 bare** (every parameter an Injekt default) and **36 needing a factory**. The factory work is mostly Reikai's own and upstream's diff gives no template for it beyond the one worked example.

**72 `rememberScreenModel` call sites across 49 files.** Two pass a `tag`, both deliberate and commented (`EntryMigrateHost`, `EntryMigrateDialog`, keyed by content type to avoid a stale instance in a reusable dialog). No `rememberNavigatorScreenModel` anywhere. Roughly 31 sites pass real constructor arguments and become `CreationExtras` at the call site.

**The blast radius is wider than the model files.** 97 files reference a `*ScreenModel` type by name, because composables, adapters and behavior interfaces take models as parameters. Every one of those signatures renames. The heaviest are `ReikaiExtensionsTab`, `ReikaiUpdatesScreen`, `EntryTrackInfoDialog`, `ReikaiSourcesTab`, `EntryMigrationListScreen`, `ReikaiHistoryScreen` and `ReikaiMigrateSourceTab`.

**Nothing outside the app module.** No other Gradle module references a Voyager model, so the migration is app-module only.

**The catalog is missing both new dependencies.** `gradle/libs.versions.toml` has `lifecycle-common`, `-process` and `-runtime` only. Phase 0 adds `lifecycle-viewmodel` and `lifecycle-viewmodelCompose` and puts both in the `androidx-lifecycle` bundle.

### The hazards, located

- **`private` models: 16 exist, 3 actually crash.** The three resolved bare are `MoreScreenModel`, `ClearDatabaseScreenModel` and `CreateBackupScreenModel`, all Mihon files that `98705910e` already un-privates, so taking that fix removes the risk before it can exist here. The other thirteen take real arguments, so their factories construct them directly with no reflection: `RestoreBackupScreenModel`, `WorkerInfoScreen.Model`, `MangaNotesScreen.Model`, `NovelNotesScreen.Model`, `EntryMigrateDialogScreenModel`, and the eight models inside `EntryTrackInfoDialog`. Un-private them anyway, matching upstream, rather than reasoning our way to a narrower fix.
- **`MangaScreenModel` is the hardest file.** It takes `lifecycle: Lifecycle` in its constructor and has **eight** `flowWithLifecycle` sites against upstream's six. Two are Reikai additions with no upstream counterpart: `getCustomMangaInfo.subscribe(mangaId)` and `getFlatMetadataById` behind `mergeGroup.selectedSourceChanges`, both cheap per-manga subscriptions. **Correction, made when phase 4 checked it:** an earlier version of this line said one of those two wrapped a combine over unfiltered `downloadCache.changes` and `downloadManager.queueState`. It does not. That combine is upstream's own first site, and upstream drops its lifecycle wrapper in the migration like all the others, so there is nothing Reikai-specific to weigh. The mapping is exact, upstream's six against our eight, leaving those two extras. Follow upstream and drop all eight.
- **Four `onDispose()` overrides need an `onCleared()` equivalent**: `EntryMigrationListScreenModel`, the migration search screen's model, `DownloadQueueScreenModel` (done in phase 5) and `NovelReaderScreenModel` (the only one left). Missing one leaks a job with no compile error. **AndroidX cancels `viewModelScope` before `onCleared()` runs**, so cleanup written as a coroutine launch inside `onCleared()` silently never happens. Verified against the lifecycle 2.11.0 sources this project builds on: `viewModelScope` is registered as a keyed closeable under `VIEW_MODEL_SCOPE_KEY`, and `ViewModelImpl.clear()` closes every keyed closeable, then the unkeyed ones, and only then invokes `onCleared()`. Both bodies here are synchronous, so both translate 1:1, but the rule binds if either is ever rewritten.
- **`ioCoroutineScope`: 9 sites in 4 files.** One does real IO (`SearchScreenModel`, a source-wide search fan-out) and needs `viewModelScope.launchIO`; the other eight only host flow collection and take plain `viewModelScope`.
- **Two abstract Reikai bases dodge the init-order crash with `stateIn(Lazily)`**: `EntryCoverScreenModel` and `BrowseSourceScreenModel` (for the `MangaDexFollows` subclass). Both must stay `Lazily`; switching either to `Eagerly` re-opens the crash.
- **Four subclasses override a parent model** and each needs its own factory, since companions are not inherited: `GlobalSearchViewModel` (upstream supplies it), `MangaDexFollowsViewModel` and the two cover models. **Corrected in 2b, down from six:** the two bulk-favorite facades were listed here but need no factory, because their constructors are all-Injekt-defaults (so a bare `viewModel<T>()` resolves them through the generated no-arg constructor) and their parent `EntryBulkFavoriteViewModel` is abstract with no companion, so there is nothing to inherit and nothing to lean on. The rule only bites where the parent is concrete and has a `Factory`.
- **Four String-keyed models** take a plugin `sourceId: String` where upstream's equivalents take `Long`, so their `CreationExtras` keys are Reikai's to define: `NovelBrowseScreenModel`, `NovelDetailsScreenModel`, `NovelCoverScreenModel`, `EntryMigrationFavoritesScreenModel`.
- **`ReaderSettingsScreenModel` is manually remembered outside any Navigator**, so `onDispose` never fires today and its scope lives until the Activity tears down. Upstream keeps it hand-constructed; the leak is worth fixing while the file is open.
- **Four tests construct a model directly.** Three inject a dispatcher and are insulated from the swap; `NovelBulkFavoriteScreenModelTest` has no main-dispatcher rule and survives only because the methods it exercises never launch.
- **R8 was settled empirically** on the reverted attempt: `dexdump` on a minified preview APK confirmed the no-arg constructors survive under the existing package keeps. Re-confirm on the first minified build; no new keep is expected.

### Rulings (2026-08-06)

1. **The `MangaScreenModel` lifecycle change follows upstream first.** Drop the constructor parameter and all eight `flowWithLifecycle` sites as upstream did, then test hard, specifically for work continuing while the details screen is backgrounded and for the two app-wide download inputs re-firing. If that testing shows a real cost, fall back to scoping those two inputs to the open entry rather than reintroducing a lifecycle.
2. **Withdrawn. There is no cover-model bug.** This ruling said `EntryDetailsDialog`'s untagged cover resolution was the stale-instance trap its migrate-flow siblings tag against, and a later reading widened it to claim the captured anchor goes stale across a merge-source switch. Phase 4 checked both and neither holds. `EntryDetailsDialogHost` is a `Screen` extension, so `MangaScreen` and `NovelScreen` already key into separate stores and cannot collide. And both adapters anchor the cover on the **group** entry (`State.Success.manga`, `NovelDetailsState.Loaded.novel`), while a source chip moves only `mergeDisplayManga` / `displayNovel` and `selectedSource*`, so the captured anchor is stable by design, which `NovelEntryAdapter`'s own comment states as the intent. No tag, no key, no follow-up. Recorded rather than deleted because the claim was believed twice and would otherwise be re-derived a third time.
3. **The upstream sync backlog runs after phase 6, in one pass.** Four of its commits are cheaper on migrated files, but mixing a feature port into a rename phase makes an on-device regression unattributable.

### Phase map

0. **Foundation and the R8 proof.** Copy `core/viewmodel` verbatim, add the two catalog entries and the bundle change, wire `settings.gradle.kts` and the app dependency, keep `voyager-screenModel`, then migrate two pilots (one bare from `reikai.**`, one factory-needing from `exh.**`), build minified and open both on-device.
1. **Leaf screens.** The marker-free Mihon files with no Reikai entanglement. Verbatim upstream blobs. Take `98705910e` here, so the three bare-private crashes never exist in Reikai's tree. Carries the first `ioCoroutineScope` removals.
2. **Browse cluster.** The browse-source and search models, sources, sources-filter, extensions, plus the Reikai riders: `ReikaiBrowseScreenModel`, the three `reikai*Tab` builders, `MangaDexFollowsScreenModel` (own factory), batch-add, metadata-view, and the novel browse / globalsearch / sources models. The four String-keyed factories land here.
3. **Library cluster.** The library, library-settings and category models plus the library tab, and the novel library / update-errors / preferred-sources models. Risk is drift, not markers: re-lay the big files against upstream's diff and drift-check the result. Verify both library view modes and both content types. **Done**; two findings the phase map had wrong are recorded under [what phase 3 corrected](#what-phase-3-corrected).
   - **Migrate flow, a batch phase 3 uncovered rather than part of it. Done.** Seven models that no phase claimed. Phase 2a took the migrate *tab* (`ReikaiMigrateSourceTab`), not the migrate *flow*, and the gap went unnoticed because the two share a word. **All seven needed a factory**, the largest such batch in the tree: not one had an all-defaults constructor. They also held **the only two tagged resolutions in the tree**, now `viewModel(key = "migrateHost-$contentType")` and the same for the dialog; dropping the key compiles clean and silently gives both content types one shared instance, which is the bug the tags existed to prevent, so each carries a comment saying the key is load-bearing.
4. **Details and reader settings.** Three things land here besides the models. **The off-path manifest's cover row must be repointed in the same commit**: its replacement is `reikai/presentation/details/EntryCoverScreenModel.kt`, the last manifest row still naming a `*ScreenModel` file, and renaming that file without touching the row trips the `pre-commit` hook ("the row protects nothing"). The migrate-flow batch hit the same hook for the same reason, so expect it rather than debugging it. **Reikai's own cover subclass is renamed** (currently `MangaCoverScreenModel` in `reikai/presentation/details/`, the same name as the Mihon file it replaced): after the migration a live Reikai class and a manifested-dead upstream file would share the name `MangaCoverViewModel`, so a syncer grepping it would read the dead upstream file as live. Rename ours to `MangaEntryCoverViewModel` in the same pass, so the file is renamed once rather than twice. **And the cover dialog host needs an explicit factory**: `EntryDetailsBehavior.createCoverScreenModel` returns the abstract star-projected `EntryCoverScreenModel<*>`, which Voyager only uses as a store key but AndroidX would try to instantiate reflectively. A star projection is legal as a reified type argument (verified by compiling a probe), so the type stays; the resolution moves into a remembered `viewModelFactory` whose initializer calls the per-type constructor. Then the details model with its id and from-source factory, plus ruling 1's lifecycle removal and its hard test. Also the cover and notes models, the reader-settings model, the novel details / cover models, the related-mangas model (the only Reikai model holding a `Context`), and the eight `EntryTrackInfoDialog` models, the largest single factory batch in the tree.
5. **History, updates, downloads, more. Done.** The history, updates, updates-settings, more and stats models, Mihon's inert download-queue model, and Reikai's novel history / updates plus both queue models. (The two bulk-favorite models this line also claimed had already gone in 2b.) 23 files, twelve models, eleven bare and one factory. **Renames only**: the content layer's `HistoryUiModel` removal and its `UpdatesUiItem` deletion do *not* fold in here. That instruction predated ruling 3, whose logic applies to it exactly: a structural deletion inside a rename commit kills rename detection on the files being renamed and makes any device regression unattributable. The two are also unequal, and treating them as one item is what made the fold-in look cheap. `UpdatesUiItem.kt` was by then collapsed to its type-neutral last-updated line with one live caller, the shared recents screen, so it was a partially-collapsed file rather than a pending move. `HistoryUiModel` was the real work, and the recents surface did it at 8g: `HistoryViewModel` emits `List<HistoryWithRelations>` now, `toHistoryUiModels` is gone, and `HistoryScreen.kt` is deleted and manifested. Both files are off the tree, so neither can fold into anything.
   - **The leftovers, assigned here so they stop being rediscovered.** Six files belonged to no phase: `exh/pagepreview/PagePreviewScreen.kt` and its model, `eu/kanade/presentation/more/settings/screen/browse/ExtensionStoresScreen.kt` and its model, and `reikai/presentation/novel/reader/NovelReaderScreen.kt` and its model. The first four are small leaf screens and ride phase 5. **The novel reader is its own step**, not a leaf: its model is 878 lines, it has an `onDispose()` override that must become `onCleared()`, and it is the novel engine's render path, so it gets its own commit and its own device pass after phase 5. This is the third time the phase map has been found short (the migrate flow, then `LibraryEngine`, now these), so re-derive the file list from `grep -rl cafe.adriel.voyager.core.model` at the start of every phase rather than trusting the map.
6. **Teardown, docs, ledger. Done, minus the dependency drop.** `Navigator.kt` was taken from upstream, a delete-only change: it differed by nothing but the dead `ioCoroutineScope` extension, whose last user went in phase 5, and the `ScreenModelStore` import that only that block used. The docs below were rewritten, the ledger row appended and the `CHANGELOG` line added. **`voyager-screenModel` stays**, per the reader ruling in Status; dropping it is a one-line change carried by Option 3 and tracked as its own roadmap item.

Phases 7 and 8 of the old plan (off-path debt, surfacing buried Reikai logic) are dropped from this initiative: the content-layer program absorbs both, and carrying them here would stack a behaviour change on top of a rename.

**Docs this migration invalidates and must rewrite with it:** [CLAUDE.md](../../../CLAUDE.md) screen conventions 2 through 7, [.claude/rules/screen-conventions.md](../../../.claude/rules/screen-conventions.md) (two headings are named after the API, and its reference screen points at a file upstream renames), [.claude/rules/architecture.md](../../../.claude/rules/architecture.md), [development.md](../development.md), the naming exemplar in [.claude/rules/code-quality.md](../../../.claude/rules/code-quality.md), and the semi-normative `.claude/skills/scout/SKILL.md` + `.claude/skills/port-audit/SKILL.md`.

### How each phase is executed

Verbatim upstream blobs where the file is marker-free, hand-merge inside the `// RK` islands where it is not, drift-check every hand-merge against the upstream post-commit blob so only RK-attributable hunks remain, and open every migrated screen on a minified `preview` build before calling the phase done. A green build does not prove a screen opens.

**Start every phase with the drift check.** For each candidate path, compare Reikai's file to upstream at `c3b99aea0^` (the last pre-migration commit). Byte-identical means no Reikai drift and no intervening hand-port, so the file can be replaced wholesale with upstream's blob at the synced base `45b1e781e` under its new name. Anything else is a hand-merge. This is cheap, it decides the method per file instead of per phase, and it also tells you which other upstream commits ride in with the copy: run `git log c3b99aea0..45b1e781e -- <new path>` per file and read the list before taking it. In phase 1 that split was thirteen clean against two drifted, and the only rider was `98705910e` itself.

Take the blob from the **synced base**, not from `refs/mihon` HEAD, or the copy silently ports unsynced commits from above the base.

### Gotchas learned in execution

- **Never blanket-replace `model.` with `viewModel.`.** Upstream renames the composable's local `val model` to `val viewModel`, and following it keeps future diffs small, but a file-wide replacement also rewrites `androidx.lifecycle.viewmodel.compose.viewModel`, `mihon.core.viewmodel.StateViewModel` and `tachiyomi.domain.source.model.Source` into nonsense, because those paths contain the same characters. The compiler catches it, but it costs a cycle and it will recur in every phase. Anchor the replacement or fix the import block afterwards.
- **`git mv` stages immediately.** A rename done early sits in the index and rides along in whatever unrelated commit is made next. Check `git diff --cached --name-status` before every commit in this initiative, because it is nearly all renames.
- **Verifying R8 needs every dex file, not just the first.** `dexdump -d` on the APK reads `classes.dex` only, and the app spans four. Extract the APK, dump each `classes*.dex`, then search for `Class descriptor` and the constructor entry. A bare-resolved model must show `<init>` with type `()V` and `PUBLIC CONSTRUCTOR`; the factory-backed one shows its real argument signature. Both were present in phase 0, so the existing package keeps are enough and no ViewModel-specific keep is needed.
- **`run-as` is refused on preview builds** (`package not debuggable`), so the database cannot be pulled from the build the migration is verified on. Verify data effects through the UI there, or reproduce on `debugY2k`, accepting that a debug build proves nothing about R8.
- **Some screens cannot be reached on a preview build at all.** `WorkerInfoScreen` sits behind a debug-only settings section, so its Context-factory shape stays unverified on a minified build. Its mechanism is the same as any other factory model, but record it as unproven rather than assumed.
- **Grepping logcat for `FATAL EXCEPTION` matches the adbd line echoing your own command**, so a count of one means nothing. Filter out lines matching `adbd`, or grep the app's pid with `logcat --pid=`.
- **PowerShell's `-replace` is case-insensitive by default**, which is the wrong default for renaming Kotlin identifiers that differ only in their first letter. A `\bbulkFavoriteScreenModel\b` pattern matched the *type* `BulkFavoriteScreenModel` in an import and lower-cased it. Use `-creplace` for every identifier rename in this initiative.
- **`Select-String -Path "dir\**\*.kt"` does not recurse**, so it reports a clean tree that was never searched. PowerShell's `**` is not a recursive glob (it behaves as a single path segment), and the failure is silent: zero matches reads exactly like zero residuals. Phase 3 passed three "no residuals left" checks that way while two broken imports sat in the tree. **Verify residuals with `grep -rn` through the Bash tool, or `Get-ChildItem -Recurse | Select-String`, never a `**` path.** The same trap applies to a `**`-pathed fix loop, which then silently edits nothing.
- **The token swap can eat an import line.** A blanket `\bscreenModelScope\b` to `viewModelScope` rewrote `import cafe.adriel.voyager.core.model.screenModelScope` into a nonexistent `...core.model.viewModelScope` in the two library adapters, which read the scope off another model rather than owning one. Replace the import lines first, or check them after; this is the `model.` trap above wearing a different token.
- **The migrate flow's Continue FAB does not appear in a `uiautomator` dump**, nor does the row-selection highlight, so an adb drive of that flow reads as "my taps did nothing" when they worked. Screenshot instead of dumping there. Selection is driven by the app bar's Select all, not by tapping a row (a row tap opens details).

### What phase 3 corrected

- **`LibraryEngine` was missing from the phase map.** It is a `ScreenModel` with a caller-supplied constructor, built inside `LibraryTab` from adapters wrapping the two library models. It needs a factory, and it is the only model in the cluster whose construction depends on two others already being resolved.
- **A `Tab`'s models land in the tab host's store, not a per-tab one.** `HomeScreen` wires a `TabNavigator` with no `tabDisposable` and no `ViewModelStoreOwner` of its own, so tab content inherits `HomeScreen`'s `AndroidScreenLifecycleOwner`. All tab ViewModels therefore share one store, keyed by class name, and are cleared together when `HomeScreen` is popped. That is what makes the engine's captured adapter pair safe: the two models it closes over cannot outlive or predecease it. It also means **two tabs resolving the same type now share one instance** where Voyager gave each its own. Not currently live (the six migrated tab resolutions are all distinct types), but it is a new way to break something quietly.
- **The scope swap is dispatcher-neutral.** Voyager's `screenModelScope` is `SupervisorJob() + PlatformMainDispatcher`, which on Android is `Dispatchers.Main.immediate`, the same as `viewModelScope`. The IO variant was always the separate `ioCoroutineScope` extension. So a plain `screenModelScope.launch` becomes `viewModelScope.launch` with no added `launchIO` and no behaviour change.
- **`CategoryViewModel` runs repository writes on a bare `launch`**, which the trace through `CategoryActions` to SQLDelight's `EmptyCoroutineContext` suggests means the main thread. That predates this work and upstream keeps the same shape, so following upstream preserves it exactly. Left alone deliberately; it wants a StrictMode check, not a rename-phase fix.

### Verification coverage so far

Recorded so a later session knows what is actually proven rather than assumed. All on a minified `preview` build on the Fold.

- **Opened and rendered:** the novel sources filter and gallery info screens (phase 0 pilots, one bare and one factory), clear database (hand-merged, bare, un-privated), create backup (bare, un-privated), manga notes (factory taking a domain object). Clear database was additionally exercised through its delete path, which works once "Keep entries with read chapters" is unticked; with it ticked an entry with any read chapter or non-zero last-page-read is kept, which is upstream's rule and not a defect.
- **Not opened:** deep link, webview, extension filter, extension details, upcoming, restore backup (needs a real backup file picked first), worker info (unreachable, see above).
- **Phase 2b, all thirteen screens opened and exercised.** Manga browse, the random-target screen it pushes, MangaDex Follows, global search, novel browse, novel global search, the migrate deep picker, bulk-selection in four hosts, and batch add. Three checks worth repeating rather than re-deriving: Follows returned the signed-in follow list rather than the source's Popular listing, which is the only observable proof the subclass factory did not construct the parent; the random-target screen and the deep picker both carried a non-default `listingQuery` through `CreationExtras`, proving per-screen scoping and the extras path; and a long-press on a global-search result raised the category dialog, proving the RK `MangaLibraryAdder` calls still run once the scope is `viewModelScope`.
- **R8 re-confirmed for 2b:** `dexdump` across all four dex files shows `<init>()V PUBLIC CONSTRUCTOR` on `BulkFavoriteViewModel`, `NovelBulkFavoriteViewModel` and `BatchAddViewModel`. Still no ViewModel-specific keep needed.
- **Phase 3 build gates green, device drive not yet run.** Full `--rerun-tasks` recompile clean, `spotlessApply` touched only the twenty intended files, and `:app:testDebugUnitTest` is 762 tests / 0 failures, unchanged from the pre-phase baseline. `LibraryEngineTest` passes unmodified, as predicted: the engine has no `init` block and keeps every scope use behind `by lazy`, so direct construction still touches neither DI nor the scope and needs no main-dispatcher rule.
- **R8 re-confirmed for 3**, `dexdump` across all four dex files of the `preview` APK. The six bare models (`LibraryViewModel`, `LibrarySettingsViewModel`, `CategoryViewModel`, `NovelLibraryViewModel`, and both preferred-sources models) each keep `<init>()V PUBLIC CONSTRUCTOR`, so the reflective default factory can still reach them. `UpdateErrorsViewModel` keeps both its no-arg and its `(ContentType, I)V` defaults constructor; `LibraryEngine` keeps only `(List)V`, which is correct, since its provider list has no default and the factory calls that signature directly. Still no ViewModel-specific keep needed.
- **Phase 3, all eight models opened and exercised on a minified `preview` build**, the read-only matrix on the Fold and the destructive half on the emulator. Zero `FATAL EXCEPTION` / `IllegalAccessException` across both drives. Manga library (159 entries) and novel library (46) each render; the settings sheet's four pages all render, with `LibrarySettingsViewModel.trackersFlow` live on the Filter page and both `EntryDisplayPage` and `ReikaiCategoriesPage` rendering off the renamed parameter; categories, preferred sources (both tabs, so both models) and update errors all open. Both library view modes were exercised by toggling "Show all categories in one list" and toggling it back.
- **Three checks worth repeating rather than re-deriving.** The **All chip** is the engine's factory proof: it renders a category of 44 against a total of 205, which is 159 manga plus 46 novels bucketed into one list, so the engine was built with both providers rather than one or none. A **long-press on a manga plus a tap on a novel** drove the app bar count to 2, proving the `EntryId`-keyed selection spans content types under `viewModelScope`. And **update errors carries a non-default `CreationExtras` value**: reached from the library it opens on All and lists three errors, reached from the novel library it opens empty, and switching that same screen to All reveals the same three. The empty state is the proof, since all three errors are manga.
- **Category create, rename and delete were driven end to end on the emulator**, which is the only part of the matrix that writes. All three land, and the delete raises the "Deleted 1 category / Undo" snackbar, so `CategoryViewModel`'s bare `viewModelScope.launch` write paths work unchanged. The emulator was left back at zero categories.
- **Migrate flow: all seven models opened and exercised on a minified build**, on the Fold, zero crashes. The config screen (twice), the single-entry search (real searches across six novel sources, one match returned), the batch list (two rows, driver settled 2/2, the "Stop migrating?" exit guard taken), the keyed migrate dialog reached from a search result with its flags loaded, the merged-entry source pick (a manga merged across seven sources), and the per-source favorites list. Both multi-entry drives were abandoned before any commit.
- **The duplicate-migrate host was driven to completion, which is the check the others do not cover.** Its only trigger is a duplicate-detected add, so it needs a real library: opening a second source's copy of a library entry, tapping Add to library, then the duplicate card. `EntryMigrateHostViewModel` resolved through its keyed factory, loaded both sides of the pair, handed off to the dialog, and the migration was **committed rather than abandoned**. The details screen flipped from "Add to library" to "In library" on its own, which is the finish callback landing. That path is worth taking on purpose: the host's own KDoc warns that losing its state mid-migrate detaches a running commit from its dialog and it finishes with no feedback, and a ViewModel-lifecycle mistake is exactly what would cause that. Nothing short of committing tests it.
- **Phase 4, all sixteen models opened and exercised on a minified build**, zero crashes. `MangaViewModel` through upstream's factory, `NovelDetailsViewModel`, both cover models through the shared star-projected factory (which proves it builds the right subclass per content type, since the same call site produced the manga one on `MangaScreen` and the novel one on `NovelScreen`), notes, related-mangas "See all", the reader settings sheet across all three of its pages, and **all eight track models**. The date remover needed a date to exist, so one was set and then removed, leaving the Kitsu track as found.
- **The `Injekt.get<Application>()` swap was tested against a real context divergence, and the first attempt was invalid.** `prepareTabletUiContext` (`DisplayExtensions.kt`) only builds its `Configuration` override when the tablet-UI preference *disagrees* with the physical width, so under `Automatic` the Activity context and the Application context are always identical and nothing is being tested. Forcing the mismatch needs `Always` on a folded screen (or `Never` on an unfolded one). **It also needs an app restart**: `prepareTabletUiContext` runs in `attachBaseContext`, and the settings screen lives inside the same `MainActivity`, so changing the preference does not re-create the context the screens are already using. With the override provably live (a vertical nav rail on a 1080-wide screen, where `Automatic` gives a bottom bar), `MangaViewModel`'s `context.stringResource(MR.strings.merge_unified)` still rendered "Unified" on a merged entry. That string is the probe worth reusing: it is the one visible thing on the details screen that goes through the swapped context, and it only renders on a **merged** entry, so a single-source one proves nothing.
- **Verify both layouts, not just the one the device happens to be in.** Everything through phase 3 and most of phase 4 was driven on an unfolded Fold, which is two-pane tablet mode; `isTabletUi()` picks a different scaffold, so the single-pane layout was untested until the device was folded. Fold for at least one pass per surface.
- **A layout change voids every cached bound.** After a fold, a rotation or a tablet-UI change, re-dump before the next tap: coordinates from the previous layout land on whatever moved into that spot, which silently closes sheets rather than failing visibly.
- **Read selection and dialogs from text, never from a highlight.** Compose selection highlights produce no `uiautomator` nodes. The categories **Add FAB does appear** in the dump, but as a clickable node with empty `text` and `content-desc`, so it is findable only by its bounds; the migrate flow's Continue FAB does not appear at all. Treat "my tap did nothing" as dump blindness before a product bug.
