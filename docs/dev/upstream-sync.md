# Syncing with Mihon

Reikai is built on [Mihon](https://github.com/mihonapp/mihon) but is a standalone repository, not a GitHub fork, so there is no upstream remote to pull from. Upstream changes are ported **by hand** from the local `refs/mihon/` reference clone. This doc is the process, the commit convention, and the running ledger of where Reikai sits relative to upstream.

## Why by hand

`refs/mihon/` is a read-only reference clone, not a remote, so there is nothing to pull. And Reikai has diverged: `// RK` islands patch Mihon's own files, large areas are re-typed onto Reikai's models (Compose/Voyager, immutable domain), and the novel and adult code is net-new. Each upstream change is re-applied by hand and re-targeted to Reikai's shape. Model the cost as a per-change re-targeting cost.

## How to sync

1. **Find the new commits:** `git -C refs/mihon log --oneline <last-synced>..HEAD` (the last-synced SHA is the top ledger row below). Pull `refs/mihon` first; the clones live in the parent dir `refs/`, not inside `app/`.
2. **Check off-path files:** `pwsh scripts/off-path-check.ps1 -MihonBase <last-synced>`. It fails if the range touched any file in the [off-path manifest](off-path-manifest.md) (a deleted Mihon UI file whose twin Reikai renders); reconcile each flagged change into its replacement twin by hand. The diff is upstream-side, so a reported path stays reported however well you reconciled it: once you have walked each one, re-run with `-Reconciled` to write the stamp the `commit-msg` hook wants. A pass that stops below upstream HEAD, because a commit in the middle is deferred, adds `-MihonThrough <sha>` to bound the range at the point it reaches; the stamp then records that SHA and every commit in the pass has to cite it.
3. **Port each commit** by the [method](#porting-method): verbatim-copy marker-free files, hand-merge `// RK` files.
   - **A Weblate import is a scripted per-key three-way merge**, never a wholesale copy: take upstream's value for a key only where Reikai's base still declares it and Reikai's own value has not diverged from the pre-sync one, so the debrand and the deliberate string divergences survive. Drop the keys upstream removed that Reikai's base no longer declares either; they are dead weight left by an earlier port. Re-parse every touched file as XML and re-check that no incoming value reintroduced upstream's branding.
   - **A locale upstream gains is a locale Reikai takes, empty or not.** Translations come from upstream and there is no capacity here to write them, so a locale is never skipped. An empty file was skipped twice on the belief that it would list the language in the system picker with nothing behind it; measured on this tree, that does not happen. moko-resources generates no `values-<lang>` folder for a file with no `<string>` entries, and the generated `locales_config.xml` lists only the locales that produced one, so an empty locale is inert and reaches no picker. Take every locale on the sync that adds it, and let it fill in later.
4. **Drift-check** the hand-merges.
5. **Compile** (`:app:compileReleaseKotlin`) and on-device verify anything user-facing.
6. **Commit** with the [convention](#commit-convention).
7. **Append a ledger row** recording the new base and what was ported or skipped.

Port every new commit by default. Only skip one for a concrete, defensible reason (it re-implements something Reikai deliberately rewrote and would contradict live behaviour, or it is N/A like a Mihon version-code bump). Surface a skip as a choice, never decide it silently.

## Commit convention

Reference upstream PRs/issues as **`mihonapp/mihon#<n>`** (a cross-repo link). A bare `#<n>` auto-links to a *Reikai* issue and is rejected by the docs lint. Cite the upstream short-SHA too, and use `chore:`, never `feat`/`fix`.

- **Subject:** `chore: sync Mihon <what> (mihon <sha>, mihonapp/mihon#<n>)` for one commit; `chore: sync Mihon <theme>` for a batch.
- **Body:** lead with what changed and why it matters, one bullet per ported commit, note any skip with its reason. Keep it concise; the deep porting mechanics live in this ledger, not the commit.
- No em dashes, no AI watermarks (see [code-quality.md](../../.claude/rules/code-quality.md)).

## Porting method

- **Marker-free file (no `// RK`):** copy the upstream post-commit blob verbatim. Confirm Reikai sits at Mihon's pre-commit base first (`diff` the file); a clean match means the copy is safe.
- **`// RK`-patched file:** re-apply the upstream hunks by hand around the RK islands. Never let an upstream change land inside or clobber an island.
- **Drift-check:** `diff` each hand-merged file against the upstream post-commit blob. A faithful port leaves only RK-attributable hunks (an RK island, an RK-supporting import, an RK-fenced line). Any other hunk is drift, a dropped or mis-applied change.
- **Collapsing a Mihon file onto a Reikai twin: delete pure-UI, keep engine + partial.** When a Mihon file's UI is *fully* replaced by a Reikai-owned twin (`reikai.*`), **delete** the Mihon original and record it in the [off-path manifest](off-path-manifest.md), rather than keeping it `// RK: inert`. A solo+AI workflow cannot afford a dead file an edit might silently land in; the `refs/mihon` clone holds the pre-delete blob as the diff base, and the manifest's sync check (`off-path-check.ps1`, step 2 of [How to sync](#how-to-sync)) fails loudly if upstream later changes the file, so a buried change is impossible. This supersedes the old keep-inert rule for pure-UI files (the `// RK: inert` tombstones `MangaCoverDialog`, `TrackInfoDialog`, `GlobalSearchCardRow` were deleted and manifested). Two things still stay live, never deleted: an **engine** file (a ViewModel, repository, source manager) is minimally patched on the render path (e.g. `DownloadQueueViewModel`, awaiting Road B); and a **partially collapsed** file keeps its live remainder in place, marked `// RK` with what moved out, so it is not manifested until nothing live remains (as `MangaInfoHeader` was once its last live piece, the expandable description, became `ExpandableEntryDescription`).
- **Review for it:** the manifest is the enforcement, but a *new* reroute that skips it is invisible. Whenever a sync or a feature reroutes a Mihon file to a `reikai.*` twin, add it to the manifest (or the next sync silently misses upstream's change to it). Periodically audit for un-manifested reroutes with `git log -M20% --find-copies-harder --name-status` over the whole source tree: `git` records a move as a rename only above its 50 percent similarity default, so a heavily rewritten twin reads as an unrelated delete plus add, and a pathspec limited to `eu/` cannot pair a rename landing in `reikai/` at all.

  A cheaper audit answers the same question without relying on rename detection: list every file in the
  `refs/mihon` tree, subtract the ones that exist here, and subtract the manifest. Anything left is a reroute
  nobody declared. **Audited that way 2026-08-14 and it came back empty:** of Mihon's Kotlin and SQLDelight
  files at `77e88a215`, every one either exists in this tree or has a manifest row, and the only upstream
  resources missing are Mihon's own launcher icon, logo and splash, which Reikai replaces by identity. The
  same audit over the 456 files upstream changed since `b3e190c62` found 64 absent here, all explained: a
  manifest row, a file upstream deleted too, a rename from the ViewModel migration, the `source-api` /
  `source-local` move out of `commonMain`. (The `az` locale was on that list until it was taken, see the top
  ledger row.) Nothing upstream-live was quietly dropped, and no sync commit justifies a skip on the grounds
  that Reikai no longer has the file.

- **Reconciliation is a separate question from the manifest, and it needs its own pass.** The manifest proves
  a change was *reported*; only reading the twin proves it was *carried*. Ten manifested paths have been
  changed upstream since the deletion that manifested them, and all ten were re-verified on 2026-08-14 by
  comparing the upstream hunks against the twin: the four `java.time` conversions (`EntryDetailsColumn`,
  `EntryActionRow`'s `daysUntil`, `LibraryQueryMatch`, and all nine date lines in `EntryTrackInfoDialog`),
  the three `collectAsStateWithLifecycle` moves (`EntryMigrationFavoritesScreen`, `ReikaiMigrateSourceTab`,
  `ReikaiSourcesTab`, the last two carrying it for the novel model too), `updateAllOrders` in both
  `CategoryActions` and `DeleteCategoryCleanup`, and the cover dialog's dropped issue-tracker comment.
  Two of those hunks sat one file over from the row that named them, which is why a Replacement is an entry
  point rather than an address; the manifest states that convention.

## Recurring gotchas

- **The EXH-override tax:** changing a Reikai-`open`ed `source-api` method breaks Reikai's two EXH overrides (`exh/source/DelegatedHttpSource.kt`, `EnhancedHttpSource.kt`, absent from Mihon). Re-type both overrides too.
- **Migration numbers diverge:** never copy Mihon's `.sqm` number; Reikai's sequence is ahead. Add a new Reikai-numbered `.sqm` with the same schema change.
- **Root `spotlessApply` is safe again.** It used to reformat ~56 unrelated files, so the rule was to format only the files a sync touched. As of 2026-07-27 the whole repo is clean, so a root `spotlessApply` reports nothing beyond your own changes. Still check `git status` after running it: CI runs root `spotlessCheck`, so any file it does touch is a real block, but it should not be one you did not edit.
- **Version and release commits are N/A:** Reikai keeps its `.y2k` identity, so skip Mihon's version-code bumps and release-artifact commits.
- **CI-action bumps go to Reikai's own workflows:** Reikai owns `build_check.yml` / `nightly.yml` / `release.yml`; apply an action bump there, not to a Mihon `build.yml` Reikai lacks.
- **Coupled bumps land together:** a major dependency bump and its deprecation-fix commit must ride one commit (e.g. xmlutil 1.0.0 with its `XML.v1` migration), or the first commit will not compile.

## Deliberate divergences from upstream

Cases where Reikai knowingly does not match `refs/mihon`, so a future syncer does not "fix" them back. Revisit each when upstream settles.

- **Backup create / validate / restore are streamed, not whole-file.** `BackupCreator`, `BackupFileValidator`, and `BackupRestorer` diverge from Mihon's `encodeToByteArray(Backup.serializer(), backup)` / `decode()` (which build the whole `Backup` in memory and OOM on a large library, unseensnick/Reikai#53). Reikai writes each top-level protobuf field to the gzip sink and reads it back one field at a time (`BackupProtoWriter` / `BackupProtoReader`), so a future sync of these three files is a hand-merge inside the `// RK` islands, never a verbatim copy. The wire format is unchanged (same field numbers), so backups stay cross-compatible with upstream.
- **The shared `categories` table carries a `content_type` discriminator and `CategoryRepository` is content-type-aware.** Reikai folded its novel category stack into Mihon's shared `categories` table with a `content_type` column (0 universal / 1 manga / 2 novel), so `categories.sq` (the column, the seeded universal row 0, `getNovelCategories` / `getNovelCategoriesByNovelId`, the sort-override-clear), the `CategoryRepository` interface + `CategoryRepositoryImpl` (a `contentType` param on `getAll` / `getAllAsFlow` / `insert`, plus `getCategoriesByNovelId`), and `DeleteCategory` (delegates to Reikai's `deleteCategoryAndCleanup`) all diverge inside `// RK` / `-- RK` islands. A category-id preference cleanup rides on top: `PreferenceRestorer` and `BackupRestorer` remap category-id prefs by name on restore and skip a dead key, and a `188f` migration scrubs them on upgrade. A sync of any of these is a hand-merge inside the islands, never a verbatim copy; read [category-schema-unification.md](plans/category-schema-unification.md) first. The two junction tables (`mangas_categories`, `novels_categories`) are unchanged, so entry storage syncs normally.

- **`action_view_upcoming` reads "Upcoming Updates", where upstream says "View Upcoming Updates".** Reikai's only caller is the Recents toolbar, where the label sits in an overflow menu on phones and the verb was redundant next to the other entries. The key is unchanged so existing translations still attach; a Weblate merge that carries upstream's English back over is the thing to watch, since the scripted three-way merge would take it silently.

- **Reikai keeps a CI `gradle.properties`, which upstream has deleted.** Mihon's workflows now run off the repo's own `gradle.properties` with no copy step; Reikai still copies `.github/runner-files/ci-gradle.properties` over the Gradle home in all three workflows. Two settings are the reason to keep it: `-Xmx5120m` where the repo asks for `-Xmx4g` (Reikai carries more modules), and `kotlin.incremental=false`, which is right for a clean CI checkout. The `org.gradle.workers.max=2` cap that used to sit beside them is gone, so `org.gradle.parallel=true` can use the whole runner. Revisit whether the file is still earning its keep the next time a sync touches CI.

- **`DownloadCache` persists the time it last scanned, where upstream restarts the clock on every launch.** Upstream's restore sets `lastRenew = System.currentTimeMillis()`, so an app opened more often than `renewInterval` (one hour) never re-scans: each launch pushes the next scan a full interval out. That defeats what the class KDoc says it is for, noticing folders the user deleted behind the app's back, and it leaves a download badge wrong indefinitely. Reikai stores the scan time in the index (`RootDirectory.lastRenew`, the last proto field so an older index simply reads as never-scanned) and a restore takes that value. **Stamp the tree at scan completion, never at write time**: the restore runs in its own coroutine, so a write racing it persists a zero and makes the next launch re-scan for nothing. Both edits are `// RK` islands; a sync of this file is a hand-merge.

- **The Updates category filter is one selection covering both content types, where upstream declares two preference keys and a second filter tab** (mihonapp/mihon#3589, mihon `1d8a2b05d`). The query half is ported: the include and exclude blocks are verbatim in `updatesView.sq`, and the interactor and repository carry the id lists. The preference and picker halves are deliberately not taken, because Reikai's selection lives in the shared category-id registry as a `Set<String>` and is edited through its own picker. Both tabs read it through `recentsCategoryFilterFlow`, and both content types wire it, so the feature is delivered; do not port upstream's `getLongArray` keys or its second tab on top. Closed 2026-08-21 after re-checking the wiring; it sat in Pending until then because nothing re-read the blocker once the recents surface shipped.

- **Both History feeds seed `null` where upstream seeds an empty list.** `HistoryViewModel` and `NovelHistoryViewModel` share their feed with `WhileSubscribed`, and the seed is the value the screen sees before the query answers. Upstream uses `emptyList()` while leaving `State.list` nullable, so its own loading branch stopped firing; Reikai seeds `null`, which is what the shared recents screen and its read lane both read as "not loaded yet". Keep the null when syncing either file, or an empty history flashes on screen a tick before the rows arrive.

_Previously:_

- The **`preview` build type keeping its name** where upstream renamed its own to `nightly` (mihonapp/mihon#3760, mihon `b393088cf`) was reconciled on 2026-08-21: Reikai renamed too, so the build type, the source set (`src/nightly/res`), `isNightlyBuildType` and the Gradle tasks all match upstream, and `BuildConfig.kt` is byte-identical again. The old entry argued the name was load-bearing because the variant installs as `eu.kanade.tachiyomi.debug`; that is `applicationIdSuffix` doing the work, not the build type's name, so the install path was never at risk. **Three things deliberately kept their old names**, all outside this tree or already published: the `unseensnick/Reikai-preview` release bucket and its `PREVIEW_REPO_TOKEN` secret (renaming the repo would strand every installed build already polling it), and the About screen's `Beta r<N>` label, which is a Reikai wording choice rather than a build-type reference.
- The **Hikka tracker response leaks** (`de027cbf1`; `// RK` in `HikkaInterceptor.kt` + `HikkaApi.kt`) were reconciled on 2026-07-09. Upstream `mihonapp/mihon#3548` (mihon `ab58a9952`) now closes the refresh-token response on the success path and rewrites `getRead` to a `try`/`awaitSuccess` form that closes on the 404 path too, so Reikai dropped both `// RK` leak patches and took upstream. The only `// RK` code left in the Hikka tracker is the Fill-from-tracker `getMangaMetadata` (a Reikai feature Mihon lacks). See the ledger.
- The notes background-crash divergence over `mihonapp/mihon#3515` was reconciled on 2026-07-05 (Mihon shipped `mihonapp/mihon#3523`, a model-level fix that makes `Manga` serializable, so Reikai adopted it and reverted its serializable-args notes screen back to upstream). See the ledger.

## Deferred upstream changes

Commits deliberately not yet ported, to be picked up later. A syncer hitting one should skip it (per [How to sync](#how-to-sync)) and port later commits around it when they are independent.

- **The ScreenModel to AndroidX ViewModel migration** (mihonapp/mihon#3594, mihon `c3b99aea0`) is **ported, with one deliberate holdout** (full record: [viewmodel-migration.md](plans/viewmodel-migration.md)). It was an architectural rewrite rather than a routine sync: 91 upstream files, roughly 29 model renames, adding a `:core:viewmodel` module, and on Reikai's side 74 model classes across 72 call sites, 41 of the models Reikai-owned and untouched by upstream's diff. **Every Mihon file is now on `ViewModel`**, so a screen-file sync applies against a matching base. The holdout is Reikai's own novel reader, left on `ScreenModel` because the tsundoku reader migration deletes it; `voyager-screenModel` therefore stays in the catalog and the bundle, diverging from upstream on those two lines until that lands. No Mihon file depends on it.
  - **Correction to the old consequence note.** It said the synced base stays at `ef1c52967`, the commit before the migration. It did not: later commits were ported around `c3b99aea0` and the base advanced past it, so the base is `45b1e781e` and the migration commit is already an ancestor of it. Reikai is carrying an unported commit below its own base, which is why every screen-file sync currently fights a mismatched shape.
  - **Sequencing.** The migration lands first, then this backlog is ported in one pass onto a matching base, then the content layer's history and updates redo. Four unported commits above the base touch files the migration renames and are cheaper afterwards: `a73271aef` (upcoming), `55be95dd5` (webview), `1d8a2b05d` (the Updates category filter moving into SQL), `6d69903a5` (four renamed files).
  - **The crash fix rides in with it** as `mihonapp/mihon#3609` (mihon `98705910e`). It simply drops the `private` modifier on the migrated model classes (`-private class MoreViewModel(` -> `class MoreViewModel(`) across `CreateBackupScreen`, `RestoreBackupScreen`, `ClearDatabaseScreen`, `WorkerInfoScreen`, `MoreTab`, `MigrationConfigScreen`, `MigrateMangaDialog`, `MangaNotesScreen` and `TrackInfoDialog`. Take it wholesale in phase 1, so the three models Reikai would otherwise resolve bare and private never crash here. **Its `TrackInfoDialog` half is now carried** by the twin `EntryTrackInfoDialog.kt`, whose eight models dropped `private` when they joined the Metro graph, since a graph-contributed factory cannot be private either.
  - **Why the fix is needed:** a model resolved by a bare `viewModel<T>()` must not be `private`. The default factory builds it reflectively from `androidx.lifecycle`, and a private top-level Kotlin class is package-private in bytecode, so `newInstance()` throws `IllegalAccessException` and the screen crashes on open, on every build type (the modifier comes from kotlinc, so this is not an R8 trap and a debug build will not hide it).

## Pending, needs planning

Unported commits sitting above the current base that are **not** routine bumps: each needs a hand-merge or a decision before it can land, so it is parked here until a sync takes it. Different from [Deferred upstream changes](#deferred-upstream-changes) above, which are deliberate refusals with a resume trigger; these are simply not done yet. Dependency and tooling bumps are not listed, a normal sweep picks those up. Delete a row once it is ported and recorded in the ledger.


- **Reikai carries part of an unmerged upstream PR** (mihonapp/mihon#3858, open). Reikai took that PR's solver script verbatim at `e6de3a7a1` and has since **deleted** it (`5119f7c88`), because the no-window path presses keys instead. The solver has also been stripped out of the PR itself, so neither side carries the asset and a merge will not reintroduce it; if a future revision does, it is not wanted. That PR now touches four files; `ForegroundActivity.kt` arrives byte-identical to ours and `App.kt` adds the registration our `// RK` island already holds, so both reconcile trivially. The two that need hands are the `CloudflareInterceptor` island and `WebViewInterceptor`. Reikai has **already taken that PR's `WebViewInterceptor` rewrite** (the nonce parameter, the `Response?` return, `getNonce` / `isBypassed` and the per-host read/write lock), so those two files should merge close to cleanly rather than needing a redesign; the divergence left is our solve-acceptance logic sitting inside them. That rewrite landed on an unmeasured benefit and carries a removal recipe in [turnstile-solver.md](plans/turnstile-solver.md), so if the PR is abandoned it is a candidate for deletion rather than something to preserve. **Fold ours in, but keep our solve-acceptance test:** its `isBypassed` treats a changed `cf_clearance` as proof of a solve, and Cloudflare issues one on a challenge it has refused, measured as a 403 on every retry in a backgrounded run. Reikai does use that rule at one seam, the sibling shortcut in `WebViewInterceptor.intercept`, and it is safe there only because a failed solve now really deletes the clearance first; before `AndroidCookieJar.remove` expired the parent domains it did not, and the sibling trusted a refused round. Full record: [turnstile-solver.md](plans/turnstile-solver.md).

- **The collect-only-while-subscribed campaign: the download-queue half** (mihonapp/mihon#3727, mihon `c68efd0e8`). Twelve of the thirteen commits are ported (see the ledger); this one is not. Mihon's `DownloadQueueViewModel` is the manifest's one sanctioned dead file, so converting it changes nothing a user can reach, and the live replacements are the wrong shape to take it: `MangaDownloadQueueViewModel` keeps a per-series `initialTotals` accumulator and a transient-empty debounce that reads its own `state.value`, neither of which is a derivation. That is the same reason upstream's own campaign **deliberately skips `UpdatesViewModel` and `LibraryViewModel`**, the two models that patch one row's download state in place. Do not convert any of them without first answering what live download progress becomes, which is the download-subsystem unification's (Road B) question, not a sync's.
<details>
<summary>Ported: java.time to kotlinx.datetime (2026-08-08), kept for the method</summary>

- **java.time to kotlinx.datetime** (mihonapp/mihon#3001, mihon `6d69903a5`), with its two dependents: the relative-date-display fix (mihonapp/mihon#3696, mihon `3d20bcc6b`) rewrites a file the conversion produced, and the MangaBaka negative-timezone fix (mihonapp/mihon#3711, mihon `f9156aa17`) is written against `kotlinx.datetime`. All three landed on 2026-08-08; the map below was measured beforehand, against the tree at `10bee7ab5`.

  **What actually changes.** Two type families move at once: `java.time.Instant` becomes `kotlin.time.Instant` (the stdlib type, not the new library), and `LocalDate` / `LocalDateTime` / `ZoneId` become their `kotlinx.datetime` equivalents. The pivot is `DateExtensions.kt`, where `Long.toLocalDate()` changes its return type and a new `toJavaLocalDate()` appears for callers that still need the java type. Because that helper is shared, callers convert whether or not upstream touched them, which is what pulls Reikai's own files in. **The tree does not type-check again until nearly all of it is done**, so treat it as one atomic edit-then-compile rather than something to land in halves.

  **The buckets** (49 Kotlin files upstream, plus the catalog, four `build.gradle.kts` files and `build-logic/Commands.kt`):

  - **25 can be taken verbatim.** They are byte-identical to upstream at `6d69903a5^`, so upstream's HEAD blob drops straight in: `Manga.kt`, `FetchInterval.kt` and its test, `GetUpdates.kt`, `DateExtensions.kt`, `TimeUtils.kt`, `DateText.kt`, `SyncChaptersWithSource`, `UpdateManga`, `AddTracks`, `MangaDialogs`, `WorkerInfoScreen`, `TrackInfoDialogHome`, `TrackerSearchPreviewProvider`, `ImageSaver`, `ALFuzzyDate`, `UpdateMangaFromRemote`, `UpcomingUIModel`, the three calendar files, `Requests.kt`, `Commands.kt` and the two widget files.
  - **18 need a hand-merge**, because Reikai diverges or because the 2026-08-08 sync already moved them: `ReaderViewModel` (45 `// RK` islands), `BrowseSourceViewModel` (19), `AnilistApi` (14), `MigrateMangaUseCase` (13), `BackupCreator` (12), `UpdatesViewModel` (12), `LibraryUpdateJob` (8), `NetworkHelper` (7), `SettingsAppearanceScreen` (6), `MangaRestorer` (5), plus `HistoryScreen`, `AboutScreen`, `CrashLogUtil`, `MangaRepositoryImpl`, `BackupNotifier`, `MangaExtensions`, `UpcomingScreenContent` and `UpcomingViewModel`.
  - **6 are manifested twins** and get reconciled by hand into their replacements: `MangaScreen`, `MangaInfoHeader`, `TrackInfoDialog`, `QueryNodeExtensions`, `HistoryviewModelStateProvider` and `UpdatesScreen`.
  - **9 Reikai-owned files are forced along** by the type change, novel and shared alike: `EntryDetailsColumn`, `EntryActionRow`, `EntryTrackInfoDialog`, `LibraryQueryMatch`, `MangaLibraryAdder`, `NovelDateParser`, `UpdateNovel`, `NovelUpdatesViewModel` and `MigrateNovelUseCase`. Most are a single `java.time.Instant` import; `LibraryQueryMatch` and `EntryTrackInfoDialog` also carry `LocalDate` and a zone type.

  **What the map got wrong, and the lesson.** The 49 upstream files re-derived exactly, but the Reikai-side list above was wrong in both directions, because it was built from "imports `java.time`" rather than from "consumes a signature that changed". Six of the nine do not break at all (`NovelDateParser`, `UpdateNovel`, `MigrateNovelUseCase`, `MangaLibraryAdder`, `NovelUpdatesViewModel`, and `LibraryQueryMatch` on its own terms): they reach `java.time` only to get epoch millis, and java.time still exists. Three that do break were missing: `ChapterListDialog` calls `toRelativeString` on a converted receiver, and `ReikaiHistoryScreen` and `ReikaiUpdatesScreen` each declare a `Header(val date: LocalDate)` fed by `toLocalDate()`. Two more surfaced only at the compiler, `UnifiedUpdatesGlanceWidget` and `UnifiedUpdatesWidgetManager`, which call `toEpochMilli()` on a `DateLimit` that a verbatim file re-typed. **The forcing set is the transitive closure of five changed signatures**, not a grep for the package: `Long.toLocalDate()`'s return type, the deleted `Instant.toLocalDate(zone)` overload, `convertEpochMillisZone`'s parameters, `Manga.expectedNextUpdate`'s type, and `FetchInterval.getWindow` / `toMangaUpdate`. Map a conversion that way and the first compile has a handful of errors instead of a hundred.

  **What was converted beyond what broke.** The six non-breaking novel files were converted anyway, because upstream converts exactly that `Instant.now().toEpochMilli()` idiom in their manga twins, and leaving the novel side on java.time is the silent divergence [content-layer.md](../../.claude/rules/content-layer.md) exists to prevent. `NovelDateParser` and the EXH source parsers were left alone: they have no manga twin, so there is no divergence to close.

  **Traps.** `GetUpdates.kt` and `UpdatesViewModel.kt` are also touched by the deferred Updates-filter commit, so take the datetime hunks only and leave the category-filter ones for the redo. The six twins are where a dropped behaviour hides, because nothing diffs them for you. `Commands.kt` lives in `build-logic`, so a mistake there breaks configuration rather than compilation, and its build-time string format changes. Reikai's `MangaBakaApi` already carries a debranded User-Agent, so `f9156aa17` is a hand-merge there too.

  **Re-derive the buckets** by comparing each path in `git -C ../refs/mihon show --name-only --format='' 6d69903a5` against `git -C ../refs/mihon show "6d69903a5^:<path>"`: identical means verbatim, different means hand-merge, absent means check the [off-path manifest](off-path-manifest.md).

</details>

## Synced-base ledger

Newest first. "Base" is the `refs/mihon` SHA Reikai is synced through; "Reikai" is the sync commit. For syncs older than the table, run `git log --oneline --grep="sync Mihon" -i`.

| Base (mihon) | Reikai | Date | Ported | Skipped / N/A |
|---|---|---|---|---|
| `aebf11a74` | `8e078e05e` | 2026-09-08 | **One commit, the whole range, and the base advances to upstream HEAD.** **Package-internal tracker utils** (mihonapp/mihon#3908, mihon `aebf11a74`, 11 files): upstream marks the per-tracker helper functions `internal` so their names stop colliding, and moves `MURecord.toTrackSearch`, `MUListItem.copyTo` and `MURating.copyTo` from top-level extensions to members of the class they extended. Kotlin `internal` is module-scoped rather than package-scoped, so the narrowing is inert for callers inside `:app`, and a grep of the whole tree found no caller of any affected symbol outside the tracker package. Nine of the eleven files were byte-identical to the base and were copied verbatim; the two carrying markers were hand-merged. `MURecord` is the only one where the two changes interact: Reikai's `authors` is nullable with a sibling `genres` field, and its body calls the RK `namesOfType` helper rather than upstream's inline `filter`/`map`, so the RK island moved inside the new member body. `MangaUpdates` drops the two imports the move made unnecessary, and keeps the `namesOfType` import, which upstream has no equivalent of. Reikai's own `novellist`, `novelupdates` and `ranobedb` trackers were deliberately left alone: upstream does not have them, so nothing about this commit reaches them, and giving them the same treatment would be uninvited cleanup. | None. |
| `3a64c8d65` | `23ed09b5e`..`6a4f226f6` | 2026-09-07 | **The whole 5-commit queue lands and the base advances to upstream HEAD.** **Tracker cleanup** (mihonapp/mihon#3900, mihon `5e99306e5`, 38 files): the eleven upstream tracker Api classes take their tracker id as a constructor parameter named `trackerId` rather than reading a `TrackerManager` constant, token expiry math moves to `kotlin.time`, the interceptors parse refresh responses through `parseAs`, unused OAuth fields leave the DTOs, and unused caught exceptions are marked `_`. Sixteen marker-free files went in verbatim; the other twenty-two were hand-merged, four of them only to re-apply Reikai's debranded User-Agent. Dropping `token_type` and `user_id` from `BGMOAuth` is safe because the graph's `Json` sets `ignoreUnknownKeys` (`AppBindings.kt:83`), so a stored Bangumi token still decodes and nobody is signed out. Reikai's own four tracker id constants stay in `TrackerManager`, since no upstream file inlines them. **Kitsu rating-system crash** (mihonapp/mihon#3887, mihon `a82baac3e`): adopted, and it **retires both Reikai Kitsu casing islands**, the read-side `.lowercase()` comment and the login-side store, so `getCurrentRatingSystem` and `login` match upstream again (the preference has exactly one reader, `Kitsu.kt:76`, and its default is already lower case). **Bumps:** okio 3.18.2 (mihonapp/mihon#3894), conscrypt 2.7.0 (mihonapp/mihon#3897). **Drift closed in passing:** `Komga.client` was eager here and `by lazy` upstream, from the rebase seed (`b8bea6755`) rather than a decision; taken as upstream has it. Verified with `:app:compileDebugKotlin`, `spotlessApply`, `di-interop-check` (30 graph-owned types) and 1458 app tests, 0 failures. Completeness checked two ways: every one of the 38 files diverges less from upstream after the port than before, and no line upstream deleted survives in our tree. | **`5b69dbad8`** backup category restore race: Reikai already fixed it, and ours covers the novel stream too (`BackupRestorer.kt:141`); only the stale half of that comment was corrected. `CHANGELOG.md` (Reikai owns its own) |
| `3acd7eaca` | `e25e98b9c`..`20f8aa8a6` | 2026-09-04 | **The pass closes: the last 4 of the 10-commit queue land and the base advances from `c1d1f6b16` to upstream HEAD.** The two rows below took 6 of them newest-first, which is why the base could not move until now. **Suspending source lookups** (mihonapp/mihon#3869, mihon `5fadea61e`, 86 files): the five `SourceManager` accessors become `suspend` and `isInitialized` leaves the interface, so a lookup awaits the extension scan instead of reading a half-built map. `AndroidSourceManager` keeps all 16 RK islands and its drift check leaves only those; `ExtensionManager` swaps its ready flag for a `CompletableDeferred` completed even when the scan throws, keeping the EH-gate combine. `ifSourcesLoaded` and its **six** call sites go (the plan said five; `EntryGlobalSearchScreen` was missed). **The novel twin followed, per the owner's ruling, and that is what made it cheap:** once `NovelSourceManager.get`/`getAll` suspended too, both sides failed at the same three shared interfaces (`FeedProvider`, `LibraryProvider.dynamicGroupingFeed`, `MigrationFlowAdapter`), so each changed once rather than twice. **Three lookups stay blocking** at boundaries that cannot suspend, matching upstream's own choice for Kavita and Suwayomi: the MDList tracker source, the MangaDex track-cover fetcher, and the MangaDex settings category's `isEnabled`. **The auto-webtoon classifier reads names resolved once in `init`** rather than per app-bar recomposition. **A test caught a real regression:** moving the migration override picker's strip seeding inside the search coroutine reproduced the bug its own comment records, where the picker span forever; it now seeds synchronously from a cached source list. **Unused imports** (mihonapp/mihon#3876, mihon `baad5c59b`): 12 of 24 apply, two kept deliberately (`WorkerInfoScreen`'s Injekt `Context` locator, `ReaderViewModel`'s Edit-info await), and the webgpu one reconciled into `EntryCoverDialog`. Driven by removing every candidate and compiling, after a first grep-based pass returned a false all-clear: Windows carriage returns in the extracted patch defeated `grep -F`. **Material Symbols** (mihonapp/mihon#3873 + mihon `34a33ed4f`, 122 files): `material-icons-extended` is gone, replaced by `:icons:material-symbols` and `:icons:simple-icons`, 119 committed SVGs that Valkyrie turns into ImageVectors at build time (nothing is fetched beyond the plugin). 108 glyphs mapped one-to-one off upstream's own imports, six took upstream's substitutions, and 17 more were substituted where the shipped set has a real analogue. **Eleven have no counterpart**, because Mihon has none of the surfaces using them, and substituting any would make a control stop saying what it does: they live in `reikai/presentation/icons/ReikaiIcons.kt` with path data and both builder helpers **copied verbatim from androidx material-icons (Apache 2.0), Rounded variants**, so nothing was redrawn by hand. Off-path checked and stamped through `34a33ed4f`; 36 reported paths across the three commits reconciled by hand. Gates on each: `di-interop-check` (30 graph-owned types), `:app:compileDebugKotlin`, `spotlessApply`, `:domain:test` and `:app:testDebugUnitTest`, 1386 app and 75 domain, 0 failures. **Nothing in this pass is device-verified**, and the icon commit especially wants a look, since it changes how every screen is drawn. | Nothing skipped; the queue is empty. |
| `c1d1f6b16` (unchanged) | `91217c88b` | 2026-09-04 | **4 more ported of the same 10-commit queue, still newest-first, so the base still does not advance.** These four were taken next because none of them touches a manifested path and none needs a design decision. **AniList expiry** (mihonapp/mihon#3888, mihon `21af65b10`): the stored expiry was multiplied by a thousand before every comparison, so a token read as valid until the year 58646 and the app kept sending credentials AniList had already rejected, surfacing as a generic 401 rather than "your sign-in expired". `ALOAuth` gains its own `isExpired` and a defaulted expiry, `createOAuth` goes, and the interceptor throws a message naming Settings. `AnilistInterceptor` looked marker-free but is not: it carries the debranded `Reikai` user agent as an unmarked identity divergence, so it was hand-merged rather than copied, and the drift check leaves only that line. `Anilist` and `AnilistApi` are RK-patched (5 and 17 markers) and took one-line changes each. Reikai's own `parseALError` island, which already raised a friendlier message from the GraphQL error body, is untouched and now sits behind a check that fires before the request. **Extension class loading** (mihonapp/mihon#3874, mihon `7f342ca07`): `ChildFirstPathClassLoader` is replaced by the platform's `DelegateLastClassLoader` behind `DelegateLastClassLoaderCompat`. Nothing else in this tree referenced the old class and upstream deletes it too, so it is deleted with no manifest row owed. **composeGrid group id** (mihonapp/mihon#3877, mihon `056d9baee`), `io.woong` to `com.cheonjaeung`, 1.2.2 to 2.8.0, with `Calendar` its only importer here. **coil 3.6.0 to 3.6.2** (mihonapp/mihon#3889, mihon `3acd7eaca`). Off-path checked per commit against each one's own parent, all four clean. **The stamp therefore names upstream HEAD while three commits below it are still unwalked**, which the hook cannot express; the honest statement is in this row. No CHANGELOG entry for the three bumps, one `Fixes` entry for the AniList expiry. Gates: `:app:compileDebugKotlin`, `:app:spotlessApply` with no unrelated reformats, `:domain:test` and `:app:testDebugUnitTest`, 1386 app and 75 domain, 0 failures. **Not device-verified:** exercising the AniList path needs an expired token. | Nothing skipped. `5fadea61e`, `b69594011`, `34a33ed4f` and `baad5c59b` remain. `34a33ed4f` is blocked behind `b69594011`, since it edits an SVG in the icons module that commit creates. **`baad5c59b` is not, and this row said otherwise when it was written.** Measured afterwards: none of its 24 removed imports is an icon import (14 are `androidx.lifecycle.viewmodel.compose.viewModel`, orphaned by our own Metro migration), so it is independent of the icon work. Its only ordering constraint is the three files it shares with `5fadea61e` (`DownloadCache.kt`, `ReaderActivity.kt`, `ReaderViewModel.kt`), where the `kotlinx.coroutines.flow.first` import orphans only once that commit removes the `isInitialized.first { it }` gates. Three of its removals must be skipped regardless, because our copies still use the symbol. `5fadea61e` and `b69594011` are both large enough to need their own approval. |
| `c1d1f6b16` (unchanged) | `df25df87c`..`76aa6ab15` | 2026-09-04 | **2 ported of 10, taken newest-first out of the queue, so the base does not advance.** Both are webgpuviewer bumps, and they were pulled forward because they are a dependent pair confined to a package this fork has never patched (all three `viewer/webgpu/` files carry zero markers) and because they answer a live complaint. **Renderer navigation and long-strip bounds** (mihonapp/mihon#3870, mihon `bb24e904a`, webgpuviewer 38 to 39): the viewer keeps its place when you jump pages, navigation directions are right, long strip stops overrunning its bounds. `ReadingModePage`, `WebGpuViewer` and `WebGpuViewerContinuous` all matched the pre-commit blob and went in verbatim; `ReaderPreferences` was hand-merged for the additive `ImageScaleTypeWebGpuViewer` list. **Renderer settings and min width** (mihonapp/mihon#3886, mihon `f3125ab89`, webgpuviewer 39 to 40): the in-reader settings dispatch becomes a three-way `when (viewer)` so a WebGPU viewer no longer falls through to the pager block, `WebGpuViewerSettings` becomes self-contained, dual page gains its own transition animation and cutout mode, and long strip gains a `continuousMinWidth` slider. Four files verbatim, `ReaderPreferences` and the base string table hand-merged. Drift checks on `ReaderPreferences` leave only its two RK islands in both commits. **This is the fix for the side padding complaint, and it is a new preference rather than a repair.** Verified before porting: at the old base, `ReadingModePage` gated the webtoon settings block on `viewer is WebtoonViewer`, and with the high quality renderer on a WEBTOON reading mode builds `WebGpuViewerContinuous`, which extends `WebGpuViewer`, so the block holding the side-padding slider never composed. It was inoperative as well as hidden, since `WebGpuConfig` registers only the pager preference family and never read `webtoonSidePadding`. Upstream's answer constrains content width under a new key instead, so an existing side padding value does not carry over. **One upgrade hazard checked rather than assumed:** `TransitionAnimation.DEFAULT` is renamed to `BASIC`, and `getEnum` catches the parse failure and returns the default, so a stored `DEFAULT` resolves to the new default instead of throwing. No migration, no `versionCode` bump. The removed `transition_animation_default` key is dropped from the base table only and left in the two locales that still declare it, which is exactly what upstream does. Off-path checks bounded at each commit (`5fadea61e..bb24e904a` and `21af65b10..f3125ab89`), both clean; neither commit touches a manifested path. Gates: `:app:compileDebugKotlin`, `:app:spotlessApply` with no unrelated reformats, `di-interop-check` 30 graph-owned types. No CHANGELOG entry: the renderer is still under `[Unreleased]`, so this is churn on a feature no release has carried. **Not device-verified**, and the renderer is off by default, so the Min width slider and the reorganised settings block still need a pass with it switched on. | Nothing skipped. `5fadea61e`, `b69594011`, `7f342ca07`, `34a33ed4f`, `baad5c59b`, `056d9baee`, `21af65b10` and `3acd7eaca` sit beneath these two and are in flight in the same pass. Three of them (`5fadea61e` 9 paths, `b69594011` 24, `baad5c59b` 5) touch manifested files and owe a reconciliation round when they land. |
| `c1d1f6b16` | `c75289937` | 2026-08-28 | 8 ported, 0 skipped, the whole range, and all of it routine bumps. Seven version-catalog one-liners: coil 3.5.0 to 3.6.0 (mihonapp/mihon#3860), baselineProfile rc01 to rc02 (mihonapp/mihon#3856), material-kolor 5.0.0 to 5.0.1 (mihonapp/mihon#3864), kim 0.37.0 to 0.38.1 (mihonapp/mihon#3862), spotless 8.10.0 to 8.10.1 (mihonapp/mihon#3865), glance 1.1.1 to 1.2.0 (mihonapp/mihon#3867), aboutLibraries 15.1.1 to 15.2.0 (mihonapp/mihon#3866). Every one matched upstream's pre-bump value here, so no divergence to reconcile and none of them broke an API, unlike the renderer bump in the row below. **`actions/setup-java` v5.7.0 to v6.0.0** (mihonapp/mihon#3861) applies to three workflows here against upstream's two, since Reikai's build file is `build_check.yml` and it also has `nightly.yml`. Off-path checked clean through HEAD. Gates: `:app:compileDebugKotlin`, `:domain:test`, `:app:testDebugUnitTest`, 1262 app and 75 domain, 0 failures, plus `spotlessCheck` on four modules to confirm the Spotless bump reformats nothing. Not device-verified: no runtime path changed, and CI is what exercises the action bump. | Nothing. |
| `deb7b3311` | `59d814e83` | 2026-08-28 | 3 ported, 0 skipped, the whole range. **Renderer library bumps** (mihonapp/mihon#3852, mihon `0854bde12`): webgpuviewer 36 to 38 and imagedecoder 9 to 10, which upstream pairs with re-done crop bounds, the transition page drawn through the text renderer, and fixes to dual-page progress, animations, navigation buttons and zoom start. **The bump and the code are one unit**, since the library changed its API: taking the versions alone does not compile. `WebGpuViewer.kt` carries no RK island and matched upstream at the base, so it went in verbatim; all three webgpu files are byte-identical to upstream again. **The off-path check flagged `MangaCoverDialog.kt`**, which is manifested, and its change was reconciled by hand into `EntryCoverDialog.kt`: `ImagePage(...)` plus a post-hoc `backgroundColor` becomes `ImagePage.ImageSingle(Image(..., createMipMaps = true, backgroundColor = 0))`, `WebGpuRenderer.dispatcher` becomes `Dispatchers.Default`, and the `state.dpi` line goes because the viewer sets its own. **One step upstream did not need**: the library dropped the nullable `homeScaleOverride` that used to suppress its built-in fit, which this fork sets to fit a cover to the dialog box, so the fit now assigns `homeScale` and `scale` together, the pairing upstream's own viewer uses. Upstream leaves its now-unused `WebGpuRenderer` import behind and this tree does not, the same divergence the `1e5b1dc5e` row records. **AGP 9.3.1 to 9.3.2** (mihonapp/mihon#3853, mihon `3f56685e0`) and **jsoup 1.23.1 to 1.23.2** (mihonapp/mihon#3854, mihon `deb7b3311` = the new base), one line each. Off-path checked and stamped through HEAD, one reported path reconciled. Gates: `:app:compileReleaseKotlin`, `spotlessApply`, `:domain:test`, `:app:testDebugUnitTest`, 1262 app and 75 domain, 0 failures. **Device-verified on a minified nightly on the Fold** with the high quality renderer on, since it is off by default and nothing else would touch it: the manga cover dialog and the novel cover dialog both draw and sit fitted to the viewport, which is the reconciled path and the one thing a compile cannot show. | Nothing skipped. |
| `ac249e366` (unchanged) | `5065a71e1` | 2026-08-28 | **A one-file correction inside the already-synced range, so the base does not move.** `az/strings.xml` (upstream mihon `52f827c6f`) had been skipped twice because an empty locale was thought to reach the system language picker with nothing behind it. Measured on this tree instead of assumed: moko-resources generates no `values-az`, and the generated `locales_config.xml` lists 68 locales with `az` absent, so an empty locale is inert. Taken, and the locale rule above corrected to say so. Found by diffing the whole upstream file set against this tree, which is also what turned up the `MihonViewModelFactory` manifest gap in the same pass. | |
| `ac249e366` | `63cf99971` | 2026-08-24 | Completes the pass the row below began, so the base advances from `a2c24ff04` to upstream HEAD. **Search filter chip divider** (mihonapp/mihon#3839, mihon `46c8f07c1`): upstream centres the chip row and caps its `VerticalDivider` to `FilterChipDefaults.Height`. The hunks had no target here, because `GlobalSearchToolbar`'s chip row is an RK island delegating to `EntrySearchSourceFilterChips`, so they were reconciled into that twin, which manga global search, novel global search and the migrate search screen all render. **Correction to what the sync commit claimed.** That commit called the divider an accidental loss from the collapse. It was not: the Phase 3 global-search collapse removed it deliberately and recorded doing so (`4b8c9bfd6`, 2026-07-13, "the manga+novel vertical divider between chips removed"), though it recorded no reason. Restoring it here therefore reverses a stated decision rather than repairing a drop. **Ruled: the divider stays** (owner, 2026-08-25). Upstream has carried it since `dd3ca0c13` (2023-07-16) and is still improving it, so a divergence with no recorded reason would only be re-closed by whoever ports this file next. `GlobalSearchToolbar` also shed the two divider imports the collapse left unused. **Extension load ordering** (mihonapp/mihon#3841, mihon `270b78ba9`): `installedExtensionsFlow` and `untrustedExtensionsFlow` become plain `Flow`s gated on the scan rather than `stateIn`-seeded `StateFlow`s, `isInitialized` turns private, and `DownloadCache`, `AndroidSourceManager`, `SearchViewModel` and `CrashLogUtil` stop awaiting it by hand. `mapExtensionsOnceInitialized` could **not** be copied: Reikai's `mapExtensions` is an RK rewrite combining the E-Hentai gate, so the helper was rebuilt as `onStart { isInitialized.first { it } }` over that combine. **A risk assessed and found not to exist:** dropping `stateIn` looked like it would leave the flow with nothing to emit, but `AndroidPreference.changes()` ends in `.onStart { emit("ignition") }`, so the gate preference emits on subscribe, and no test constructs a real `ExtensionManager` (`DomainModuleTest` graph-resolves it, `AdultContentCheckerTest` mocks it). **Three Reikai-owned readers upstream cannot see were forced along**, none findable by grepping the commit's own paths: `AdultContentChecker`, `ExtensionBackupCreator` and `ExtensionRestorer` all read `.value`. The checker is the load-bearing one, since it backs the hide-adult-notification switch and `LibraryUpdateNotifier.hideContent` was called from inside non-suspend notification builders. Resolved by having the notifier ask once per batch before it starts building (`showUpdateNotifications` becomes suspend; both callers already were) and by **failing closed**, so a scan that never completes marks every entry adult rather than letting a title reach the lock screen (owner, 2026-08-24). An unbounded wait was rejected because nothing flips the gate if `initExtensions` throws and `ExtensionLoader.loadExtensions` has real throw paths (an `applicationInfo!!` assertion, `getInstalledPackages`); the wait is capped at 5s and pinned by a new test, verified by mutation. **Write-once:** `NovelGlobalSearchViewModel.search` carried the same defect through `NovelSourceManager.getAll()`, whose own KDoc says an early read "reports every installed source as missing", so source resolution moved inside the search job behind `ensureLoaded()` in this same commit, and its `init` lost the now-redundant wait. Off-path check clean through `ac249e366`. Gates: `di-interop-check` (31 graph-owned types), `:app:compileDebugKotlin`, `spotlessApply`, `:domain:test`, `:app:testDebugUnitTest`, 0 failures, **1127** app (up one for the fail-closed test) and 75 domain. **Device-verified on the Fold.** A cold start (process `18:33:03.6`, UI `18:33:05.4`) followed by a global search ten seconds in searched all 20 source hosts, the four built-in adult ones included, which is what proves the RK E-Hentai gate survived the helper being rebuilt rather than copied. A library update with the hide-adult switch on posted its new-chapters notifications normally, naming safe titles and showing the generic line for adult ones, so the batch resolves and the 5s cap did not expire. **The novel gate could not be exercised, and probably cannot be by hand:** on a second cold start the eager plugin load finished at `18:48:51.019`, 43ms before the UI was drawn at `18:48:51.062`, and the fastest search a person could start landed at `18:49:02.8`, twelve seconds later. It covered all six plugins, so the restructure is sound, but the race it guards is unreachable while `LnPluginInstaller` loads eagerly pre-UI. The novel half is therefore defence in depth rather than a fix for an observable defect; the reachable version of this bug is the manga one, whose extension scan does run well after the UI appears. | Nothing. |
| `a2c24ff04` (unchanged) | `e7cf5511b` | 2026-08-24 | 2 of 4 ported, taken newest-first, so the base cannot advance until `46c8f07c1` and `270b78ba9` land beneath them. **WebGPU bump** (mihonapp/mihon#3843, mihon `e3813028b`): animated image frames now carry the reader's own background colour instead of the library default, and `TransitionAnimation` gains `NONE`, which the reader settings sheet picks up for free because `ReadingModePage` renders `entries`. Both `WebGpuViewer` files were byte-identical to the pre-commit blob and taken verbatim; `ReaderPreferences`, the string table and `webgpuviewer` 31 to 36 are one-liners. No CHANGELOG entry: the renderer itself is still under `[Unreleased]`, so this is churn on a feature no release has carried, the same call `262d247a1` made. **Cloudflare interactive challenge** (mihonapp/mihon#3842, mihon `ac249e366`): detection moves off the `403` / `503` status check plus the Jsoup body parse onto the `cf-mitigated: challenge` header, and the WebView now aborts as soon as the challenge reports going interactive, through a small JavaScript bridge. All three changed regions were byte-identical to the pre-commit blob, so upstream's own patch was applied with `git apply --3way` rather than retyped; the drift check leaves only the two RK islands, the FlareSolverr constructor params and the FlareSolverr path in `intercept`. **Two deliberate non-divergences:** the bridge keeps upstream's `mihon` name (owner, 2026-08-24) and the now-unused `org.jsoup.Jsoup` import stays, since both are invisible at runtime and either change would add a permanent drift hunk to a file every sync hand-merges. Gates on both commits: `di-interop-check` (31 graph-owned types), `:app:compileDebugKotlin`, `spotlessApply`, `:domain:test`, `:app:testDebugUnitTest`, 0 failures, 1126 app and 75 domain. **Device-verified on the Fold, natomanga through global search:** the `403` carried `cf-mitigated: challenge` plus `server: cloudflare`, `shouldIntercept` fired (logcat has the stack through `resolveWithWebView` into `createWebView`), and FlareSolverr served the page. The proof it was FlareSolverr and not a retry is that the whole log holds exactly two natomanga search requests, the `403` and a later direct `200`, with results rendering between them under the pinned `Chrome/151.0.0.0 (X11; Linux x86_64)` agent rather than the app's `Tachiyomi Dalvik`. A repeat search answered `200` in 175ms, so the clearance held. **What the device pass could not settle:** whether the new early exit actually fires. Challenge to results was 28.2s against a 30s latch, which fits either an early abort followed by a slow solve or a WebView running close to timeout. The Turnstile frame logs `Failed to execute 'postMessage'` with a `challenges.cloudflare.com` target origin against a `natomanga.com` recipient from one second in, and that is the shape of message upstream's page-level listener waits for, so it may never be delivered on a Turnstile page. Left unchased: the FlareSolverr fallback covers it either way, and the question is upstream's design rather than this merge. | Nothing. `46c8f07c1` and `270b78ba9` are in flight in this same pass, not skipped. |
| `a2c24ff04` (unchanged) | `bf4c8d528` | 2026-08-22 | Backfill of the one commit the row below deferred, so the synced base does not move. **Kitsu GraphQL** (mihonapp/mihon#3792, mihon `31d3c2371`): upstream rewrites `KitsuApi` onto `https://kitsu.app/api/graphql`, deletes `KitsuDateHelper` plus the `KitsuListSearch` / `KitsuSearch` / `KitsuSearchItemCover` DTOs, and adds ten GraphQL DTOs. The deferral reasoning in that row turned out to be **half wrong and worth recording**: the `getUserLibrary` island did NOT need rewriting for GraphQL. Upstream keeps `Accept` / `Content-Type: application/vnd.api+json` on the same `authClient` and leaves `/api/edge/` alone, it simply stops calling it, so both Reikai islands (`getUserLibrary` for the recommendation taste profile, `getMangaMetadata` for Fill-from-tracker) transplant verbatim behind `JSON_API_BASE_URL`. A drift check against the upstream blob leaves only RK-attributable hunks: `Kitsu.kt` +31/-6, `KitsuApi.kt` +144/-3, `KitsuInterceptor.kt` +1/-1 (the `Reikai` user agent), and every removed upstream line is accounted for by an island. The three `Kitsu.kt` islands are the library-pull passthrough, `supportsNovels` + `searchNovel`, and the `getMangaMetadata` override; `getUserId()` is kept (upstream dropped it when GraphQL stopped needing a user id) and now feeds the pull from stored credentials instead of a second `getCurrentUser` request. **Two casing traps, both live-verified:** GraphQL returns enums in screaming case, so the novel filter compares `subtype` case-insensitively through one `KitsuManga.isNovel()` both search paths call, and upstream's `login` lower-cases the rating system in its guard but stores the raw value, which made a `SIMPLE` account throw `Unknown score type` on every tracking sheet (fixed on write and on read, so an already-stored upper-case value recovers without a re-login; worth reporting upstream). Kitsu's schema could serve both islands (`currentProfile { library { all(mediaType: MANGA) } }`, `mappings`, `categories`), but their GraphQL is self-labelled pre-production while JSON:API is not deprecated, so moving them is parked rather than done. Gates: `di-interop-check` (31 graph-owned types), `:app:compileDebugKotlin`, `spotlessApply`, `:domain:test`, `:app:testDebugUnitTest`, 0 failures. **Device-verified on the Fold against a real Kitsu account**: novel search returned Shadow Slave typed `Novel` off a 200 from `/api/graphql`, the taste pull stored 88 rows with cross-tracker ids and tags off a 200 from `/api/edge/library-entries`, and Fill-from-tracker appended Kitsu genres off a 200 from `/api/edge/manga/{id}`. Mutating paths (add, update, delete) were left untested rather than write to the owner's real Kitsu library. | Nothing. |
| `a2c24ff04` | `6d8f8a5a6` | 2026-08-22 | 3 ported of 4. **Upstream scaffold** (mihonapp/mihon#3834, mihon `a2c24ff04`): `HomeScreen` moves off `Scaffold` plus Mihon's own `NavigationBar` / `NavigationRail` onto `NavigationSuiteScaffold`, which merges the two per-form item builders into one `NavigationSuiteItem(tab, navigationSuiteType)` and turns the badge from a `BadgedBox` wrapper into a `tabBadge(tab)` returning a nullable slot; bottom-bar visibility becomes `navigationSuiteState.show()` / `hide()` rather than an `AnimatedVisibility`. `presentation-core` deletes `NavigationBar.kt` and `NavigationRail.kt` (nothing outside `HomeScreen` referenced either, on our side or upstream's) and takes `material3-adaptive-navigation-suite` as an **`api`** dependency, which is how the app module resolves the new imports without a dependency of its own. `HomeScreen` was hand-merged: all six RK islands re-applied, the load-bearing ones being `tabs(combinedRecents)` in place of the `TABS` constant, `recentsTarget` for the two recents intents, the `replacementFor` fixup that moves a tab that just left the set, and the badge asking `is ShowsUpdatesBadge` rather than `is UpdatesTab` in **both** the count and the description branches, since upstream's rewrite duplicated that test. Drift check against the upstream blob leaves only RK-attributable hunks. One RK comment was dropped deliberately: it explained hoisting `context` out of `produceState`, which upstream's own shape now does. **Two bumps**: conscrypt 2.6.3 (mihonapp/mihon#3832, mihon `645824618`), aboutLibraries 15.1.1 (mihonapp/mihon#3831, mihon `9ec479266`). Off-path check clean through `a2c24ff04`. Gates: `di-interop-check` (31 graph-owned types), `:app:compileDebugKotlin`, `spotlessApply`, `:domain:test`, `:app:testDebugUnitTest`, 0 failures. **Device-verified on the emulator**: the new bar renders Library / Updates / History / Browse / More with combined Recents off, and Library / Recents / Browse / More with it on, which is the island the merge had to carry. The tablet rail path was not exercised. | **`31d3c2371` Kitsu GraphQL (mihonapp/mihon#3792): deferred, not skipped.** It rewrites `KitsuApi.kt` and replaces the DTO set, and Reikai's divergence there is not mechanical: a ~113-line `// RK` island implements `getUserLibrary` against Kitsu's JSON:API `/library-entries` for the recommendation taste profile, and `getUserLibrary` has no occurrence upstream after the commit, so there is nothing to merge against and the query has to be rewritten for GraphQL. The `novel` subtype filtering that makes Kitsu one of the seven novel-capable trackers also has to be re-found in the new schema. It lands on novel tracking and recommendations at once, so it wants its own scout rather than a hand-merge in passing. |
| `497e26625` | `31fc173ac`..`76df9f189` | 2026-08-21 | 11 ported of 13, the whole range above Metro. **Renderer fixes** (mihonapp/mihon#3823 + mihonapp/mihon#3801, mihon `497e26625`, `f75f2598a`, webgpuviewer 28 to 31): chapter-boundary page loading now retries once the adjacent chapter has really loaded, fit height / fit width / original size anchor correctly, and right-to-left direction moves into the viewer library. **This reconciles the `ImageDecoder` divergence**: Reikai read `resources.source()` where upstream read `sourceOrNull()`, which is null for a file-backed source such as a disk-cached cover, so the decoder threw, coil swallowed it and the cover dialog drew nothing. Upstream introduced that in mihonapp/mihon#3786 and has now taken the identical fix, so the `// RK` island is deleted. Reikai's own two-line `graph` field in `WebGpuViewer` went with it, since it only matched the sibling viewers and bought no behaviour; all three webgpu files and `ImageDecoder` are byte-identical to upstream again. **MangaUpdates score and creators** (mihonapp/mihon#3795, mihon `c613b5250`): hand-merged, because Reikai already carried its own `MUAuthor` and `MUGenre` for Fill from tracker. `MUAuthor` moves to upstream's file, keeps Reikai's nullable fields (the search record omits them), and the author/artist split becomes one `namesOfType` helper both the search mapper and Fill from tracker call, keeping Reikai's substring match over upstream's equality test. **Shikimori update endpoint** (mihonapp/mihon#3810, mihon `0974e787a`): `updateLibManga` becomes a real `PUT /v2/user_rates/{id}`, `SMAddMangaResponse` becomes `SMLibraryIdResponse`. **A second divergence reconciles here**: Reikai already returned null from `findLibManga` when the title was not in the user's list, where upstream gated it on `isRefresh` and so let `bind()` take its found branch with status 0 and throw; upstream dropped the flag, so the patch and its comment go. **Kitsu rating scales** (mihonapp/mihon#3818, mihon `01bfaa99a`): scores move to Kitsu's native 2-20 scale and the scale follows the account's own setting, read at sign-in. `TrackPreferences`, `KitsuUtils` and both DTOs verbatim; `Kitsu.kt` hand-merged and now differing only by its RK islands. **The migration is `38.sqm` and covers `novel_tracks` as well as `manga_sync`**, which upstream has no notion of: novels bind through the same tracker and stored scores the same way, so omitting them would have halved every tracked novel's rating. No `versionCode` bump, a `.sqm` runs off the database's own `user_version`. **Translations** (mihonapp/mihon#3701, mihon `2b532ea0c`): scripted per-key three-way merge, 72 added, 96 updated, 0 conflicts, plus 296 dead lines dropped across 55 files for seven keys upstream removed that Reikai's base no longer declares either. **Five bumps** (mihonapp/mihon#3814, mihonapp/mihon#3798, mihonapp/mihon#3820, mihonapp/mihon#3819, mihonapp/mihon#3822): Gradle wrapper 9.7.1, spotless 8.10.0, firebase-bom 34.18.0, crashlytics 3.0.8, aboutlibraries-compose-m3 15.1.0. Off-path checked clean through HEAD. Gates: `di-interop-check` (31 graph-owned types), `:app:compileDebugKotlin`, `spotlessApply`, `:domain:test`, `:app:testDebugUnitTest`, 0 failures. **Device-verified on the Fold**: the Kitsu migration ran on a real database, seven manga rows and two novel rows doubled, the unset zero stayed zero, no other tracker moved, and both a tracked manga and a tracked novel still display 9 and 8 on screen rather than 18 and 16. **The renderer was exercised both ways on the Fold**, since it is off by default and nothing else would have touched it: manga cover dialog, novel cover dialog, manga reader and novel reader, each with the high quality renderer off and then on, six of the eight cases being the ones the cover bug and the WebGPU viewer sit behind. All eight render. Crossing a chapter boundary under the WebGPU viewer moves the top bar to the next chapter, resets the page slider to its new count and decodes its pages, which is the behaviour mihonapp/mihon#3823 fixes. The setting was returned to off afterwards. | **`3e98c15b7` (duplicate string removed): N/A**, Reikai's base never had the duplicate. **`3cc0d2c32` (trim the `id:` query): N/A**, Reikai folded the prefix into one `String.trackerSearchId` helper on `BaseTracker` in the mihonapp/mihon#3776 port, and it already trims, for six trackers rather than upstream's two. **Azerbaijani was not added**: upstream's `az/strings.xml` is empty, so the folder would only put the language in the system picker with nothing behind it. Take it as soon as it has content. |
| `b2015d1ef` | `f674e77bf` | 2026-08-20 | 1 ported, the largest single sync this fork has taken. **Metro DI** (mihonapp/mihon#3608, mihon `b2015d1ef`): upstream replaces Injekt with a compile-time graph across 314 files; Reikai had 265 files importing Injekt, over half of them Reikai-owned with no upstream counterpart to copy, so most of it was a rewrite rather than a merge. Landed as eight phases over 81 commits from `cac77f1aa`, each compiling and booting, with its own record in [metro-di-migration.md](plans/metro-di-migration.md). The graph is `mihon/app/di/AppGraph.kt` at 71 accessors and 23 `inject()` members, with `AppBindings` (upstream's) and `ReikaiBindings` (ours) beside it; `AppModule`, `PreferenceModule` and `migrations/Migrations.kt` are deleted and `DomainModule` is down from 125 registrations to twelve. **Four deliberate divergences, all fenced and recorded**: interop entries are `Provider`s rather than eager instances, the migration set and the cover cache are read at their call sites rather than injected as `App` fields, and `AndroidSourceManager` keeps the `context` upstream dropped, the first three because `LegacyYokaiDbImporter` has to move an incompatible database aside before anything opens the database. Where a dependency's construction had to stay deferred it is a `Provider<T>` parameter rather than a lazy delegate, `NovelDownloadManager` above all, since building it restores the persisted download queue and can start the worker. **What stays on Injekt is permanent or ruled**: `source-api` and `source-local` are the contract installed extensions compile against, and the novel reader goes with the tsundoku migration; `MetroInteropModule` hands 89 graph-owned singletons back for them. Three device passes plus a per-surface sweep on minified builds: backup and restore across a full wipe, a chapter downloaded to a finished `.cbz`, tracker binds on live accounts, all sixteen migrations running in version order after rewinding `last_version_code`, both library and Recents chips, both details screens with their cover dialogs, the recommendation carousel making a real request, and a bulk novel download drained and read back as downloaded. 1082 app tests and 75 domain tests, 0 failures. | None. The two open questions this row carried before it landed are both answered: the R8 keeps stay (upstream keeps the same three package trees while fully on Metro; this row said Reikai's two leave with the reader, which the owner reversed on 2026-08-21, all five are permanent, see architecture.md), and the hand-written ViewModel factories moved to `metrox-viewmodel`. |
| `1e5b1dc5e` (unchanged) | `b055a23a5` | 2026-08-20 | 2 ported, both from *below* the frontier. **Baseline profile regenerations** (mihon `91e967128` and `4296d056b`). Not a base advance: both are already ancestors of the base, and they were missed because a profile file changes without any code change to notice. Reikai's copies were byte-identical to upstream's at mihon `b3e190c62`, the one commit that had ever touched them here, so upstream's two regenerations were the entire difference: 34,225 lines down to 33,385 for `baseline-prof.txt`, 28,779 down to 27,659 for `startup-prof.txt`. Taken verbatim by copy, as the first sync did; the `:baseline-profile` module could generate them locally but that would replace an upstream-tracked file with a locally generated one and make every later sync of it a conflict. **Upstream has still not regenerated since its own Metro commit**, so these carry 229 `injekt` lines and two `MigrationsKt` entries for a class neither tree has any more; that is upstream's state to inherit, not drift to fix, and a profile entry for an absent class is ignored at install time. No code changed, so the gates are unchanged from the commit below. | None. The off-path stamp is bounded at `b2015d1ef`, the range this tree has actually walked. |
| `1e5b1dc5e` | `7f50306ae` | 2026-08-16 | 7 ported, 0 skipped, the whole range below Metro. **Filter icon says when a filter is on** (mihonapp/mihon#3772, mihon `abdecb3cb`): `UpcomingViewModel` was byte-identical to upstream and went in verbatim, so its unused `State.hasActiveFilters` finally gets computed. Upstream's `UpdatesViewModel` half has no counterpart, because the recents engine owns `filterActive`, and it already counted the category filter through `RecentsCategoryFilter.active`; what was missing was the excluded-scanlators switch, so `recentsFilterActive` gained a `byScanlator` term. It also gained a `chipShowsManga` term, because scanlator exclusion is manga-only by mechanism (`NovelUpdatesViewModel` says so already), and the engine's own rule is that a filter reaching nothing on screen must not report the feed as filtered. Two tests pin the pair, both verified by mutation. **Tracker `id:` search** (mihonapp/mihon#3776, mihon `a81b7bce9`): the six trackers upstream touched, plus the write-once half upstream has no notion of. AniList and Kitsu take the `novel` flag their title search already carries; Shikimori and Hikka get a private `detailsByKind` / `contentDetails` beside the existing `searchByKind` / `searchContent`, so the manga and novel lookups differ only in the kind or content-type string; MangaUpdates filters the record by type the way its `searchNovel` does; Bangumi is manga-only here and took upstream verbatim. **The `id:` parse itself is one `String.trackerSearchId` helper on `BaseTracker`** rather than upstream's per-tracker block, because a tracker's manga and novel searches restating the prefix is exactly the drift the write-once rule exists to stop; MyAnimeList and MangaBaka, which already carried the prefix twice each, fold into it. One deliberate divergence: MangaUpdates no longer returns an empty list for an unparseable id, it falls through to a title search like the other five. **Legacy decoder dropped** (mihonapp/mihon#3786, mihon `00fb597ae`, with its two dependency bumps mihonapp/mihon#3777 and mihonapp/mihon#3774): `com.github.mihonapp:image-decoder` goes, `ca.mpreg:imagedecoder` (formerly `image-decoder2`) becomes the only one, `TachiyomiImageDecoder` and `GLUtil` are deleted, `ImageDecoder` is new, and `ImageUtil` loses `canUseHardwareBitmap` and the device blocklist. Five upstream files were byte-identical and went in verbatim; `App`, `ReaderActivity` and `SettingsAdvancedScreen` were hand-merged around their RK islands; `MangaCoverDialog` is a manifested twin, so its `ImageDecoder2` rename was reconciled into `EntryCoverDialog`. Three preferences go with it (hardware bitmap threshold, legacy long strip decoding, custom display profile) and their five base strings; the locale files keep theirs, as upstream's do. **Upstream leaves eight now-unused imports across those three files and this tree does not**, the one place the files stop being byte-faithful. **Extensions load off the main thread** (mihonapp/mihon#3788, mihon `5fa5f7e15`), and **okhttp 5.5.0** (mihonapp/mihon#3789, mihon `1e5b1dc5e` = the new base). Tests 1064 (two new), 0 failures. Verified with `:app:compileDebugKotlin`, `spotlessApply` and `:app:testDebugUnitTest`, then on the emulator: reader pages and the transition page under the WebGPU renderer, library covers on both chips, and the filter icon tinting on Manga while staying plain on Novels with the scanlator switch on. | **Metro DI (mihonapp/mihon#3608, mihon `b2015d1ef`): not in this pass**, which is why the stamp is bounded at `1e5b1dc5e`. It is a 314-file upstream migration against 265 Injekt-importing files here, and the owner has taken it as its own scoped piece of work rather than a sync row. The Upcoming filter icon was not exercised on device: it is a verbatim port into a field the toolbar already read. |
| `ed30766a9` (unchanged) | `fcc272543` | 2026-08-15 | 1 ported, closing the campaign and the deferral the row below opened. **Library models collect only while subscribed** (mihonapp/mihon#3762, mihon `d00efa4bf`). Not a base advance: already an ancestor of the base. `LibraryViewModel` decomposes into a `searchQuery` holder, an `activeCategoryIndex` holder, a `displayPreferences` combine, a `hasActiveFilters` flow and a `libraryData` query shared with `WhileSubscribed(5.seconds)`, with `state` derived over the five; `NovelLibraryViewModel` takes the same shape in the same commit per the write-once rule. Upstream's `selection`, `dialog` and `groupedFavorites` have no counterpart, since the engine owns all three. **The models alone would have been inert**, which is what made this bigger than upstream's diff: both library adapters re-published their model's state with `SharingStarted.Eagerly` and `LibraryEngine.assembled` shared eagerly over both providers' rows, so two permanent subscribers held the window open for the model's whole life; all three move to `WhileSubscribed(5.seconds)` and every `LibraryTab` read of them moves to `collectAsStateWithLifecycle`. `contentType`, `display`, `dialog`, `selection` and `globalSort` deliberately stay eager and plainly collected, on the rule the recents engine states: a value a verb reads synchronously is not shared while subscribed. `randomEntry` reads `assembled.value` and holds anyway, because a share that has emitted keeps its last value after the window closes. **`lifecycle-runtime-compose` is now declared** in the catalog and the lifecycle bundle; 27 call sites had been compiling against a transitive copy at whatever version it happened to bring. The novel model's synchronous search-field write is gone with its state, and fast typing was re-checked on device for it. Tests 1062, unchanged, and the assembly-prune test was re-verified by mutation under the new sharing policy. Verified with `:app:compileDebugKotlin`, `spotlessApply` and `:app:testDebugUnitTest`, then on the emulator: cold start, a 25 second tab round trip, two 30 second background round trips (manga and novels), eight fast keystrokes landing in order in the search field, all three chips swapping without a stall, selection plus the set-categories dialog, and Open random entry. | None. |
| `ed30766a9` (unchanged) | `bc119615d` | 2026-08-15 | 1 ported, half of a standing deferral. **Updates feeds collect only while subscribed** (mihonapp/mihon#3761, mihon `b1a9a0592`). Not a base advance: the commit is already an ancestor of the base, so this clears half a deferral from *below* the frontier, and its library twin stays in [Pending](#pending-needs-planning). `UpdatesViewModel`'s feed leaves its always-on `init` collector for a `flowOn(Dispatchers.IO)` plus `WhileSubscribed(5.seconds)` `stateIn`, with `state` derived from it. Upstream's `downloadStates` override map came along and had to: once the feed is derived, `updateDownloadState` has nowhere to write, and `cancelDownload` calls it synchronously. The map also drops an override on a terminal status rather than letting it outlive the row it described. Upstream's `selectedChapterIds`, `dialog` and `hasActiveFilters` decomposition has no counterpart here, because all three already moved to the recents engine and `toUpdateItems` already carried no `selected`. `NovelUpdatesViewModel` takes the identical shape in the same commit per the write-once rule, minus the override map: per-byte progress is not a novel capability, so its queue state rides its own combine and a queue change re-derives the rows outright. **No consumer changed**, unlike every earlier commit in this campaign: the only readers are the two adapters' cold `updatedLane` maps, already under the engine's `WhileSubscribed` and already read with `collectAsStateWithLifecycle`. Both models now recompute their three-month window per subscription, matching upstream. Tests 1062, unchanged. Verified with `:app:compileDebugKotlin`, `spotlessApply` and `:app:testDebugUnitTest`, then on the emulator: the feed on all three chips, a tab round trip and a background round trip past the timeout on both manga and novels, a 35-chapter download repainting rows individually as each chapter landed, and a mid-download cancel that left no stale override and no error in an app-scoped logcat. | **The library twin (mihonapp/mihon#3762, mihon `d00efa4bf`): still DEFERRED**, see [Pending](#pending-needs-planning). It cannot land alone: the two library adapters and `LibraryEngine.assembled` have to leave `SharingStarted.Eagerly` in the same change, or the conversion is inert. |
| `ed30766a9` | `262d247a1` | 2026-08-15 | 1 ported. **WebGPU reader background and transition colours match the old reader** (mihonapp/mihon#3765, mihon `ed30766a9`), a follow-up to the renderer taken the day before. The activity's private grey and its automatic-background helper become one `Context.readerBackgroundColor(readerTheme)` in `ContextExtensions`, which both the activity and the viewer call, so the WebGPU viewer stops drawing its own `0x808080` grey and a hardcoded black-and-white transition. `ContextExtensions` and `WebGpuViewer` were byte-identical to upstream's pre-commit blob and went in verbatim; `ReaderActivity` was hand-merged around its 15 RK islands and drift-checked clean. Upstream leaves the now-unused `Color` and `isNightMode` imports in the activity, so those stay here too. Verified on the emulator with the Gray theme: the letterbox reads `0x202125`, the reader's own grey, where the old code drew `0x808080`. | None. |
| `b1efee47f` | `ef15fca50`..`0511203a7` | 2026-08-14 | 2 ported of 5. **StateViewModel removed in favour of explicit backing fields** (mihonapp/mihon#3763, mihon `b1efee47f`, `ef15fca50`): 36 models across `app`, `exh` and `reikai` declare their own `val state: StateFlow<S>` with a `field = MutableStateFlow<S>(...)`, `mutableState` writes become `state`, and the `core:viewmodel` module goes with its settings and app-dependency entries. The syntax is Kotlin's explicit backing field and needs no compiler flag on 2.4.10, which was verified on one file before the rest were touched. Five of upstream's files were byte-identical to its pre-commit blob and went in verbatim; six were hand-merged around their RK islands and drift-checked; five manifested twins took the rename by hand. `SearchViewModel` keeps upstream's `protected fun updateState`, because a backing field is private to its declaring class; Reikai's own abstract base needs none, since all three subclasses of the two bases write no state. Two lambda parameters that would have shadowed the new property were renamed, matching upstream, and the shadowing locals upstream chose to keep are kept here for diffability. **kim 0.36.0** (mihonapp/mihon#3757, mihon `55675e7f8`). Tests 1062, unchanged. Verified with `:app:compileDebugKotlin`, `spotlessApply`, `:app:testDebugUnitTest` and a minified `:app:assemblePreview`. | **The library and updates while-subscribed conversions (mihonapp/mihon#3762, mihonapp/mihon#3761): DEFERRED** to their own pass with a device test, not refused; see [Pending](#pending-needs-planning) for why they are a redesign rather than a mechanical port and what they oblige on the novel side. The base advances past them, as it did around the ViewModel migration. **The `preview` to `nightly` rename (mihonapp/mihon#3760): DECLINED**, see [Deliberate divergences](#deliberate-divergences-from-upstream); its companion workflow change in the `mihon-preview` repo is being taken in part, on its own branch. |
| `77e88a215` | `77c57bc51` | 2026-08-14 | 6 ported, 0 skipped, the whole range. **The high quality WebGPU renderer** (mihonapp/mihon#3388, mihon `5c3ae37d5`): an alternative manga viewer behind `pref_high_quality_renderer_key` in Settings -> Advanced, default off, bringing dual page view, twelve page-transition animations and a display cutout mode with it. Three net-new files (`WebGpuViewer`, `WebGpuConfig`, `WebGpuViewerContinuous`) plus five files that were byte-identical to upstream's pre-commit blob and so went in verbatim (`ReadingMode`, `ReadingModePage`, `BasePreferences`, `TachiyomiImageDecoder`, `Utils`); hand-merged `ReaderPreferences` (3 RK islands), `ReaderActivity` (15) and `SettingsAdvancedScreen` (10), each drift-checked clean against the post-commit blob. `MangaCoverDialog` is a manifested twin, so its alternate render branch was reconciled by hand into `EntryCoverDialog`, which serves both content types, so a novel cover takes the new path on the same switch. The renderer itself is manga-only with no write-once debt: it draws image pages, novels have their own engine, and the reader sits at chrome-only on the seam table. **Bumps**: appcompat 1.8.0, paging 3.5.1, webkit 1.17.0, baseline-profile 1.5.0-rc01, and kim 0.34.0 straight to 0.35.0 since the renderer introduces it (mihonapp/mihon#3751, mihonapp/mihon#3750, mihonapp/mihon#3756, mihonapp/mihon#3749, mihonapp/mihon#3757). Off-path checked through HEAD after reconciling the one reported path. Tests: 1062, unchanged. Verified with `:app:compileDebugKotlin`, `spotlessApply`, `:app:testDebugUnitTest` and a minified `:app:assemblePreview`. **Size cost, measured on the arm64 preview APK: 22.4 MB compressed**, being `libimagedecoder2.so` at 16.6 MB and `libwebgpu_c_bundled.so` at 5.8 MB, per ABI; that APK is now 50.8 MB and the universal one 155.3 MB. | None. |
| `db7f1663b` | `1d78b0a3c` | 2026-08-13 | 16 ported in one commit, closing the campaign the previous row opened. **The rest of collect-only-while-subscribed** (mihonapp/mihon#3716 through mihonapp/mihon#3729, mihon `083488433`..`77a1a5afb`): twelve models leave an always-on `init` collector for a `WhileSubscribed(5.seconds)` `stateIn`, and every reader moves to `collectAsStateWithLifecycle`. Six were verbatim (`SourcesViewModel`, `SourcesFilter*`, `MigrateSourceViewModel`, `ExtensionFilter*`, `ExtensionDetails*`, `Upcoming*`); the rest hand-merged. **Both content types in the same commit** per the write-once rule: `NovelSourcesViewModel`, `NovelSourcesFilterViewModel` and `MigrateNovelSourcesViewModel` take the identical shape, and the cover conversion lands once in the shared `EntryCoverViewModel` base, which moves from `SharingStarted.Lazily` rather than from an init collector. Five manifested twins carried the change by hand: `ReikaiSourcesTab`, `ReikaiMigrateSourceTab`, `EntryCoverViewModel` and `EntryMigrationFavoritesScreen` (twice). Two traps paid: `ExtensionsViewModel` publishes `searchQuery` and `updatesCount` as their own flows so `BrowseTab` and the tab badge stop holding the whole state open, which `ReikaiExtensionsTab` needed too; and both migrate-sources sort toggles now read the preference through `getAndSet` instead of `state.value`, which answers its seed while nothing is subscribed. `CategoryViewModel` was the heaviest at 19 `// RK` islands, and keeps its five-way content combine with the dialog combined in beside it rather than folded into the list flow. **MangaBaka scores at step sizes above 1** (mihonapp/mihon#3740, mihon `cd1f42694`), hand-merged around the two RK islands; `getScoreList` was returning the score's list index. **Bumps**: conscrypt 2.6.2, kotest 6.2.4, xmlutil 1.0.2 (mihonapp/mihon#3745, mihonapp/mihon#3744, mihonapp/mihon#3730). Off-path checked through HEAD after walking all 5 reported paths. Tests: 1019, unchanged, re-run with `--rerun-tasks` because `getMangaByIdAsFlow` and `GetManga.subscribe` drop `suspend`. Verified with `:app:compileDebugKotlin`, `spotlessApply`, `:app:testDebugUnitTest` and a minified `:app:assemblePreview`. On the emulator, every converted screen was opened and put through a background round trip past the 5 second timeout: both chips of sources, the sources filter and migrate-sources, the migrate favourites picker with a selection held across the trip, the categories screen, the repos screen, extensions with its search bar and badge, extension details, the cover viewer and the upcoming calendar. The Categories rename dialog and the add-repo dialog both survive the trip, which is the case that fails if the dialog stays folded into the derived list flow. Editing a cover through the system photo picker also works, and that is the one `.value` read that happens while the flow is genuinely stopped; it holds because `stateIn` retains its last value after sharing ends. The MangaBaka fix was checked on the Fold at step size 10: picking 90 (index 9) stores and displays 90 rather than 9, survives a cold start, and lands as 90 on the MangaBaka site. Note that the fix corrects new writes only, so a score set on an older build is still stored as its index. | **The download-queue conversion (mihonapp/mihon#3727): NOT taken**, see [Pending](#pending-needs-planning). Mihon's `DownloadQueueViewModel` is the manifest's sanctioned dead file, and the live `MangaDownloadQueueViewModel` keeps a per-series totals accumulator and a transient-empty debounce over its own `state.value`, so it is the same shape upstream skips in `UpdatesViewModel` and `LibraryViewModel`; Road B owns the question. `437dd8722` (a fix to Mihon's own changelog): N/A, Reikai owns its own. |
| `ed274e26f` (unchanged) | `ac0fe5d17` | 2026-08-11 | **History feeds collect only while subscribed** (mihonapp/mihon#3728, mihon `a494ebd79`). Not a base advance: this takes one commit from *above* the frontier and leaves its twelve siblings, so the base stays put. Taken early because the recents cutover is about to be built on this file's shape, and porting it afterwards would rework the shell. `HistoryViewModel` leaves `StateViewModel` for a plain `ViewModel`, its search query and dialog become their own holders, and the feed plus the assembled state are shared with `WhileSubscribed(5.seconds)`. Hand-merged, since Reikai's subscription key carries the History category selection beside the query and the flow carries the custom-info overlay. `NovelHistoryViewModel` takes the identical shape in the same commit per the write-once rule, and both tabs plus `ReikaiHistoryScreen` move to `collectAsStateWithLifecycle`, without which a backgrounded app keeps collecting and the change buys nothing. Tests: 933, unchanged. Verified with `:app:compileDebugKotlin`, `spotlessApply`, `:app:testDebugUnitTest`, and on the emulator across both chips: cold start, search and clear, a tab round trip and a background round trip past the timeout, both dialogs, the add-to-library duplicate prompt and tab-reselect resume. | **Upstream's `emptyList()` seed**, see [Deliberate divergences](#deliberate-divergences-from-upstream). **The other twelve conversions in the same campaign: NOT taken**, see [Pending](#pending-needs-planning). **`UpdatesViewModel` and `LibraryViewModel`: not converted, and upstream has not converted them either**; both keep an incremental download-state accumulator that has no derivation, and reworking it is Road B's. `2506b0496` (xmlutil 1.0.2): routine bump, left for a normal sweep. |
| `ed274e26f` (unchanged) | `0e193a338` | 2026-08-08 | The query half of **the Updates category filter** (mihonapp/mihon#3589, mihon `1d8a2b05d`). Not a base advance: that commit is already an ancestor of the base, so this clears part of an unported commit from *below* the frontier. The two include/exclude blocks go into `updatesView.sq` verbatim, sentinel id 0 for uncategorized and all, and `GetUpdates` / `UpdatesRepository` gain the two id lists; the one call site passes empty, which the query reads as no constraint, so nothing changes behaviour yet. Verified with `:app:compileDebugKotlin`. | **The preference and picker halves, deliberately.** Upstream declares two `getLongArray` keys on `UpdatesPreferences` and adds a Categories tab to its filter sheet. Reikai's selection is one set spanning both content types, typed `Set<String>` so the shared category-id registry scrubs it on a category delete, and it is edited through its own picker; adopting upstream's shape would need a parallel registry bucket. The [Pending](#pending-needs-planning) row stays until the recents surface wires the query to that selection. |
| `ed274e26f` | `ddeb1a67d`..`668c0f3d5` | 2026-08-08 | 4 ported across 3 commits, closing the backlog. **java.time to kotlinx.datetime** (`ddeb1a67d`, mihonapp/mihon#3001, mihon `6d69903a5`): the catalog gains kotlinx-datetime 0.8.0 and four modules take it, plus its own proguard keep; 49 upstream files split 27 verbatim (two of them taken at `6d69903a5` rather than HEAD, since later commits touch `DateExtensions` and `GetUpdates`), 18 hand-merged and 6 manifested twins reconciled by hand, each into the Reikai file that actually holds its code today, which for `MangaScreen` and `MangaInfoHeader` is `EntryDetailsColumn` and `EntryActionRow`, not the `EntryDetailsContent` / `EntryInfoBox` the manifest names as their replacements (the commit message names the manifest rows instead, and is wrong on those two); 13 Reikai-owned files in total, being those 6, plus 3 that broke on their own (`ChapterListDialog` and the two unified-updates widgets, which call `toEpochMilli` on a re-typed `DateLimit`) and 4 converted for twin parity. `Commands.kt` in `build-logic` verified by a configuration run before anything else. **Relative-date fix** (`7d726ea44`, mihonapp/mihon#3696, mihon `3d20bcc6b`): `DatePeriod.days` is the day component of a period, not the total, so a date past a month understated its age; two lines in `DateExtensions`. **MangaBaka timezone fix** (`668c0f3d5`, mihonapp/mihon#3711, mihon `f9156aa17`): hand-merged, since Reikai's `MangaBakaApi` carries a debranded User-Agent. The method and what the pre-measured map got wrong are kept in [Pending](#pending-needs-planning) under a fold. Off-path check clean through HEAD after walking all 8 reported paths: the 6 datetime twins reconciled this pass, plus `ReorderCategory` and `DeleteCategory`, whose twins (`CategoryActions`, `DeleteCategoryCleanup`) already carried `updateAllOrders` from the previous pass, which had bounded its range below them. Tests: 762, unchanged. Verified with `:app:compileDebugKotlin`, `spotlessApply`, `:app:testDebugUnitTest` and a minified `:app:assemblePreview`. | **`1d8a2b05d` (Updates-tab category filters): STILL DEFERRED, and now BELOW the frontier**, since the base advanced past it; the history and updates redo owns it, and the [Pending](#pending-needs-planning) row is the only record because none of its files are manifested. **`ed274e26f` (junit-jupiter 6.1.3): already carried**, picked up ahead of the base by the previous toolchain sweep, so the base advance is bookkeeping. |
| `0648e2eaa` | `abd21736f`..`8dd714baa` | 2026-08-08 | 27 ported across 9 commits, themed. **Toolchain** (`abd21736f`): 11 renovate bumps plus Gradle 9.7.0 and its wrapper jar, the setup-java and gradle/actions pins into Reikai's own three Gradle workflows, and the Compose-workaround removal (mihonapp/mihon#3651, mihon `54ae69eaa`) that deletes `animateItemFastScroll`, the focus-request delays and two manual dividers. **R8 keeps** (`1a4d9dc68`): the Shizuku member keep (mihonapp/mihon#3676) and the androidx.window dontwarns (mihon `869fa1a4b`). **Fixes** (`09b44e024`): backup restore no longer losing a whole batch to one bad entry, with the retry applied to the novel stream too (mihonapp/mihon#3667); reader init moved into the ViewModel so a rotation mid-open cannot cancel it, with Reikai's source-scope flag moved to the same intent-backed read (mihonapp/mihon#3686); verbose crash dumps (mihon `8fceb737a`). **Trackers** (`1ad916682`): MyAnimeList partial dates (mihonapp/mihon#3573). **WebView** (`d075b9ae0`): client-hint spoofing via a new `setUserAgent`, the X-Requested-With thread fix and Chrome 149 (mihonapp/mihon#3678); adds `androidx.webkit` to `core:common`. **App updater** (`9f35e6afe`): the download-and-install-in-screen rework (mihonapp/mihon#3669 + mihonapp/mihon#3707), cold-start checks replacing the three-day and one-day throttles (mihonapp/mihon#3658), and no re-check on configuration change (mihonapp/mihon#3708); `AppUpdateNotifier` and `AppUpdateDownloadJob` deleted. **Categories** (`5ac72273e`): dedicated per-column update queries replacing `CategoryUpdate` (mihonapp/mihon#3693), with seven Reikai call sites re-typed and an RK `updateFlags(flagsById)` for the novel sort reset. **Upcoming** (`b831b88a9`): category filters on the calendar (mihonapp/mihon#3607), kept on java.time. **Translations** (`8dd714baa`): a scripted three-way merge of both Weblate imports, 164 added / 468 updated / 106 removed / 1 conflict resolved by hand. Off-path checked with `-MihonThrough 0648e2eaa -Reconciled`; the tooling change that allows a bounded pass is `03df25367`. | **`6d69903a5`, `3d20bcc6b`, `f9156aa17` (java.time to kotlinx.datetime and its two dependents): DEFERRED** to their own pass, which is why the base stops at `0648e2eaa` rather than reaching `ed274e26f`. **`1d8a2b05d` (Updates-tab category filters): DEFERRED** to the history and updates redo; see [Pending](#pending-needs-planning). Releases v0.20.2, v0.20.3 and v0.20.4, the regenerated baseline profiles (Reikai generates its own) and the repo-image update: N/A. `CHANGELOG.md` (Reikai owns its own). |
| `45b1e781e` (unchanged) | `fa06232cd`..`42b2a2799` | 2026-08-07 | **The ScreenModel to AndroidX ViewModel migration** (mihonapp/mihon#3594, mihon `c3b99aea0`), plus its own crash fix (mihonapp/mihon#3609, mihon `98705910e`). Not a base advance: `c3b99aea0` was already an ancestor of the base, so this clears an unported commit from *below* the frontier rather than moving it. Landed in seven phases over `fa06232cd` (the `:core:viewmodel` module and two pilots), `ca1fd1096`, `52a0bc077`, `80fec521e`, `aae9212da`, `447e4597f`, `43905b2f3`, `a168226da` and `42b2a2799` (upstream's `Navigator.kt`, docs). Every Mihon file is on `ViewModel`. | **The novel reader**, deliberately: it is Reikai's own file with no Mihon counterpart, and the tsundoku reader migration deletes it. `voyager-screenModel` stays in the catalog and bundle until then, the one place Reikai diverges from upstream's post-migration state. |
| `45b1e781e` | `ace279c92` | 2026-07-27 | 12 ported across 6 commits (`0fea38882`..`ace279c92`). **Bumps + telemetry** (`0fea38882`): AGP 9.3.1 (mihonapp/mihon#3634), okio 3.18.0 (mihonapp/mihon#3628), kotest 6.2.3 (mihonapp/mihon#3622), compose-bom-alpha 2026.07.00 (mihonapp/mihon#3597), actions/checkout v7.0.1 (mihonapp/mihon#3620) applied to all four of Reikai's own workflows, and the device-ABI analytics field (mihon `069e5c91a`). That commit's message calls the ABI line inert in Reikai; that stopped being true the same day, when `ca6f96b0a` pointed the production-app gate at Reikai's own packages. **Extension installation** (`46ab16bf9`): the shizuku install fix (mihonapp/mihon#3630) verbatim across the AIDL and both shizuku files, and the private-installer update fix (mihonapp/mihon#3614), hand-merged into the RK-patched `ExtensionManager` in a stock region. **Resumed-download progress** (`ca49599ff`, mihonapp/mihon#3616), both files verbatim. **Tokyo Night theme** (`6ab13e0e0`, mihonapp/mihon#3502): five files verbatim, three hand-merged; note the two colour resources live in `presentation-core`, not `app`, and the style block references them across modules, so it needs `processReleaseResources` to verify, not just a Kotlin compile. **`:source-api` + `:source-local` to Android library modules** (`283a045d0`, mihonapp/mihon#3636): 72 files against upstream's 38, since Reikai's EXH metadata subsystem and its extra source interfaces live there; `commonMain`/`androidMain` collapse into `main`, `androidHostTest` into `test`, the expect/actual pairs lose their keywords, and `api()` becomes `implementation()` so `data` gains injekt to compensate. `source-api` keeps two RK deps (i18n for the EXH MR strings, the test bundle). The architecture rule moved with it: `i18n` is now the only multiplatform module. **AST library search** (`ace279c92`, mihonapp/mihon#3554): the lexer, parser, AST and matcher verbatim; `LibraryItem` and `LibraryScreenModel` hand-merged. The EXH tag-search grammar stays as a separate path the screen model dispatches to, and **novel search deliberately did not move**, so the two libraries now answer a typed query differently until the All chip unifies them (queued in ROADMAP). | **`47d7c0f2e` (import reorder across all modules): SKIPPED** by owner ruling. Reikai has 78 `// RK` islands in `LibraryScreenModel` alone, so a repo-wide reorder is a large diff that buries real changes; spotless settles import order on files a change already touches. **mihonapp/mihon#3615 (MangaBaka in the README tracker list): N/A**, Reikai owns its README. **mihon `98705910e`: still DEFERRED** with the ViewModel migration bundle; it is the only commit in this range touching `TrackInfoDialog`, which is why the off-path check flags that file. `CHANGELOG.md` (Reikai owns its own). |
| `5ce7d00eb` | `4f42f25df` | 2026-07-20 | 1 ported. **actions/setup-java `v5.5.0` -> `v5.6.0`** (mihonapp/mihon#3602, mihon `5ce7d00eb` = the new base): the pinned-SHA bump (`0f481fcb...` -> `03ad4de0...`), applied to all three of Reikai's own workflows (`release.yml`, `build_check.yml`, `preview.yml`); upstream touches `build.yml` + `release.yml`, which Reikai maps onto its own workflow set. CI-only, no RK island, nothing to compile. | **mihon `98705910e` (mihonapp/mihon#3609, the ViewModel-migration crash fix): DEFERRED**, sits above this commit and rides in with the migration bundle, see [Deferred upstream changes](#deferred-upstream-changes). |
| `03229a380` | `7af294cdf` | 2026-07-18 | 1 ported. **aboutLibraries `15.0.3` -> `15.0.4`** (mihonapp/mihon#3604, mihon `03229a380` = the new base): one line in `gradle/libs.versions.toml`, the library behind the open-source-licenses screen, a patch bump. No RK island touched. Verified with `:app:compileReleaseKotlin`. | None. |
| `493140c7f` | `d552ecbe0` | 2026-07-16 | 2 ported, ahead of the deferred migration commit. **Reader-init cancellation** (mihon `98bb731b4`): the reader-init coroutine changes from `launchNonCancellable` to `launch` so backing out of a slow load actually cancels init; hand-applied to the one stock line in the RK-patched `ReaderActivity`, leaving the second `launchNonCancellable` call upstream did not touch (the `kotlinx.coroutines.launch` import already existed). **Shizuku detection** (mihonapp/mihon#3565, mihon `493140c7f` = the new base): `isShizukuInstalled` now probes for the Shizuku permission via `getPermissionInfo(ShizukuProvider.PERMISSION)` in a try/catch instead of a fixed package name plus `Sui.isSui()`; `ContextExtensions.kt` was byte-identical to upstream so taken verbatim (`PackageManager` already imported, and the `dev.rikka.shizuku:provider` artifact `ShizukuProvider` needs is already on the classpath via the `shizuku` bundle). Verified with `:app:compileReleaseKotlin` + `spotlessCheck`. | **mihon `c3b99aea0` (the ViewModel migration): DEFERRED**, not skipped; the base advances around it, see [Deferred upstream changes](#deferred-upstream-changes). `CHANGELOG.md` (Reikai owns its own): added one `Other` credit line for the Shizuku change. |
| `ef1c52967` | `ec5d8c94f` | 2026-07-14 | 4 ported, 0 skipped. All dependency / tooling bumps. **AGP `9.2.1` -> `9.3.0`** (mihonapp/mihon#3593, mihon `ef1c52967` = the new base): the bump carries a required build-script migration, replacing `source-api/build.gradle.kts`'s `consumerKeepRules.apply { publish = true; file(...) }` with the single-call `consumerKeepRules.file("consumer-proguard.pro")`. Taken verbatim (the region is stock; the file's only `// RK` marker is the EXH gallery-metadata note further down), so `source-api` stays byte-identical to upstream, including upstream's drop of the explicit `publish = true`. Kotlin `2.4.0` -> `2.4.10` (mihonapp/mihon#3592, mihon `633eea4ce`) and conscrypt `2.6.0` -> `2.6.1` (mihonapp/mihon#3587, mihon `e9b7c760a`), one line each in `gradle/libs.versions.toml`. `softprops/action-gh-release` pinned SHA v3.0.1 -> v3.0.2 (mihonapp/mihon#3588, mihon `31a4d6ba3`) applied to **both** `release.yml` and `preview.yml`; upstream has only `release.yml`. No RK island touched. Verified with `:app:compileReleaseKotlin`. The full recompile the toolchain bump forces surfaces pre-existing "overrides a deprecated member" warnings in the EXH sources (they override `source-api`'s long-deprecated RxJava `fetch*` API); pre-existing, not from the bump. | None. |
| `ef1e7ba90` | `9645b0717` | 2026-07-14 | 3 ported. **MangaBaka User-Agent fix (mihonapp/mihon#3578, mihon `526d79a97`):** the tracker's outbound User-Agent interpolated `${MR.strings.app_name}`, a `StringResource` object, so it sent the object's `toString()` rather than the resolved app name. Hardcoded the name in `MangaBakaApi.kt` like upstream, but debranded to `Reikai/v...` (upstream uses `Mihon/`); upstream leaves the now-unused `MR` import in place, so Reikai keeps it too to stay byte-faithful. `material-kolor` `5.0.0-alpha07` -> `5.0.0` (mihonapp/mihon#3580, mihon `b9ac9a042`) and `junit-jupiter` `6.1.1` -> `6.1.2` (mihonapp/mihon#3582 = the new base `ef1e7ba90`), one line each in `gradle/libs.versions.toml`. No RK island touched. Verified with `:app:compileReleaseKotlin`. | `CHANGELOG.md` (Reikai owns its own). |
| `d171768cf` | `3c0841a7c` | 2026-07-11 | 1 ported: paged-reader app-bar fix (mihonapp/mihon#3567, mihon `b4635c41a`). Hand-merged an `onPageIndexChangeFinished` callback through `ReaderActivity` -> `ReaderAppBars` -> `ChapterNavigator` (all three `// RK`-patched by the Phase 7 reader-chrome unification), clearing `isScrollingThroughPages` on `SliderState.onValueChangeFinished` so the bars hide on the next tap after a slider drag. Stock-region additions only, no RK island touched. Verified with `:app:compileReleaseKotlin`. | Fastlane store metadata + its Vietnamese translation (mihonapp/mihon#3564, mihonapp/mihon#3576): N/A, Reikai has no `fastlane/` dir, no F-Droid/Play listing pipeline, and the copy is Mihon-branded; the `build.yml` `!fastlane/**` path-ignore targets a workflow Reikai lacks. |
| `217b20123` | `4b539d565` | 2026-07-09 | 3 ported. HTTP 416 resumable-download fix (mihon `4a66b8b5d`): hand-applied to the stock (non-`// RK`) `downloadImage` region of `Downloader.kt` with an import swap (add `HttpException`, drop the now-unused `storage.extension`); the disable + revert resumable-download pair (mihon `3d3bd49d1` + `b2ca6bc5f`) cancels to net-zero, so only the fix lands. `firebase-bom` 34.15.0 -> 34.16.0 (mihon `217b20123` = the new base, mihonapp/mihon#3562), one line in `gradle/libs.versions.toml`. **Deferred Weblate translations (mihonapp/mihon#3477) now resolved** (mihon `52f827c6f`): a scripted per-key 3-way merge across 21 locale files (skipped `az`, whose upstream file is empty) that imports Mihon's translated value for a key only if Reikai's base has it AND Reikai hasn't diverged on it (current locale value matches Mihon's value before that commit), auto-preserving the debrand (`app_name`, `crash_screen_description`) and skipping Mihon-only keys; also dropped the stale `pref_webtoon_vertical_navigator` from 13 locales (Reikai removed it from base when it ported the new vertical navigator, mihonapp/mihon#3531). 71 adds / 10 mods / 13 removes / 0 conflicts; every touched file XML-validated. Verified with `:app:compileReleaseKotlin`. | Release v0.20.1 (mihon `a8d3e3977`): N/A, Reikai keeps its `.y2k` identity, own CHANGELOG + issue templates; its `az/strings.xml` touch is a trailing-newline only. Disable/revert resumable downloads (mihon `3d3bd49d1` + `b2ca6bc5f`): cancel out, nothing to port. `CHANGELOG.md` (Reikai owns its own). |
| `52f827c6f` | `2283d39ff` | 2026-07-09 | 3 ported: Hikka media-type underscore-to-space + nullable `mediaType` (mihonapp/mihon#3560); `com.squareup.zstd` proguard keep (mihon `abef1cb8c`); removed the redundant `disable-code-shrink` build flag, hardcoding `isMinifyEnabled`/`isShrinkResources = true` on release now that the `benchmark` variant already provides a non-minified build (mihon `aa434285b`; verified Reikai carries the `benchmark` variant + `:baseline-profile` module and nothing else references `disable-code-shrink`). All in non-`// RK` lines of otherwise-patched files. Verified with `:app:compileReleaseKotlin`. | Regenerated baseline profiles (mihon `91e967128`): SKIPPED, Reikai generates its own via `:app:generateBaselineProfile`; Mihon's are Mihon-tuned. Weblate translations (mihonapp/mihon#3477): DEFERRED, a 19-file bulk mixing shared keys with Mihon-specific/debranded ones + Weblate metadata, better handled by a scripted translation refresh. `CHANGELOG.md` (Reikai owns its own). |
| `75f506836` | `69c0e33d1` | 2026-07-09 | 11 ported, 0 skipped, in 3 commits (`e0112a25e`..`69c0e33d1`). **Features:** MangaBaka tracker (mihonapp/mihon#3047), with a Reikai `// RK` `getMangaMetadata` + a `genres` field on the series DTO so Fill-from-tracker matches the other online trackers (verified genres exist on `api.mangabaka.org/v1/series`); vertical chapter navigator per reading-mode + height (mihonapp/mihon#3531), with a net-new `VerticalNavigatorMigration` renumbered to `181f` (Reikai's next release code, not Mihon's `25f`, which no Reikai upgrader's range would ever hit) and reguarded on `previousVersion != 0`; reader slider-step fix (mihonapp/mihon#3549). **Fixes / reconciliations:** Hikka unclosed-response fix (mihonapp/mihon#3548) fully covers and retires Reikai's two `// RK` leak islands (interceptor token-refresh success path + `getRead` 404), leaving only the Fill-from-tracker code; `extensionLib` metadata now read via `getFloat` (mihonapp/mihon#3545 + mihonapp/mihon#3559), retiring Reikai's `// RK` versionName-only workaround so `ExtensionLoader` is byte-identical to upstream again. **Bumps:** aboutLibraries v15 (mihonapp/mihon#3553), kotest (mihonapp/mihon#3544), conscrypt (mihonapp/mihon#3552), xmlutil (mihonapp/mihon#3555), setup-java v5.5.0 (mihonapp/mihon#3543) applied to Reikai's own workflows. Verified with `:app:compileReleaseKotlin`; migration exercised on-device at a temporary `versionCode 181`. | `CHANGELOG.md` (Reikai owns its own); no upstream `versionCode` bump adopted |
| `0787678c1` | `ee8b4c45e` | 2026-07-07 | 1: telemetry now checks whether Google Play Services is installed via `PackageManager` instead of the play-services availability API, so the check no longer depends on GMS being present (mihonapp/mihon#3525). Verbatim copy of `telemetry/.../TelemetryConfig.kt`, no RK island. | `README.md` + `CHANGELOG.md` (Reikai owns its own) |
| `d0c79399b` | `bb5c6a5fc` | 2026-07-07 | 3: chapter `memo` now updates for existing chapters (mihonapp/mihon#3538), Hikka plan-to-read default for no-progress titles (mihonapp/mihon#3534), and each tracker's username in Settings > Tracking (mihonapp/mihon#3533). For the username feature: verbatim-copied the marker-free contract/DTO/widget files + the new `MUCurrentUser.kt`; hand-merged the per-tracker login + `getCurrentUser()` changes around Reikai's `// RK` islands; `DummyTracker` needed the two new `Tracker` methods. Adapted three RK recommendation library-pull calls (Kitsu, Shikimori, Bangumi) to the changed `getCurrentUser()` return types so the taste profile still compiles. MdList (Reikai-only) left unwired for display username. | `README.md` + `CHANGELOG.md` (Reikai owns its own) |
| `aa46039e6` | `3f5c29d3e` | 2026-07-07 | 1: Hikka tracker support (mihonapp/mihon#1386), a net-new Ukrainian anime/manga tracker. Verbatim-copied the `data/track/hikka/` package + `brand_hikka.xml`; hand-merged `TrackerManager` (id `10L`, matching upstream), `SettingsTrackingScreen`, `TrackLoginActivity`, `AndroidManifest` around Reikai's existing MdList / MangaDex-OAuth `// RK` islands. No new proguard keep (`hikka` is under the existing `eu.kanade.**` keep). | `README.md` + `CHANGELOG.md` (Reikai owns its own) |
| `3b078331a` | `4acf0a54f` | 2026-07-06 | 1: unifile bump to Mihon's own fork (`com.github.tachiyomiorg` -> `com.github.mihon`, `e0def6b3dc` -> `08f224c8f9`) fixing non-system SAF provider support (mihonapp/mihon#3530). Hand-merged the two `gradle/libs.versions.toml` lines; no RK island. | None |
| `b8e5f22c0` | `414af9e9e` | 2026-07-06 | 1: proguard keep for `Serializable` `writeReplace`/`readResolve` (mihon b8e5f22c0), so the mihonapp/mihon#3523 backgrounding fix survives R8 in Reikai's minified preview/release builds. Verbatim copy, no RK island at the insert. | None |
| `c3bf7a78c` | `71b2a026a` | 2026-07-05 | 1: `Manga`-model serialization for safe backgrounding (mihonapp/mihon#3523). Reconciled the `mihonapp/mihon#3515` divergence: adopted the model-level fix and reverted Reikai's serializable-args notes screen back to upstream's whole-`Manga` `MangaNotesScreen`. | None |
| `94b3b5eaa` | `3e49c63f3` | 2026-07-04 | None (divergence review only) | `94b3b5eaa` revert of `mihonapp/mihon#3515`: Reikai deliberately kept its port at the time (reconciled 2026-07-05, see the row above) |
| `27284a40a` | `2d12e7c88` | 2026-07-04 | 4: notes background-crash serializable args (mihonapp/mihon#3515), notes text-select crash / composeRichEditor rc13 (mihonapp/mihon#3516), Shikimori GraphQL search+lookup+currentUser with a `// RK` not-in-list bind fix (mihonapp/mihon#3499), download-cache invalidation after restore (mihonapp/mihon#3096) | `mihonapp/mihon#3514` download wrong-file-check: Reikai already ships the correct logic (`3b1d34759`) |
| `0772f7202` | `d0cb409f7` | 2026-07-03 | 9: 7 dependency bumps, Shikimori `.io` (mihonapp/mihon#3497), xmlutil v1 + Compose ListItem deprecations (mihonapp/mihon#3507) | `mihonapp/mihon#3504` duplicate-images-on-resume: already fixed by Reikai `3b1d34759`, and upstream's helper logic is inverted |
| `d8c3440d3` | `77c4b0842` | 2026-07-01 | 1: resumable image downloads (mihonapp/mihon#3167) | None |
| `a82ccea6f` | `a5ebecd5b` | 2026-06-27 | 2: Gradle 9.6.1 (mihonapp/mihon#3475), Voyager 2.x (mihonapp/mihon#3466) | `19f1d00fc` Mihon v0.20.0 release |
| `6c6a07c0c` | `3484f2800` | 2026-06-26 | 3: extension-store content warning (mihonapp/mihon#3472), shortcut colors to app module (mihonapp/mihon#3473), setup-java bump | None |
| `735cea35f` | `30f53e211` | 2026-06-24 | 10: backup batching (mihonapp/mihon#3267), locale files, ChapterCache guard, deps | `1cb38a1c3` version-code bump |
| `b3e190c62` | `e9d06bced` | 2026-06-21 | 1: baseline-profile module refactor (mihonapp/mihon#3434) | None |
